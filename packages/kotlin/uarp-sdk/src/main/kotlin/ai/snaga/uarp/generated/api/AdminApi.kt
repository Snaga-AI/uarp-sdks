// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

@file:Suppress("unused", "UNUSED_IMPORT", "RedundantVisibilityModifier")

package ai.snaga.uarp.api

import ai.snaga.uarp.Body
import ai.snaga.uarp.FilePart
import ai.snaga.uarp.Part
import ai.snaga.uarp.RequestOptions
import ai.snaga.uarp.RequestSpec
import ai.snaga.uarp.ServerEvent
import ai.snaga.uarp.UarpClient
import ai.snaga.uarp.autoPaginate
import ai.snaga.uarp.encodePathSegment
import ai.snaga.uarp.uarpJson
import ai.snaga.uarp.models.*
import kotlinx.serialization.encodeToString
import kotlinx.serialization.json.JsonElement
import kotlinx.serialization.json.JsonObject
import kotlinx.coroutines.flow.Flow

/**
 * Platform admin: tenants, audit, reconciliation, DLQ, analytics
 */
public class AdminApi internal constructor(private val client: UarpClient) {
    /**
     * Add addresses to the roster by hand
     *
     * **Super-admin only.** Addresses reach the owner from the Play console and from people who
     * write directly, so the landing form is not the only door. Partial success is normal: each
     * address lands in exactly one of the three lists and the call is still 200.
     *
     * `POST /api/v1/admin/testers/android`
     *
     * Required scopes: `admin`.
     */
    public suspend fun addAndroidTesters(body: AddAndroidTestersRequest, options: RequestOptions = RequestOptions()): AddAndroidTestersResponse {
        return client.request<AddAndroidTestersResponse>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/admin/testers/android",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Platform-wide agent analytics
     *
     * `GET /api/v1/admin/analytics/agents`
     *
     * Required scopes: `admin`.
     */
    public suspend fun adminAnalyticsAgents(days: Long? = null, options: RequestOptions = RequestOptions()): AgentAnalyticsSummary {
        val query = buildList {
            if (days != null) add("days" to days.toString())
        }
        return client.request<AgentAnalyticsSummary>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/analytics/agents",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Platform-wide event analytics
     *
     * `GET /api/v1/admin/analytics/events`
     *
     * Required scopes: `admin`.
     */
    public suspend fun adminAnalyticsEvents(days: Long? = null, limit: Long? = null, type: String? = null, options: RequestOptions = RequestOptions()): AdminAnalyticsEventsResponse {
        val query = buildList {
            if (days != null) add("days" to days.toString())
            if (limit != null) add("limit" to limit.toString())
            if (type != null) add("type" to type)
        }
        return client.request<AdminAnalyticsEventsResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/analytics/events",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Platform-wide analytics overview
     *
     * `GET /api/v1/admin/analytics/overview`
     *
     * Required scopes: `admin`.
     */
    public suspend fun adminAnalyticsOverview(days: Long? = null, options: RequestOptions = RequestOptions()): AdminAnalyticsOverviewResponse {
        val query = buildList {
            if (days != null) add("days" to days.toString())
        }
        return client.request<AdminAnalyticsOverviewResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/analytics/overview",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Full KV scan (admin diagnostic)
     *
     * Dangerous: full DB key scan. Admin-only. Use the namespace-scoped endpoints under
     * `/admin/data-explorer&#47;*` for normal browsing.
     *
     * `GET /api/v1/admin/data-explorer/raw-keys`
     *
     * Required scopes: `admin`.
     */
    public suspend fun adminDataExplorerRawKeys(cursor: String? = null, limit: Long? = null, prefix: String? = null, search: String? = null, options: RequestOptions = RequestOptions()): AdminDataExplorerRawKeysResponse {
        val query = buildList {
            if (cursor != null) add("cursor" to cursor)
            if (limit != null) add("limit" to limit.toString())
            if (prefix != null) add("prefix" to prefix)
            if (search != null) add("search" to search)
        }
        return client.request<AdminDataExplorerRawKeysResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/data-explorer/raw-keys",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Stream every item returned by `adminDataExplorerRawKeys`, following the `cursor` cursor
     * until the server reports no further pages.
     */
    public fun adminDataExplorerRawKeysAll(cursor: String? = null, limit: Long? = null, prefix: String? = null, search: String? = null, options: RequestOptions = RequestOptions()): Flow<AdminDataExplorerRawKeysResponseKey> = autoPaginate(
        fetch = { pageCursor -> adminDataExplorerRawKeys(cursor = pageCursor, limit = limit, prefix = prefix, search = search, options = options) },
        items = { it.keys },
        cursor = { it.cursor },
        hasMore = { null },
    )

    /**
     * Remove OAuth login provider (super-admin)
     *
     * `DELETE /api/v1/admin/oauth-login-providers/{provider}`
     *
     * Required scopes: `admin`.
     */
    public suspend fun adminDeleteOAuthProvider(provider: OAuthLoginProviderConfigStatusProvider, options: RequestOptions = RequestOptions()): OAuthLoginProviderConfigDeleted {
        return client.request<OAuthLoginProviderConfigDeleted>(
            RequestSpec(
                method = "DELETE",
                path = "/api/v1/admin/oauth-login-providers/${encodePathSegment(provider.toString())}",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Admin: get landing-page featured-agent config
     *
     * `GET /api/v1/admin/config/landing`
     *
     * Required scopes: `admin`.
     */
    public suspend fun adminGetLandingConfig(options: RequestOptions = RequestOptions()): AdminGetLandingConfigResponse {
        return client.request<AdminGetLandingConfigResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/config/landing",
                options = options,
            )
        )
    }

    /**
     * Admin: get curated model catalog
     *
     * `GET /api/v1/admin/config/model-catalog`
     *
     * Required scopes: `admin`.
     */
    public suspend fun adminGetModelCatalog(options: RequestOptions = RequestOptions()): AdminModelCatalog {
        return client.request<AdminModelCatalog>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/config/model-catalog",
                options = options,
            )
        )
    }

    /**
     * Admin: model catalog default seed
     *
     * `GET /api/v1/admin/config/model-catalog/seed`
     *
     * Required scopes: `admin`.
     */
    public suspend fun adminGetModelCatalogSeed(options: RequestOptions = RequestOptions()): AdminModelCatalogSeed {
        return client.request<AdminModelCatalogSeed>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/config/model-catalog/seed",
                options = options,
            )
        )
    }

    /**
     * Admin: live per-model pricing (synced from the configured provider)
     *
     * `GET /api/v1/admin/config/model-pricing`
     *
     * Required scopes: `admin`.
     */
    public suspend fun adminGetModelPricing(options: RequestOptions = RequestOptions()): AdminModelPricingList {
        return client.request<AdminModelPricingList>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/config/model-pricing",
                options = options,
            )
        )
    }

    /**
     * Get configured OAuth login provider (super-admin)
     *
     * Returns provider config with the client_secret masked (last 4 chars only). Returns
     * `{configured: false}` for unconfigured providers.
     *
     * `GET /api/v1/admin/oauth-login-providers/{provider}`
     *
     * Required scopes: `admin`.
     */
    public suspend fun adminGetOAuthProvider(provider: OAuthLoginProviderConfigStatusProvider, options: RequestOptions = RequestOptions()): OAuthLoginProviderConfigStatus {
        return client.request<OAuthLoginProviderConfigStatus>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/oauth-login-providers/${encodePathSegment(provider.toString())}",
                options = options,
            )
        )
    }

    /**
     * Get last stored cost-reconciliation result for a tenant
     *
     * `GET /api/v1/admin/reconciliation/{tenantId}`
     *
     * Required scopes: `admin`.
     */
    public suspend fun adminGetReconciliation(tenantId: String, options: RequestOptions = RequestOptions()): AdminGetReconciliationResponse {
        return client.request<AdminGetReconciliationResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/reconciliation/${encodePathSegment(tenantId)}",
                options = options,
            )
        )
    }

    /**
     * Admin: get default voice (STT/TTS) config
     *
     * `GET /api/v1/admin/config/voice`
     *
     * Required scopes: `admin`.
     */
    public suspend fun adminGetVoiceConfig(options: RequestOptions = RequestOptions()): AdminGetVoiceConfigResponse {
        return client.request<AdminGetVoiceConfigResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/config/voice",
                options = options,
            )
        )
    }

    /**
     * Admin: get per-model voice presets
     *
     * `GET /api/v1/admin/config/voice-presets`
     *
     * Required scopes: `admin`.
     */
    public suspend fun adminGetVoicePresets(options: RequestOptions = RequestOptions()): AdminVoicePresets {
        return client.request<AdminVoicePresets>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/config/voice-presets",
                options = options,
            )
        )
    }

    /**
     * Admin: enumerate available built-in tools
     *
     * `GET /api/v1/admin/tools`
     *
     * Required scopes: `admin`.
     */
    public suspend fun adminListTools(options: RequestOptions = RequestOptions()): AdminListToolsResponse {
        return client.request<AdminListToolsResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/tools",
                options = options,
            )
        )
    }

    /**
     * List dead-lettered webhook deliveries
     *
     * `GET /api/v1/admin/webhooks/dlq`
     *
     * Required scopes: `admin`.
     */
    public suspend fun adminListWebhookDLQ(options: RequestOptions = RequestOptions()): AdminListWebhookDLQResponse {
        return client.request<AdminListWebhookDLQResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/webhooks/dlq",
                options = options,
            )
        )
    }

    /**
     * Admin: set landing-page featured-agent config
     *
     * `PUT /api/v1/admin/config/landing`
     *
     * Required scopes: `admin`.
     */
    public suspend fun adminPutLandingConfig(body: JsonObject, options: RequestOptions = RequestOptions()): AdminPutLandingConfigResponse {
        return client.request<AdminPutLandingConfigResponse>(
            RequestSpec(
                method = "PUT",
                path = "/api/v1/admin/config/landing",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Admin: replace curated model catalog
     *
     * `PUT /api/v1/admin/config/model-catalog`
     *
     * Required scopes: `admin`.
     */
    public suspend fun adminPutModelCatalog(body: JsonObject, options: RequestOptions = RequestOptions()): AdminPutModelCatalogResponse {
        return client.request<AdminPutModelCatalogResponse>(
            RequestSpec(
                method = "PUT",
                path = "/api/v1/admin/config/model-catalog",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Set/update OAuth login provider (super-admin)
     *
     * Merge semantics: omitting client_secret keeps the existing one (so toggling `enabled`
     * doesn't require re-pasting the secret). First-time PUT requires both client_id and
     * client_secret. Stored encrypted at rest.
     *
     * `PUT /api/v1/admin/oauth-login-providers/{provider}`
     *
     * Required scopes: `admin`.
     */
    public suspend fun adminPutOAuthProvider(provider: OAuthLoginProviderConfigStatusProvider, body: OAuthLoginProviderConfigUpdate, options: RequestOptions = RequestOptions()): OAuthLoginProviderConfigUpdateResponse {
        return client.request<OAuthLoginProviderConfigUpdateResponse>(
            RequestSpec(
                method = "PUT",
                path = "/api/v1/admin/oauth-login-providers/${encodePathSegment(provider.toString())}",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Admin: set default voice config
     *
     * `PUT /api/v1/admin/config/voice`
     *
     * Required scopes: `admin`.
     */
    public suspend fun adminPutVoiceConfig(body: JsonObject, options: RequestOptions = RequestOptions()): AdminPutVoiceConfigResponse {
        return client.request<AdminPutVoiceConfigResponse>(
            RequestSpec(
                method = "PUT",
                path = "/api/v1/admin/config/voice",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Admin: set per-model voice presets
     *
     * `PUT /api/v1/admin/config/voice-presets`
     *
     * Required scopes: `admin`.
     */
    public suspend fun adminPutVoicePresets(body: JsonObject, options: RequestOptions = RequestOptions()): AdminVoicePresets {
        return client.request<AdminVoicePresets>(
            RequestSpec(
                method = "PUT",
                path = "/api/v1/admin/config/voice-presets",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Replay a dead-lettered webhook event
     *
     * `POST /api/v1/admin/webhooks/dlq/{eventId}/replay`
     *
     * Required scopes: `admin`.
     */
    public suspend fun adminReplayWebhookDLQ(eventId: String, options: RequestOptions = RequestOptions()): AdminReplayWebhookDLQResponse {
        return client.request<AdminReplayWebhookDLQResponse>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/admin/webhooks/dlq/${encodePathSegment(eventId)}/replay",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Write a post by hand
     *
     * `source` is stamped `manual` and cannot be set by the caller. The slug is derived from the
     * title and made unique; `status` defaults to `draft`.
     *
     * `POST /api/v1/admin/blog/posts`
     *
     * Required scopes: `admin`.
     */
    public suspend fun createAdminBlogPost(body: CreateAdminBlogPostRequest, options: RequestOptions = RequestOptions()): CreateAdminBlogPostResponse {
        return client.request<CreateAdminBlogPostResponse>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/admin/blog/posts",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Create custom provider
     *
     * `POST /api/v1/admin/providers`
     *
     * Required scopes: `admin`.
     */
    public suspend fun createAdminProvider(body: JsonObject, options: RequestOptions = RequestOptions()): CreateAdminProviderResponse {
        return client.request<CreateAdminProviderResponse>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/admin/providers",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Create new tenant
     *
     * `POST /api/v1/admin/tenants`
     *
     * Required scopes: `admin`.
     */
    public suspend fun createTenant(body: JsonObject, options: RequestOptions = RequestOptions()): Tenant {
        return client.request<Tenant>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/admin/tenants",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Delete a post
     *
     * `DELETE /api/v1/admin/blog/posts/{postId}`
     *
     * Required scopes: `admin`.
     */
    public suspend fun deleteAdminBlogPost(postId: String, options: RequestOptions = RequestOptions()): DeleteAdminBlogPostResponse {
        return client.request<DeleteAdminBlogPostResponse>(
            RequestSpec(
                method = "DELETE",
                path = "/api/v1/admin/blog/posts/${encodePathSegment(postId)}",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Remove one provider's stored OAuth credentials
     *
     * Deletes the record outright — this is the only way to clear a stored credential, since the
     * PUT treats a blank value as "keep". Idempotent: deleting a provider that has nothing stored
     * is still 200.
     *
     * `DELETE /api/v1/admin/integration-oauth-providers/{provider}`
     *
     * Required scopes: `admin`.
     */
    public suspend fun deleteAdminIntegrationOAuthProvider(provider: GetAdminIntegrationOAuthProviderProvider, options: RequestOptions = RequestOptions()): DeleteAdminIntegrationOAuthProviderResponse {
        return client.request<DeleteAdminIntegrationOAuthProviderResponse>(
            RequestSpec(
                method = "DELETE",
                path = "/api/v1/admin/integration-oauth-providers/${encodePathSegment(provider.toString())}",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Remove the platform API key for a provider
     *
     * Drops the stored platform key. The PROVIDER survives — this is the key, not the definition;
     * `DELETE /api/v1/admin/providers/{providerId}` is the other one, and it removes a custom
     * provider outright.
     *
     * Answers `configured: false` for the provider, which is the same field `GET
     * /api/v1/admin/llm-defaults` reports per provider, so the caller can apply the answer without
     * a re-read.
     *
     * `DELETE /api/v1/admin/llm-defaults/{providerId}`
     *
     * Required scopes: `admin`.
     */
    public suspend fun deleteAdminLLMDefault(providerId: String, options: RequestOptions = RequestOptions()): DeleteAdminLLMDefaultResponse {
        return client.request<DeleteAdminLLMDefaultResponse>(
            RequestSpec(
                method = "DELETE",
                path = "/api/v1/admin/llm-defaults/${encodePathSegment(providerId)}",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Remove a custom provider (super-admin, fresh MFA)
     *
     * Removes a CUSTOM provider and its settings. A built-in provider is not deletable and answers
     * 404 here — that 404 means "no such CUSTOM provider", not "no such provider", which is worth
     * knowing before reading it as a routing mistake.
     *
     * Gated on a fresh MFA challenge because it is irreversible: the definition and its settings
     * are deleted outright, not disabled. To stop using a provider without losing it, PATCH
     * `enabled: false` instead. To drop the platform API KEY while keeping the provider, use
     * `DELETE /api/v1/admin/llm-defaults/{providerId}` — a different route with a different
     * subject.
     *
     * `DELETE /api/v1/admin/providers/{providerId}`
     *
     * Required scopes: `admin`.
     */
    public suspend fun deleteAdminProvider(providerId: String, options: RequestOptions = RequestOptions()): DeleteAdminProviderResponse {
        return client.request<DeleteAdminProviderResponse>(
            RequestSpec(
                method = "DELETE",
                path = "/api/v1/admin/providers/${encodePathSegment(providerId)}",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Take one address off the roster
     *
     * **Super-admin only.** The address is a path segment, so it arrives percent-encoded.
     *
     * `DELETE /api/v1/admin/testers/android/{email}`
     *
     * Required scopes: `admin`.
     */
    public suspend fun deleteAndroidTester(email: String, options: RequestOptions = RequestOptions()) {
        client.requestUnit(
            RequestSpec(
                method = "DELETE",
                path = "/api/v1/admin/testers/android/${encodePathSegment(email)}",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Run the authoring agent now and create a post
     *
     * Generates immediately, ignoring `frequency` — this is the operator's manual trigger, not a
     * schedule nudge. Whether the result lands published or as a draft follows the stored
     * `auto_publish`.
     *
     * A generation that FAILS is **422**, not a 200 carrying an error field, so a client reads the
     * status. Success is **201**.
     *
     * `POST /api/v1/admin/blog/generate`
     *
     * Required scopes: `admin`.
     */
    public suspend fun generateAdminBlogPost(options: RequestOptions = RequestOptions()): GenerateAdminBlogPostResponse {
        return client.request<GenerateAdminBlogPostResponse>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/admin/blog/generate",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Get the blog's authoring configuration
     *
     * Super-admin only. Wrapped in `{config}` rather than returned bare — the whole blog surface
     * uses envelopes.
     *
     * `GET /api/v1/admin/blog/config`
     *
     * Required scopes: `admin`.
     */
    public suspend fun getAdminBlogConfig(options: RequestOptions = RequestOptions()): GetAdminBlogConfigResponse {
        return client.request<GetAdminBlogConfigResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/blog/config",
                options = options,
            )
        )
    }

    /**
     * Read one provider's stored OAuth credentials (secret masked)
     *
     * **The response has two shapes and a client must handle both.** With nothing stored it is the
     * three-field form — `{provider, enabled: false, configured: false}` — and `client_id`,
     * `client_secret_hint` and `scopes` are ABSENT, not null. With a record stored, all six are
     * present.
     *
     * The secret is never echoed. `client_secret_hint` is the last four characters behind dots,
     * enough for an operator to confirm which credential is stored without seeing it, and it is
     * null when the stored secret is empty.
     *
     * `GET /api/v1/admin/integration-oauth-providers/{provider}`
     *
     * Required scopes: `admin`.
     */
    public suspend fun getAdminIntegrationOAuthProvider(provider: GetAdminIntegrationOAuthProviderProvider, options: RequestOptions = RequestOptions()): GetAdminIntegrationOAuthProviderResponse {
        return client.request<GetAdminIntegrationOAuthProviderResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/integration-oauth-providers/${encodePathSegment(provider.toString())}",
                options = options,
            )
        )
    }

    /**
     * List platform API keys (masked)
     *
     * `GET /api/v1/admin/llm-defaults`
     *
     * Required scopes: `admin`.
     */
    public suspend fun getAdminLLMDefaults(options: RequestOptions = RequestOptions()): GetAdminLLMDefaultsResponse {
        return client.request<GetAdminLLMDefaultsResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/llm-defaults",
                options = options,
            )
        )
    }

    /**
     * Get pricing configuration
     *
     * `GET /api/v1/admin/config/pricing`
     *
     * Required scopes: `admin`.
     */
    public suspend fun getAdminPricing(options: RequestOptions = RequestOptions()): AdminPricingConfig {
        return client.request<AdminPricingConfig>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/config/pricing",
                options = options,
            )
        )
    }

    /**
     * Get single provider
     *
     * `GET /api/v1/admin/providers/{providerId}`
     *
     * Required scopes: `admin`.
     */
    public suspend fun getAdminProvider(providerId: String, options: RequestOptions = RequestOptions()): AdminProvider {
        return client.request<AdminProvider>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/providers/${encodePathSegment(providerId)}",
                options = options,
            )
        )
    }

    /**
     * Aggregate usage stats
     *
     * `GET /api/v1/admin/stats`
     *
     * Required scopes: `admin`.
     */
    public suspend fun getAdminStats(options: RequestOptions = RequestOptions()): GetAdminStatsResponse {
        return client.request<GetAdminStatsResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/stats",
                options = options,
            )
        )
    }

    /**
     * Trace delegation chain
     *
     * `GET /api/v1/admin/trace/{traceId}`
     *
     * Required scopes: `admin`.
     */
    public suspend fun getAdminTrace(traceId: String, options: RequestOptions = RequestOptions()): GetAdminTraceResponse {
        return client.request<GetAdminTraceResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/trace/${encodePathSegment(traceId)}",
                options = options,
            )
        )
    }

    /**
     * Get audit entries for a specific target
     *
     * `GET /api/v1/admin/audit/{targetId}`
     *
     * Required scopes: `admin`.
     */
    public suspend fun getAuditForTarget(targetId: String, type: String? = null, options: RequestOptions = RequestOptions()): AdminAuditList {
        val query = buildList {
            if (type != null) add("type" to type)
        }
        return client.request<AdminAuditList>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/audit/${encodePathSegment(targetId)}",
                query = query,
                options = options,
            )
        )
    }

    /**
     * EU AI Act conformity report
     *
     * `GET /api/v1/admin/conformity-report`
     *
     * Required scopes: `admin`.
     */
    public suspend fun getConformityReport(options: RequestOptions = RequestOptions()): ConformityReport {
        return client.request<ConformityReport>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/conformity-report",
                options = options,
            )
        )
    }

    /**
     * Immutable audit log
     *
     * `GET /api/v1/admin/immutable-audit`
     *
     * Required scopes: `admin`.
     */
    public suspend fun getImmutableAudit(agentId: String? = null, event: String? = null, from: String? = null, to: String? = null, limit: Long? = null, options: RequestOptions = RequestOptions()): GetImmutableAuditResponse {
        val query = buildList {
            if (agentId != null) add("agent_id" to agentId)
            if (event != null) add("event" to event)
            if (from != null) add("from" to from)
            if (to != null) add("to" to to)
            if (limit != null) add("limit" to limit.toString())
        }
        return client.request<GetImmutableAuditResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/immutable-audit",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Full maintenance record
     *
     * The whole record, including who turned it on and when — the audit trail the public status
     * deliberately omits. **Super-admin only**, and a caller on the synthetic default tenant is
     * 401 rather than 403.
     *
     * `GET /api/v1/admin/maintenance`
     *
     * Required scopes: `admin`.
     */
    public suspend fun getMaintenanceState(options: RequestOptions = RequestOptions()): MaintenanceState {
        return client.request<MaintenanceState>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/maintenance",
                options = options,
            )
        )
    }

    /**
     * Platform revenue, host cost and margin
     *
     * Stripe subscriptions against real DigitalOcean spend, with the computed margin.
     * **Super-admin only.**
     *
     * Served from a short-lived cache; `cache` says whether this response was a hit, a miss, or a
     * forced recomputation. A Stripe or provider outage does not fail the call — the affected
     * block carries `error` and the rest is still served, so a partial answer is never mistaken
     * for zeros.
     *
     * `GET /api/v1/admin/economics`
     *
     * Required scopes: `admin`.
     */
    public suspend fun getPlatformEconomics(refresh: DeleteCustomPlanForce? = null, options: RequestOptions = RequestOptions()): PlatformEconomics {
        val query = buildList {
            if (refresh != null) add("refresh" to refresh.value)
        }
        return client.request<PlatformEconomics>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/economics",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Get tenant details
     *
     * `GET /api/v1/admin/tenants/{tenantId}`
     *
     * Required scopes: `admin`.
     */
    public suspend fun getTenant(tenantId: String, options: RequestOptions = RequestOptions()): Tenant {
        return client.request<Tenant>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/tenants/${encodePathSegment(tenantId)}",
                options = options,
            )
        )
    }

    /**
     * One tenant's mission-framework overrides, and what they resolve to
     *
     * Answers both layers at once: `mef_config` is what an operator stored for this tenant,
     * `effective` is what the runtime will actually do. They differ whenever the platform is the
     * deciding factor — with the mission service absent, every effective flag is false no matter
     * what the tenant record says, so an operator reading only `mef_config` sees settings that do
     * nothing.
     *
     * `mef_config` is `null` when nothing is overridden, not an empty object.
     *
     * `GET /api/v1/admin/tenants/{tenantId}/mef-config`
     *
     * Required scopes: `admin`.
     */
    public suspend fun getTenantMefConfig(tenantId: String, options: RequestOptions = RequestOptions()): TenantMefConfigResponse {
        return client.request<TenantMefConfigResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/tenants/${encodePathSegment(tenantId)}/mef-config",
                options = options,
            )
        )
    }

    /**
     * Get tenant usage metrics
     *
     * `GET /api/v1/admin/tenants/{tenantId}/usage`
     *
     * Required scopes: `admin`.
     */
    public suspend fun getTenantUsage(tenantId: String, period: String? = null, options: RequestOptions = RequestOptions()): GetTenantUsageResponse {
        val query = buildList {
            if (period != null) add("period" to period)
        }
        return client.request<GetTenantUsageResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/tenants/${encodePathSegment(tenantId)}/usage",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Internal domain verification
     *
     * `GET /api/v1/internal/verify-domain`
     */
    public suspend fun internalVerifyDomain(options: RequestOptions = RequestOptions()): InternalVerifyDomainResponse {
        return client.request<InternalVerifyDomainResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/internal/verify-domain",
                options = options,
            )
        )
    }

    /**
     * List every post, drafts included
     *
     * The admin view: unlike the public blog read, drafts are included. Unpaged.
     *
     * `GET /api/v1/admin/blog/posts`
     *
     * Required scopes: `admin`.
     */
    public suspend fun listAdminBlogPosts(options: RequestOptions = RequestOptions()): ListAdminBlogPostsResponse {
        return client.request<ListAdminBlogPostsResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/blog/posts",
                options = options,
            )
        )
    }

    /**
     * Every tenant's custom domain, worst first
     *
     * **The order is the product.** Rows are sorted by problem severity — failed, then drift, then
     * renewal_due, then pending, then healthy — so an operator's eye lands on what is broken. A
     * client that re-sorts alphabetically throws that away and should sort back, or not sort at
     * all.
     *
     * Tenants with no custom domain are omitted entirely, so `count` is the number of configured
     * domains and not the number of tenants.
     *
     * Bounded: the tenant registry scan takes at most 5000 entries in one pass and is not paged,
     * so on a platform past that size this list is silently partial.
     *
     * `GET /api/v1/admin/domains/health`
     *
     * Required scopes: `admin`.
     */
    public suspend fun listAdminDomainHealth(options: RequestOptions = RequestOptions()): ListAdminDomainHealthResponse {
        return client.request<ListAdminDomainHealthResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/domains/health",
                options = options,
            )
        )
    }

    /**
     * Which integration providers an operator has configured
     *
     * Super-admin only, and never returns a secret — provider id, an enabled bit, and whether
     * credentials exist at all. The dashboard uses it to decide which connector rows need a
     * "Configure" call to action.
     *
     * Distinct from `/api/v1/integrations/catalog`, which is tenant-facing and lists every known
     * connector regardless of OAuth-readiness.
     *
     * The list is the SUPPORTED set, not the stored set: every supported provider appears, with
     * `configured: false` where nothing is stored. So an empty result means the supported set is
     * empty, never that nothing is configured.
     *
     * `GET /api/v1/admin/integration-oauth-providers`
     *
     * Required scopes: `admin`.
     */
    public suspend fun listAdminIntegrationOAuthProviders(options: RequestOptions = RequestOptions()): ListAdminIntegrationOAuthProvidersResponse {
        return client.request<ListAdminIntegrationOAuthProvidersResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/integration-oauth-providers",
                options = options,
            )
        )
    }

    /**
     * List providers with admin settings
     *
     * `GET /api/v1/admin/providers`
     *
     * Required scopes: `admin`.
     */
    public suspend fun listAdminProviders(options: RequestOptions = RequestOptions()): ListAdminProvidersResponse {
        return client.request<ListAdminProvidersResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/providers",
                options = options,
            )
        )
    }

    /**
     * The Android closed-testing roster
     *
     * **Super-admin only.** The addresses the owner works from when adding people to the Play
     * group. `count`, `not_yet_emailed` and `given_up` describe THIS PAGE, not the whole roster:
     * all three are computed over the rows returned, so a roster longer than `limit` under-reports
     * until every page is walked with `cursor`. `not_yet_emailed` counts rows no letter has gone
     * to; `given_up` counts the ones the backfill has stopped retrying after repeated refusals,
     * which need an eye rather than another pass.
     *
     * `GET /api/v1/admin/testers/android`
     *
     * Required scopes: `admin`.
     */
    public suspend fun listAndroidTesters(limit: Long? = null, cursor: String? = null, options: RequestOptions = RequestOptions()): ListAndroidTestersResponse {
        val query = buildList {
            if (limit != null) add("limit" to limit.toString())
            if (cursor != null) add("cursor" to cursor)
        }
        return client.request<ListAndroidTestersResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/testers/android",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Stream every item returned by `listAndroidTesters`, following the `cursor` cursor until the
     * server reports no further pages.
     */
    public fun listAndroidTestersAll(limit: Long? = null, cursor: String? = null, options: RequestOptions = RequestOptions()): Flow<AndroidTester> = autoPaginate(
        fetch = { pageCursor -> listAndroidTesters(limit = limit, cursor = pageCursor, options = options) },
        items = { it.testers },
        cursor = { it.cursor },
        hasMore = { null },
    )

    /**
     * The reports inbox
     *
     * Every report from every tenant, newest first. **Super-admin only.** `new_count` counts the
     * unresolved reports in the returned set, so a filtered list does not silently under-report
     * the backlog.
     *
     * `GET /api/v1/admin/feedback`
     *
     * Required scopes: `admin`.
     */
    public suspend fun listFeedback(status: ErrorReportStatus? = null, limit: Long? = null, options: RequestOptions = RequestOptions()): ListFeedbackResponse {
        val query = buildList {
            if (status != null) add("status" to status.value)
            if (limit != null) add("limit" to limit.toString())
        }
        return client.request<ListFeedbackResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/feedback",
                query = query,
                options = options,
            )
        )
    }

    /**
     * List all tenants (super admin only)
     *
     * `GET /api/v1/admin/tenants`
     *
     * Required scopes: `admin`.
     */
    public suspend fun listTenants(options: RequestOptions = RequestOptions()): ListTenantsResponse {
        return client.request<ListTenantsResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/tenants",
                options = options,
            )
        )
    }

    /**
     * Purge tenant
     *
     * `DELETE /api/v1/admin/tenants/{tenantId}`
     *
     * Required scopes: `admin`.
     */
    public suspend fun purgeAdminTenant(tenantId: String, options: RequestOptions = RequestOptions()): PurgeAdminTenantResponse {
        return client.request<PurgeAdminTenantResponse>(
            RequestSpec(
                method = "DELETE",
                path = "/api/v1/admin/tenants/${encodePathSegment(tenantId)}",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Query admin audit log
     *
     * `GET /api/v1/admin/audit`
     *
     * Required scopes: `admin`.
     */
    public suspend fun queryAuditLog(limit: Long? = null, action: String? = null, actorId: String? = null, options: RequestOptions = RequestOptions()): AdminAuditList {
        val query = buildList {
            if (limit != null) add("limit" to limit.toString())
            if (action != null) add("action" to action)
            if (actorId != null) add("actor_id" to actorId)
        }
        return client.request<AdminAuditList>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/audit",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Reactivate a suspended tenant
     *
     * `PUT /api/v1/admin/tenants/{tenantId}/reactivate`
     *
     * Required scopes: `admin`.
     */
    public suspend fun reactivateTenant(tenantId: String, options: RequestOptions = RequestOptions()): ReactivateTenantResponse {
        return client.request<ReactivateTenantResponse>(
            RequestSpec(
                method = "PUT",
                path = "/api/v1/admin/tenants/${encodePathSegment(tenantId)}/reactivate",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Store or update one provider's OAuth credentials
     *
     * WRITE SEMANTICS: merges, and the merge is what makes the route usable. An omitted or blank
     * `client_id` or `client_secret` keeps the stored one, so an operator can flip `enabled` or
     * rotate `scopes` WITHOUT re-pasting a secret they cannot read back. Blank counts as omitted
     * here: a whitespace-only value does not clear anything.
     *
     * The consequence is that there is no way to clear a credential through this route — DELETE
     * the provider instead.
     *
     * A FIRST write still needs both: with no stored record and either missing, the answer is 400.
     * `enabled` defaults to true on a first write and otherwise keeps its stored value. A present
     * `scopes` REPLACES the stored list.
     *
     * The response is the short form, not the record: `{provider, enabled, configured: true}`,
     * with no echo of the credentials just written.
     *
     * `PUT /api/v1/admin/integration-oauth-providers/{provider}`
     *
     * Required scopes: `admin`.
     */
    public suspend fun setAdminIntegrationOAuthProvider(provider: GetAdminIntegrationOAuthProviderProvider, body: SetAdminIntegrationOAuthProviderRequest, options: RequestOptions = RequestOptions()): SetAdminIntegrationOAuthProviderResponse {
        return client.request<SetAdminIntegrationOAuthProviderResponse>(
            RequestSpec(
                method = "PUT",
                path = "/api/v1/admin/integration-oauth-providers/${encodePathSegment(provider.toString())}",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Set platform API key
     *
     * `PUT /api/v1/admin/llm-defaults/{providerId}`
     *
     * Required scopes: `admin`.
     */
    public suspend fun setAdminLLMDefault(providerId: String, body: SetAdminLLMDefaultRequest, options: RequestOptions = RequestOptions()): SetAdminLLMDefaultResponse {
        return client.request<SetAdminLLMDefaultResponse>(
            RequestSpec(
                method = "PUT",
                path = "/api/v1/admin/llm-defaults/${encodePathSegment(providerId)}",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Set default model config
     *
     * `PUT /api/v1/admin/llm-defaults/model-config`
     *
     * Required scopes: `admin`.
     */
    public suspend fun setAdminModelConfig(body: JsonObject, options: RequestOptions = RequestOptions()): SetAdminModelConfigResponse {
        return client.request<SetAdminModelConfigResponse>(
            RequestSpec(
                method = "PUT",
                path = "/api/v1/admin/llm-defaults/model-config",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Turn maintenance mode on or off (super-admin)
     *
     * Undocumented until now, while the GET beside it was described in full — so a generated
     * client could READ the maintenance state and had no way to change it. The web has been
     * calling this all along (`lib/hooks/use-maintenance-mode.ts`).
     *
     * Answers the whole record back, the same shape the GET serves, so a client need not re-read
     * to learn `enabled_at` and `enabled_by_email`.
     *
     * WRITE SEMANTICS: replaces. The record is rebuilt from this body and written whole; nothing
     * is read first. Turning maintenance ON without a `message` DROPS the message a previous ON
     * had set, and turning it OFF wipes the message unconditionally, keeping only the timestamp
     * and the actor so the audit trail still shows who closed the window. `enabled_at` and
     * `enabled_by_email` are stamped on EVERY call, including one that changes nothing.
     *
     * `PUT /api/v1/admin/maintenance`
     *
     * Required scopes: `admin`.
     */
    public suspend fun setMaintenanceState(body: SetMaintenanceStateRequest, options: RequestOptions = RequestOptions()): MaintenanceState {
        return client.request<MaintenanceState>(
            RequestSpec(
                method = "PUT",
                path = "/api/v1/admin/maintenance",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Suspend a tenant
     *
     * `PUT /api/v1/admin/tenants/{tenantId}/suspend`
     *
     * Required scopes: `admin`.
     */
    public suspend fun suspendTenant(tenantId: String, body: SuspendTenantRequest? = null, options: RequestOptions = RequestOptions()): SuspendTenantResponse {
        return client.request<SuspendTenantResponse>(
            RequestSpec(
                method = "PUT",
                path = "/api/v1/admin/tenants/${encodePathSegment(tenantId)}/suspend",
                body = body?.let { Body.Json(uarpJson.encodeToString(it)) },
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Pull one provider's model list into the catalogue
     *
     * Asks the provider what models it offers and MERGES the result into the platform catalogue.
     * Additive only — nothing is removed, so a model the provider has withdrawn stays in the
     * catalogue until it is deleted deliberately.
     *
     * CUSTOM providers only: the 404 means "no custom provider with that id", so a built-in
     * provider id is also 404 here. `added` counts new entries, `scanned` is the provider's
     * reported inventory, and `total` is the catalogue size after the merge — so `added` is zero
     * on a run that changed nothing, which is the normal result of a second run.
     *
     * `POST /api/v1/admin/providers/{providerId}/sync-models`
     *
     * Required scopes: `admin`.
     */
    public suspend fun syncProviderModels(providerId: String, options: RequestOptions = RequestOptions()): SyncProviderModelsResponse {
        return client.request<SyncProviderModelsResponse>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/admin/providers/${encodePathSegment(providerId)}/sync-models",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Update the blog's authoring configuration
     *
     * WRITE SEMANTICS: merges. The body is spread over the stored record, so an omitted field
     * keeps its value.
     *
     * One field is not a plain merge: setting `agent_id` also pins `agent_tenant_id` to the
     * CALLING tenant, because the cron that auto-writes posts runs without a request context and
     * would otherwise have no tenant to run the agent in. Clearing `agent_id` to `null` nulls
     * both. `agent_tenant_id` is therefore never sent by a client and never has to be — it is
     * derived.
     *
     * Answers the stored record, so a client sees what took effect.
     *
     * `PUT /api/v1/admin/blog/config`
     *
     * Required scopes: `admin`.
     */
    public suspend fun updateAdminBlogConfig(body: UpdateAdminBlogConfigRequest, options: RequestOptions = RequestOptions()): UpdateAdminBlogConfigResponse {
        return client.request<UpdateAdminBlogConfigResponse>(
            RequestSpec(
                method = "PUT",
                path = "/api/v1/admin/blog/config",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Edit a post
     *
     * WRITE SEMANTICS: merges. An omitted field keeps its value; `tags` present REPLACES the list.
     *
     * Three things move on their own and a client should not try to send them. Editing the `title`
     * or the `body` re-stamps `source` to `manual`, even on a post the agent wrote — the record
     * then says who last shaped it rather than who started it. Changing the `title` mints a new
     * unique `slug`, so a published post's URL changes under it. And `published_at` follows
     * `status`: it is stamped on the first transition to `published` and set back to null on
     * `draft`, so a republished post carries a NEW timestamp rather than its original one.
     *
     * `PATCH /api/v1/admin/blog/posts/{postId}`
     *
     * Required scopes: `admin`.
     */
    public suspend fun updateAdminBlogPost(postId: String, body: UpdateAdminBlogPostRequest, options: RequestOptions = RequestOptions()): UpdateAdminBlogPostResponse {
        return client.request<UpdateAdminBlogPostResponse>(
            RequestSpec(
                method = "PATCH",
                path = "/api/v1/admin/blog/posts/${encodePathSegment(postId)}",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Update pricing configuration
     *
     * `PUT /api/v1/admin/config/pricing`
     *
     * Required scopes: `admin`.
     */
    public suspend fun updateAdminPricing(body: JsonObject? = null, options: RequestOptions = RequestOptions()): UpdateAdminPricingResponse {
        return client.request<UpdateAdminPricingResponse>(
            RequestSpec(
                method = "PUT",
                path = "/api/v1/admin/config/pricing",
                body = body?.let { Body.Json(uarpJson.encodeToString(it)) },
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Update provider
     *
     * `PATCH /api/v1/admin/providers/{providerId}`
     *
     * Required scopes: `admin`.
     */
    public suspend fun updateAdminProvider(providerId: String, body: JsonObject, options: RequestOptions = RequestOptions()): UpdateAdminProviderResponse {
        return client.request<UpdateAdminProviderResponse>(
            RequestSpec(
                method = "PATCH",
                path = "/api/v1/admin/providers/${encodePathSegment(providerId)}",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Update tenant settings
     *
     * `PATCH /api/v1/admin/tenants/{tenantId}/settings`
     *
     * Required scopes: `admin`.
     */
    public suspend fun updateAdminTenantSettings(tenantId: String, body: JsonObject, options: RequestOptions = RequestOptions()): UpdateAdminTenantSettingsResponse {
        return client.request<UpdateAdminTenantSettingsResponse>(
            RequestSpec(
                method = "PATCH",
                path = "/api/v1/admin/tenants/${encodePathSegment(tenantId)}/settings",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Mark one feedback report resolved, or reopen it
     *
     * **Only the exact string `resolved` resolves a report; every other value sets it to `new`.**
     * There is no validation and no error path: `"Resolved"`, `"closed"`, a typo, a missing
     * `status`, or a body that is not JSON at all are each accepted with 200 and REOPEN a resolved
     * report. A client must send the literal value and must not rely on being told when it did
     * not.
     *
     * WRITE SEMANTICS: replaces the status field only; nothing else on the report is touched.
     *
     * `PATCH /api/v1/admin/feedback/{reportId}`
     *
     * Required scopes: `admin`.
     */
    public suspend fun updateFeedbackReportStatus(reportId: String, body: UpdateFeedbackReportStatusRequest? = null, options: RequestOptions = RequestOptions()): UpdateFeedbackReportStatusResponse {
        return client.request<UpdateFeedbackReportStatusResponse>(
            RequestSpec(
                method = "PATCH",
                path = "/api/v1/admin/feedback/${encodePathSegment(reportId)}",
                body = body?.let { Body.Json(uarpJson.encodeToString(it)) },
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Set or clear one tenant's mission-framework overrides
     *
     * WRITE SEMANTICS: merges, per key. Only the four known keys are read; a key the body omits
     * keeps its stored value, and a key sent as `null` CLEARS that override so the flag falls back
     * to the platform default. Unknown keys are ignored silently rather than rejected.
     *
     * A non-boolean, non-null value for a known key is **422**. Note the title on that one is
     * `ValidationError` without a space, unlike the `Validation Error` used elsewhere on this
     * surface.
     *
     * When clearing the last override leaves nothing set, the whole `mef_config` is dropped rather
     * than stored as `{}` — a later read answers `null`. And every accepted request writes: even a
     * body that changes nothing stamps a new `updated_at` on the tenant record.
     *
     * Answers the same body as the GET, so a client sees both the stored overrides and what they
     * now resolve to.
     *
     * `PATCH /api/v1/admin/tenants/{tenantId}/mef-config`
     *
     * Required scopes: `admin`.
     */
    public suspend fun updateTenantMefConfig(tenantId: String, body: UpdateTenantMefConfigRequest, options: RequestOptions = RequestOptions()): TenantMefConfigResponse {
        return client.request<TenantMefConfigResponse>(
            RequestSpec(
                method = "PATCH",
                path = "/api/v1/admin/tenants/${encodePathSegment(tenantId)}/mef-config",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Set a tenant's plan (super-admin)
     *
     * The operator's manual plan grant, undocumented while GET, PATCH and DELETE on this same path
     * were described. It is not a general tenant update: `plan` is required and it is what the
     * route is for.
     *
     * WRITE SEMANTICS: mixed. The write is a compare-and-set MERGE onto the current record, so
     * `name`, `slug` and `quota_overrides` keep their stored values when omitted. `plan` and
     * `quotas` do not: both are written on every call, and omitting `quotas` REPLACES the tenant
     * quotas with the resolved plan defaults rather than leaving them alone. A caller raising one
     * dimension must send `quota_overrides`, not `quotas`.
     *
     * `quotas` REPLACES the plan's quotas for this tenant. `quota_overrides` is the durable one —
     * a partial grant that survives a later Stripe subscription change, where a plain `quotas`
     * write does not. When a tenant's plan keeps reverting, the override is the field that makes
     * it stick, and it must be set BEFORE the plan is put back, not after.
     *
     * Granting any paid plan clears a stale `billing_status` (a "cancelled" left over from an
     * earlier Stripe cancellation becomes "active"); a downgrade to free leaves the status alone,
     * since free is never gated.
     *
     * `PUT /api/v1/admin/tenants/{tenantId}`
     *
     * Required scopes: `admin`.
     */
    public suspend fun updateTenantPlan(tenantId: String, body: UpdateTenantPlanRequest, options: RequestOptions = RequestOptions()): UpdateTenantPlanResponse {
        return client.request<UpdateTenantPlanResponse>(
            RequestSpec(
                method = "PUT",
                path = "/api/v1/admin/tenants/${encodePathSegment(tenantId)}",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }
}

/**
 * Platform admin: tenants, audit, reconciliation, DLQ, analytics
 */
public val UarpClient.admin: AdminApi get() = AdminApi(this)
