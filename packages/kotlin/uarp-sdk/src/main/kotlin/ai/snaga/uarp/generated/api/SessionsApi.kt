// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

@file:Suppress("unused", "UNUSED_IMPORT", "RedundantVisibilityModifier")

package ai.snaga.uarp.api

import ai.snaga.uarp.Body
import ai.snaga.uarp.FilePart
import ai.snaga.uarp.Part
import ai.snaga.uarp.RequestOptions
import ai.snaga.uarp.RequestSpec
import ai.snaga.uarp.ServerEvent
import ai.snaga.uarp.UarpClient
import ai.snaga.uarp.autoPaginate
import ai.snaga.uarp.encodePathSegment
import ai.snaga.uarp.uarpJson
import ai.snaga.uarp.models.*
import kotlinx.serialization.encodeToString
import kotlinx.serialization.json.JsonElement
import kotlinx.serialization.json.JsonObject
import kotlinx.coroutines.flow.Flow

/**
 * Conversation sessions
 */
public class SessionsApi internal constructor(private val client: UarpClient) {
    /**
     * Switch active branch
     *
     * `PUT /api/v1/sessions/{sessionId}/branches/{branchId}/activate`
     *
     * Required scopes: `sessions:write`.
     */
    public suspend fun activateSessionBranch(sessionId: String, branchId: String, options: RequestOptions = RequestOptions()): Session {
        return client.request<Session>(
            RequestSpec(
                method = "PUT",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/branches/${encodePathSegment(branchId)}/activate",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Close a session
     *
     * `DELETE /api/v1/sessions/{sessionId}`
     *
     * Required scopes: `sessions:write`.
     */
    public suspend fun closeSession(sessionId: String, options: RequestOptions = RequestOptions()): JsonElement {
        return client.request<JsonElement>(
            RequestSpec(
                method = "DELETE",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Confirm or cancel a todo execution
     *
     * `POST /api/v1/sessions/{sessionId}/todos/{todoId}/confirm`
     *
     * Required scopes: `sessions:write`.
     */
    public suspend fun confirmSessionTodo(sessionId: String, todoId: String, body: JsonObject? = null, options: RequestOptions = RequestOptions()): JsonObject {
        return client.request<JsonObject>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/todos/${encodePathSegment(todoId)}/confirm",
                body = body?.let { Body.Json(uarpJson.encodeToString(it)) },
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Create a session
     *
     * `POST /api/v1/sessions`
     *
     * Required scopes: `sessions:write`.
     */
    public suspend fun create(body: CreateSessionRequest, options: RequestOptions = RequestOptions()): Session {
        return client.request<Session>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/sessions",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Create annotation
     *
     * `POST /api/v1/sessions/{sessionId}/annotations`
     *
     * Required scopes: `sessions:write`.
     */
    public suspend fun createSessionAnnotation(sessionId: String, body: CreateSessionAnnotationRequest? = null, options: RequestOptions = RequestOptions()): CreateSessionAnnotationResponse {
        return client.request<CreateSessionAnnotationResponse>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/annotations",
                body = body?.let { Body.Json(uarpJson.encodeToString(it)) },
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Create a branch point in session
     *
     * `POST /api/v1/sessions/{sessionId}/branch`
     *
     * Required scopes: `sessions:write`.
     */
    public suspend fun createSessionBranch(sessionId: String, body: JsonObject? = null, options: RequestOptions = RequestOptions()): JsonObject {
        return client.request<JsonObject>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/branch",
                body = body?.let { Body.Json(uarpJson.encodeToString(it)) },
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Create session share link
     *
     * `POST /api/v1/sessions/{sessionId}/share`
     *
     * Required scopes: `sessions:write`.
     */
    public suspend fun createSessionShare(sessionId: String, body: CreateSessionShareRequest? = null, options: RequestOptions = RequestOptions()): CreateSessionShareResponse {
        return client.request<CreateSessionShareResponse>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/share",
                body = body?.let { Body.Json(uarpJson.encodeToString(it)) },
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Create a todo in session
     *
     * `POST /api/v1/sessions/{sessionId}/todos`
     *
     * Required scopes: `sessions:write`.
     */
    public suspend fun createSessionTodo(sessionId: String, body: CreateSessionTodoRequest, options: RequestOptions = RequestOptions()): JsonObject {
        return client.request<JsonObject>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/todos",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Create a standalone task
     *
     * Addresses one agent, several agents (fan-out, one session each, shared parent_task_id), or a
     * squad. The server bootstraps the session(s). `due_at` omitted fires immediately; a timestamp
     * schedules it; explicit `null` files it in the backlog with no schedule at all.
     *
     * `POST /api/v1/todos`
     */
    public suspend fun createTask(body: CreateTaskRequest, options: RequestOptions = RequestOptions()): JsonObject {
        return client.request<JsonObject>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/todos",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Delete annotation
     *
     * `DELETE /api/v1/sessions/{sessionId}/annotations/{annotationId}`
     *
     * Required scopes: `sessions:write`.
     */
    public suspend fun deleteSessionAnnotation(sessionId: String, annotationId: String, options: RequestOptions = RequestOptions()) {
        client.requestUnit(
            RequestSpec(
                method = "DELETE",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/annotations/${encodePathSegment(annotationId)}",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Delete a single todo
     *
     * Deletes ONE todo and its background schedule row. The session and its other todos are
     * untouched.
     *
     * `DELETE /api/v1/sessions/{sessionId}/todos/{todoId}`
     *
     * Required scopes: `sessions:write`.
     */
    public suspend fun deleteSessionTodo(sessionId: String, todoId: String, options: RequestOptions = RequestOptions()): DeleteSessionTodoResponse {
        return client.request<DeleteSessionTodoResponse>(
            RequestSpec(
                method = "DELETE",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/todos/${encodePathSegment(todoId)}",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Get a session
     *
     * `GET /api/v1/sessions/{sessionId}`
     *
     * Required scopes: `sessions:read`.
     */
    public suspend fun `get`(sessionId: String, options: RequestOptions = RequestOptions()): JsonElement {
        return client.request<JsonElement>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}",
                options = options,
            )
        )
    }

    /**
     * Get audit log scoped to session
     *
     * `GET /api/v1/sessions/{sessionId}/audit-log`
     *
     * Required scopes: `sessions:read`.
     */
    public suspend fun getSessionAuditLog(sessionId: String, options: RequestOptions = RequestOptions()): JsonObject {
        return client.request<JsonObject>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/audit-log",
                options = options,
            )
        )
    }

    /**
     * Get feedback for a run in session
     *
     * `GET /api/v1/sessions/{sessionId}/runs/{runId}/feedback`
     *
     * Required scopes: `sessions:read`.
     */
    public suspend fun getSessionRunFeedback(sessionId: String, runId: String, messageId: String? = null, options: RequestOptions = RequestOptions()): JsonObject {
        val query = buildList {
            if (messageId != null) add("message_id" to messageId)
        }
        return client.request<JsonObject>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/runs/${encodePathSegment(runId)}/feedback",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Get session share link status
     *
     * `GET /api/v1/sessions/{sessionId}/share`
     *
     * Required scopes: `sessions:read`.
     */
    public suspend fun getSessionShare(sessionId: String, options: RequestOptions = RequestOptions()): GetSessionShareResponse {
        return client.request<GetSessionShareResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/share",
                options = options,
            )
        )
    }

    /**
     * List sessions
     *
     * `GET /api/v1/sessions`
     *
     * Required scopes: `sessions:read`.
     */
    public suspend fun list(agentId: String? = null, limit: Long? = null, cursor: String? = null, options: RequestOptions = RequestOptions()): ListSessionsResponse {
        val query = buildList {
            if (agentId != null) add("agent_id" to agentId)
            if (limit != null) add("limit" to limit.toString())
            if (cursor != null) add("cursor" to cursor)
        }
        return client.request<ListSessionsResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/sessions",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Stream every item returned by `listSessions`, following the `cursor` cursor until the server
     * reports no further pages.
     */
    public fun listAll(agentId: String? = null, limit: Long? = null, cursor: String? = null, options: RequestOptions = RequestOptions()): Flow<ListSessionsResponseItem> = autoPaginate(
        fetch = { pageCursor -> list(agentId = agentId, limit = limit, cursor = pageCursor, options = options) },
        items = { it.items },
        cursor = { it.cursor },
        hasMore = { it.hasMore },
    )

    /**
     * List session annotations
     *
     * `GET /api/v1/sessions/{sessionId}/annotations`
     *
     * Required scopes: `sessions:read`.
     */
    public suspend fun listSessionAnnotations(sessionId: String, options: RequestOptions = RequestOptions()): ListSessionAnnotationsResponse {
        return client.request<ListSessionAnnotationsResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/annotations",
                options = options,
            )
        )
    }

    /**
     * List artifacts across all runs in session
     *
     * `GET /api/v1/sessions/{sessionId}/artifacts`
     *
     * Required scopes: `sessions:read`.
     */
    public suspend fun listSessionArtifacts(sessionId: String, options: RequestOptions = RequestOptions()): ListSessionArtifactsResponse {
        return client.request<ListSessionArtifactsResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/artifacts",
                options = options,
            )
        )
    }

    /**
     * List session branches
     *
     * `GET /api/v1/sessions/{sessionId}/branches`
     *
     * Required scopes: `sessions:read`.
     */
    public suspend fun listSessionBranches(sessionId: String, options: RequestOptions = RequestOptions()): ListSessionBranchesResponse {
        return client.request<ListSessionBranchesResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/branches",
                options = options,
            )
        )
    }

    /**
     * List session todos
     *
     * `GET /api/v1/sessions/{sessionId}/todos`
     *
     * Required scopes: `sessions:read`.
     */
    public suspend fun listSessionTodos(sessionId: String, from: String? = null, to: String? = null, options: RequestOptions = RequestOptions()): ListSessionTodosResponse {
        val query = buildList {
            if (from != null) add("from" to from)
            if (to != null) add("to" to to)
        }
        return client.request<ListSessionTodosResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/todos",
                query = query,
                options = options,
            )
        )
    }

    /**
     * List all todos across sessions
     *
     * `GET /api/v1/todos`
     */
    public suspend fun listTodos(options: RequestOptions = RequestOptions()): ListTodosResponse {
        return client.request<ListTodosResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/todos",
                options = options,
            )
        )
    }

    /**
     * Resolve shared session (no auth)
     *
     * `GET /api/v1/shared/{shareId}`
     */
    public suspend fun resolveSharedSession(shareId: String, options: RequestOptions = RequestOptions()): ResolveSharedSessionResponse {
        return client.request<ResolveSharedSessionResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/shared/${encodePathSegment(shareId)}",
                options = options,
            )
        )
    }

    /**
     * Revoke session share link
     *
     * `DELETE /api/v1/sessions/{sessionId}/share`
     *
     * Required scopes: `sessions:write`.
     */
    public suspend fun revokeSessionShare(sessionId: String, options: RequestOptions = RequestOptions()) {
        client.requestUnit(
            RequestSpec(
                method = "DELETE",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/share",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Run a pending todo immediately
     *
     * Dispatches the todo's run now instead of waiting for its due time. Only `pending` /
     * `pending_confirmation` todos can be run; anything else answers 409. A recurring todo keeps
     * its cron (next occurrence recomputed from now); a one-shot todo's schedule entry is dropped
     * so it cannot fire twice.
     *
     * `POST /api/v1/sessions/{sessionId}/todos/{todoId}/run`
     *
     * Required scopes: `sessions:write`.
     */
    public suspend fun runSessionTodoNow(sessionId: String, todoId: String, options: RequestOptions = RequestOptions()): JsonObject {
        return client.request<JsonObject>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/todos/${encodePathSegment(todoId)}/run",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Send message
     *
     * Submits a user message to the session and schedules a run on the bound agent. Returns `202
     * Accepted` with the new run id; subscribe to `/api/v1/runs/{runId}/events` (SSE) for
     * streaming output.
     *
     * `POST /api/v1/sessions/{sessionId}/messages`
     *
     * Required scopes: `sessions:write`.
     */
    public suspend fun sendSessionMessage(sessionId: String, body: SendSessionMessageRequest, options: RequestOptions = RequestOptions()): SendSessionMessageResponse {
        return client.request<SendSessionMessageResponse>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/messages",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Save feedback/reaction for a session run
     *
     * `PUT /api/v1/sessions/{sessionId}/runs/{runId}/feedback`
     *
     * Required scopes: `sessions:write`.
     */
    public suspend fun setSessionRunFeedback(sessionId: String, runId: String, body: JsonObject, options: RequestOptions = RequestOptions()): JsonObject {
        return client.request<JsonObject>(
            RequestSpec(
                method = "PUT",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/runs/${encodePathSegment(runId)}/feedback",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Stream session events (SSE)
     *
     * `GET /api/v1/sessions/{sessionId}/events`
     *
     * Required scopes: `events:read`.
     *
     * Returns a cold flow of server-sent events.
     */
    public fun streamSessionEvents(sessionId: String, token: String? = null, lastEventId: String? = null, options: RequestOptions = RequestOptions()): Flow<ServerEvent> {
        val query = buildList {
            if (token != null) add("token" to token)
        }
        val headers = buildList {
            if (lastEventId != null) add("Last-Event-ID" to lastEventId)
        }
        return client.stream(
            RequestSpec(
                method = "GET",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/events",
                query = query,
                headers = headers,
                options = options,
            )
        )
    }

    /**
     * Update session metadata
     *
     * `PUT /api/v1/sessions/{sessionId}`
     *
     * Required scopes: `sessions:write`.
     */
    public suspend fun update(sessionId: String, body: UpdateSessionRequest, options: RequestOptions = RequestOptions()): JsonObject {
        return client.request<JsonObject>(
            RequestSpec(
                method = "PUT",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Update annotation (e.g. resolve)
     *
     * `PATCH /api/v1/sessions/{sessionId}/annotations/{annotationId}`
     *
     * Required scopes: `sessions:write`.
     */
    public suspend fun updateSessionAnnotation(sessionId: String, annotationId: String, body: UpdateSessionAnnotationRequest? = null, options: RequestOptions = RequestOptions()): JsonElement {
        return client.request<JsonElement>(
            RequestSpec(
                method = "PATCH",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/annotations/${encodePathSegment(annotationId)}",
                body = body?.let { Body.Json(uarpJson.encodeToString(it)) },
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Update a todo
     *
     * `PATCH /api/v1/sessions/{sessionId}/todos/{todoId}`
     *
     * Required scopes: `sessions:write`.
     */
    public suspend fun updateSessionTodo(sessionId: String, todoId: String, body: JsonObject, options: RequestOptions = RequestOptions()): JsonObject {
        return client.request<JsonObject>(
            RequestSpec(
                method = "PATCH",
                path = "/api/v1/sessions/${encodePathSegment(sessionId)}/todos/${encodePathSegment(todoId)}",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }
}

/**
 * Conversation sessions
 */
public val UarpClient.sessions: SessionsApi get() = SessionsApi(this)
