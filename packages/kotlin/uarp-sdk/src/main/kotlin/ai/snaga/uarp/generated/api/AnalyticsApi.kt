// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

@file:Suppress("unused", "UNUSED_IMPORT", "RedundantVisibilityModifier")

package ai.snaga.uarp.api

import ai.snaga.uarp.Body
import ai.snaga.uarp.FilePart
import ai.snaga.uarp.Part
import ai.snaga.uarp.RequestOptions
import ai.snaga.uarp.RequestSpec
import ai.snaga.uarp.ServerEvent
import ai.snaga.uarp.UarpClient
import ai.snaga.uarp.autoPaginate
import ai.snaga.uarp.encodePathSegment
import ai.snaga.uarp.uarpJson
import ai.snaga.uarp.models.*
import kotlinx.serialization.encodeToString
import kotlinx.serialization.json.JsonElement
import kotlinx.serialization.json.JsonObject

/**
 * Tenant-scoped usage analytics
 */
public class AnalyticsApi internal constructor(private val client: UarpClient) {
    /**
     * Public chat funnel
     *
     * Visits, engagement and messages on the tenant's public chat surfaces, with
     * country/device/browser/OS/referrer/UTM breakdowns and a per-agent split.
     *
     * The conversion figures are RATIOS (0.25 = a quarter), unlike the admin overview's `*_pct`
     * fields, which are percentages.
     *
     * `GET /api/v1/analytics/public-chat`
     *
     * Required scopes: `analytics:read`.
     */
    public suspend fun getPublicChatAnalytics(days: Long? = null, options: RequestOptions = RequestOptions()): PublicChatAnalytics {
        val query = buildList {
            if (days != null) add("days" to days.toString())
        }
        return client.request<PublicChatAnalytics>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/analytics/public-chat",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Runs waiting on a human
     *
     * Approvals, input requests, paused and failed runs, each with a one-line summary of what is
     * being asked — tool names, the question, or the error.
     *
     * `counts` is computed over the whole scan while `items` honours `limit`, so a truncated list
     * still reports the true backlog.
     *
     * `GET /api/v1/analytics/inbox`
     *
     * Required scopes: `analytics:read`.
     */
    public suspend fun getTenantInbox(limit: Long? = null, options: RequestOptions = RequestOptions()): TenantInbox {
        val query = buildList {
            if (limit != null) add("limit" to limit.toString())
        }
        return client.request<TenantInbox>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/analytics/inbox",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Mission Control overview
     *
     * One aggregate for the dashboard: fleet, run status buckets, recent runs, pending approvals,
     * quota, worker health and schedules at risk.
     *
     * The run scan is bounded, so `runs.scanned` is what the numbers actually describe. A tenant
     * busier than the cap sees a window, not its whole history.
     *
     * `GET /api/v1/analytics/overview`
     *
     * Required scopes: `analytics:read`.
     */
    public suspend fun getTenantOverview(days: Long? = null, options: RequestOptions = RequestOptions()): TenantOverview {
        val query = buildList {
            if (days != null) add("days" to days.toString())
        }
        return client.request<TenantOverview>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/analytics/overview",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Tenant-scoped agent analytics
     *
     * `GET /api/v1/analytics/agents`
     *
     * Required scopes: `analytics:read`.
     */
    public suspend fun tenantAnalyticsAgents(days: Long? = null, options: RequestOptions = RequestOptions()): AgentAnalyticsSummary {
        val query = buildList {
            if (days != null) add("days" to days.toString())
        }
        return client.request<AgentAnalyticsSummary>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/analytics/agents",
                query = query,
                options = options,
            )
        )
    }
}

/**
 * Tenant-scoped usage analytics
 */
public val UarpClient.analytics: AnalyticsApi get() = AnalyticsApi(this)
