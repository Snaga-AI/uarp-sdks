// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

@file:Suppress("unused", "UNUSED_IMPORT", "RedundantVisibilityModifier")

package ai.snaga.uarp.api

import ai.snaga.uarp.Body
import ai.snaga.uarp.FilePart
import ai.snaga.uarp.Part
import ai.snaga.uarp.RequestOptions
import ai.snaga.uarp.RequestSpec
import ai.snaga.uarp.ServerEvent
import ai.snaga.uarp.UarpClient
import ai.snaga.uarp.autoPaginate
import ai.snaga.uarp.encodePathSegment
import ai.snaga.uarp.uarpJson
import ai.snaga.uarp.models.*
import kotlinx.serialization.encodeToString
import kotlinx.serialization.json.JsonElement
import kotlinx.serialization.json.JsonObject

/**
 * Workspace management, file storage, and sharing
 */
public class WorkspacesApi internal constructor(private val client: UarpClient) {
    /**
     * Assign agent/team/company to workspace
     *
     * `POST /api/v1/workspaces/{workspaceId}/assign`
     *
     * Required scopes: `files:write`.
     */
    public suspend fun assignWorkspace(workspaceId: String, body: AssignWorkspaceRequest, options: RequestOptions = RequestOptions()): Workspace {
        return client.request<Workspace>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}/assign",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Copy a file within the workspace
     *
     * Stores the bytes again under the new path (workspace-store.ts copyFile → storeFile): a NEW
     * `file_id` and its own record come back, 201; the source is untouched. Body: `source_path`,
     * `dest_path` (the move route spells them `from_path`/`to_path`).
     *
     * `POST /api/v1/workspaces/{workspaceId}/files/copy`
     *
     * Required scopes: `files:write`.
     */
    public suspend fun copyWorkspaceFile(workspaceId: String, body: CopyWorkspaceFileRequest, options: RequestOptions = RequestOptions()): WorkspaceFile {
        return client.request<WorkspaceFile>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}/files/copy",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Create a workspace
     *
     * `POST /api/v1/workspaces`
     *
     * Required scopes: `files:write`.
     */
    public suspend fun create(body: CreateWorkspaceRequest, options: RequestOptions = RequestOptions()): Workspace {
        return client.request<Workspace>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/workspaces",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Delete workspace and all files
     *
     * `DELETE /api/v1/workspaces/{workspaceId}`
     *
     * Required scopes: `files:write`.
     */
    public suspend fun delete(workspaceId: String, options: RequestOptions = RequestOptions()) {
        client.requestUnit(
            RequestSpec(
                method = "DELETE",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Delete a file
     *
     * `DELETE /api/v1/workspaces/{workspaceId}/files`
     *
     * Required scopes: `files:write`.
     */
    public suspend fun deleteWorkspaceFile(workspaceId: String, path: String, trash: DeleteWorkspaceFileTrash? = null, options: RequestOptions = RequestOptions()): DeleteWorkspaceFileResponse {
        val query = buildList {
            add("path" to path)
            if (trash != null) add("trash" to trash.value)
        }
        return client.request<DeleteWorkspaceFileResponse>(
            RequestSpec(
                method = "DELETE",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}/files",
                query = query,
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Download file content
     *
     * `GET /api/v1/workspaces/{workspaceId}/files/content`
     *
     * Required scopes: `files:read`.
     */
    public suspend fun downloadWorkspaceFile(workspaceId: String, path: String, options: RequestOptions = RequestOptions()): ByteArray {
        val query = buildList {
            add("path" to path)
        }
        return client.requestBytes(
            RequestSpec(
                method = "GET",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}/files/content",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Permanently empty workspace trash
     *
     * `DELETE /api/v1/workspaces/{workspaceId}/trash`
     *
     * Required scopes: `files:write`.
     */
    public suspend fun emptyWorkspaceTrash(workspaceId: String, options: RequestOptions = RequestOptions()): EmptyWorkspaceTrashResponse {
        return client.request<EmptyWorkspaceTrashResponse>(
            RequestSpec(
                method = "DELETE",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}/trash",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Get workspace metadata
     *
     * `GET /api/v1/workspaces/{workspaceId}`
     *
     * Required scopes: `files:read`.
     */
    public suspend fun `get`(workspaceId: String, options: RequestOptions = RequestOptions()): Workspace {
        return client.request<Workspace>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}",
                options = options,
            )
        )
    }

    /**
     * Bytes of a prior version
     *
     * `version` is a `file_id` from `listWorkspaceFileHistory` and must belong to `path`'s history
     * — any other id is 404. Served with the version's own `Content-Type`, its `Content-Length`,
     * and `Cache-Control: private, max-age=3600`.
     *
     * `GET /api/v1/workspaces/{workspaceId}/files/history/content`
     *
     * Required scopes: `files:read`.
     */
    public suspend fun getWorkspaceFileHistoryContent(workspaceId: String, path: String, version: String, options: RequestOptions = RequestOptions()): ByteArray {
        val query = buildList {
            add("path" to path)
            add("version" to version)
        }
        return client.requestBytes(
            RequestSpec(
                method = "GET",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}/files/history/content",
                query = query,
                options = options,
            )
        )
    }

    /**
     * WebSocket terminal session for workspace
     *
     * `GET /api/v1/workspaces/{workspaceId}/terminal`
     *
     * Required scopes: `files:read`.
     */
    public suspend fun getWorkspaceTerminal(workspaceId: String, options: RequestOptions = RequestOptions()): JsonElement {
        return client.request<JsonElement>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}/terminal",
                options = options,
            )
        )
    }

    /**
     * List all workspaces
     *
     * `GET /api/v1/workspaces`
     *
     * Required scopes: `files:read`.
     */
    public suspend fun list(options: RequestOptions = RequestOptions()): ListWorkspacesResponse {
        return client.request<ListWorkspacesResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/workspaces",
                options = options,
            )
        )
    }

    /**
     * List agent workspace files (shortcut)
     *
     * `GET /api/v1/agents/{agentId}/workspace/files`
     *
     * Required scopes: `agents:read`.
     */
    public suspend fun listAgentWorkspaceFiles(agentId: String, path: String? = null, options: RequestOptions = RequestOptions()): ListAgentWorkspaceFilesResponse {
        val query = buildList {
            if (path != null) add("path" to path)
        }
        return client.request<ListAgentWorkspaceFilesResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/agents/${encodePathSegment(agentId)}/workspace/files",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Prior versions of a file
     *
     * Every earlier version kept for `path`, newest first; the current content is not in the list.
     * `versions` is empty for a file that has never been overwritten (measured 2026-09-10). Read a
     * version's bytes with `getWorkspaceFileHistoryContent`.
     *
     * `GET /api/v1/workspaces/{workspaceId}/files/history`
     *
     * Required scopes: `files:read`.
     */
    public suspend fun listWorkspaceFileHistory(workspaceId: String, path: String, options: RequestOptions = RequestOptions()): ListWorkspaceFileHistoryResponse {
        val query = buildList {
            add("path" to path)
        }
        return client.request<ListWorkspaceFileHistoryResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}/files/history",
                query = query,
                options = options,
            )
        )
    }

    /**
     * List files in workspace directory
     *
     * `GET /api/v1/workspaces/{workspaceId}/files`
     *
     * Required scopes: `files:read`.
     */
    public suspend fun listWorkspaceFiles(workspaceId: String, path: String? = null, recursive: Boolean? = null, options: RequestOptions = RequestOptions()): ListWorkspaceFilesResponse {
        val query = buildList {
            if (path != null) add("path" to path)
            if (recursive != null) add("recursive" to recursive.toString())
        }
        return client.request<ListWorkspaceFilesResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}/files",
                query = query,
                options = options,
            )
        )
    }

    /**
     * List trashed files in workspace
     *
     * `GET /api/v1/workspaces/{workspaceId}/trash`
     *
     * Required scopes: `files:read`.
     */
    public suspend fun listWorkspaceTrash(workspaceId: String, options: RequestOptions = RequestOptions()): ListWorkspaceTrashResponse {
        return client.request<ListWorkspaceTrashResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}/trash",
                options = options,
            )
        )
    }

    /**
     * Move/rename a file
     *
     * Renames the record in place (workspace-store.ts moveFile): the SAME `file_id` comes back
     * with the new `path` — a client holding the id keeps a live key. The content and its `etag`
     * do not change. Answers 200. Body: `from_path`, `to_path` (the copy route spells them
     * `source_path`/`dest_path`; both stay as they are).
     *
     * `POST /api/v1/workspaces/{workspaceId}/files/move`
     *
     * Required scopes: `files:write`.
     */
    public suspend fun moveWorkspaceFile(workspaceId: String, body: MoveWorkspaceFileRequest, options: RequestOptions = RequestOptions()): WorkspaceFile {
        return client.request<WorkspaceFile>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}/files/move",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Mint a public share link for an HTML snapshot
     *
     * Stores a self-contained HTML page (assets already inlined by the client, ≤3 MB) under an
     * opaque token for seven days; `GET /public/share/{token}` serves it back as JSON data for the
     * sandboxed viewer, never as executable HTML. The workspace id is not consulted beyond scope.
     *
     * `POST /api/v1/workspaces/{workspaceId}/publish`
     *
     * Required scopes: `files:write`.
     */
    public suspend fun publishWorkspaceSnapshot(workspaceId: String, body: PublishWorkspaceSnapshotRequest, options: RequestOptions = RequestOptions()): PublishWorkspaceSnapshotResponse {
        return client.request<PublishWorkspaceSnapshotResponse>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}/publish",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Restore a trashed file to its original path
     *
     * `POST /api/v1/workspaces/{workspaceId}/trash/restore`
     *
     * Required scopes: `files:write`.
     */
    public suspend fun restoreWorkspaceTrash(workspaceId: String, body: RestoreWorkspaceTrashRequest, options: RequestOptions = RequestOptions()): RestoreWorkspaceTrashResponse {
        return client.request<RestoreWorkspaceTrashResponse>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}/trash/restore",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Revoke workspace sharing
     *
     * Removes the agent from the workspace's `shared_with` list and returns the updated workspace;
     * the workspace and its files are untouched. `POST` the share again to restore access.
     *
     * `DELETE /api/v1/workspaces/{workspaceId}/share/{agentId}`
     *
     * Required scopes: `files:write`.
     */
    public suspend fun revokeWorkspaceShare(workspaceId: String, agentId: String, options: RequestOptions = RequestOptions()): Workspace {
        return client.request<Workspace>(
            RequestSpec(
                method = "DELETE",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}/share/${encodePathSegment(agentId)}",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Execute shell command in workspace
     *
     * `POST /api/v1/workspaces/{workspaceId}/run-command`
     *
     * Required scopes: `files:write`.
     */
    public suspend fun runWorkspaceCommand(workspaceId: String, body: RunWorkspaceCommandRequest, options: RequestOptions = RequestOptions()): RunWorkspaceCommandResponse {
        return client.request<RunWorkspaceCommandResponse>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}/run-command",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Search files in workspace
     *
     * `GET /api/v1/workspaces/{workspaceId}/search`
     *
     * Required scopes: `files:read`.
     */
    public suspend fun searchWorkspaceFiles(workspaceId: String, q: String, glob: String? = null, regex: SearchWorkspaceFilesRegex? = null, options: RequestOptions = RequestOptions()): SearchWorkspaceFilesResponse {
        val query = buildList {
            add("q" to q)
            if (glob != null) add("glob" to glob)
            if (regex != null) add("regex" to regex.value)
        }
        return client.request<SearchWorkspaceFilesResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}/search",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Share workspace with an agent
     *
     * `POST /api/v1/workspaces/{workspaceId}/share`
     *
     * Required scopes: `files:write`.
     */
    public suspend fun share(workspaceId: String, body: ShareWorkspaceRequest, options: RequestOptions = RequestOptions()): Workspace {
        return client.request<Workspace>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}/share",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Unassign agent/team/company from workspace
     *
     * The body names WHICH assignment to remove — `agent_id`, `team_id` or `company_id`. It was
     * undeclared until 2026-08-31, and the omission did not stay in the document: the SDK
     * generator faithfully wrote `body: false`, a proxy honoured that flag and dropped the body,
     * and the server still read it. The result was a DELETE that always succeeded and changed
     * nothing — 200, no error, unchanged state, under a button labelled Unassign.
     *
     * Three layers each behaved correctly on what they were given; the mistake in the first passed
     * through both and came out the other side as "it works". A spec cannot be checked against a
     * spec.
     *
     * `DELETE /api/v1/workspaces/{workspaceId}/assign`
     *
     * Required scopes: `files:write`.
     */
    public suspend fun unassignWorkspace(workspaceId: String, body: UnassignWorkspaceRequest, options: RequestOptions = RequestOptions()): Workspace {
        return client.request<Workspace>(
            RequestSpec(
                method = "DELETE",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}/assign",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Update workspace name
     *
     * `PATCH /api/v1/workspaces/{workspaceId}`
     *
     * Required scopes: `files:write`.
     */
    public suspend fun update(workspaceId: String, body: UpdateWorkspaceRequest, options: RequestOptions = RequestOptions()): Workspace {
        return client.request<Workspace>(
            RequestSpec(
                method = "PATCH",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Upload or update a file
     *
     * Writes the file at `?path=` — a NEW record with a NEW `file_id` every time, also when the
     * path already exists (workspace-store.ts storeFile: the previous record moves into the path's
     * version history and its bytes stay readable through it; a client that keeps the old
     * `file_id` after re-uploading holds a key to the previous version). `etag` is the lowercase
     * hex sha256 of the content. Body: multipart/form-data with a `file` part, or the raw bytes as
     * application/octet-stream. Conditional forms via `If-Match` / `If-None-Match` — see the
     * parameters.
     *
     * `PUT /api/v1/workspaces/{workspaceId}/files`
     *
     * Required scopes: `files:write`.
     */
    public suspend fun uploadWorkspaceFile(workspaceId: String, body: UploadWorkspaceFileRequest, path: String, ifMatch: String? = null, ifNoneMatch: UploadWorkspaceFileIfNoneMatch? = null, options: RequestOptions = RequestOptions()): WorkspaceFile {
        val query = buildList {
            add("path" to path)
        }
        val headers = buildList {
            if (ifMatch != null) add("If-Match" to ifMatch)
            if (ifNoneMatch != null) add("If-None-Match" to ifNoneMatch.value)
        }
        val parts = buildList {
            add(Part.File("file", body.`file`))
        }
        return client.request<WorkspaceFile>(
            RequestSpec(
                method = "PUT",
                path = "/api/v1/workspaces/${encodePathSegment(workspaceId)}/files",
                query = query,
                headers = headers,
                body = Body.Multipart(parts),
                idempotent = true,
                options = options,
            )
        )
    }
}

/**
 * Workspace management, file storage, and sharing
 */
public val UarpClient.workspaces: WorkspacesApi get() = WorkspacesApi(this)
