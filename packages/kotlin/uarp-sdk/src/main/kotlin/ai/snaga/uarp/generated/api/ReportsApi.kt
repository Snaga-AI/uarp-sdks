// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

@file:Suppress("unused", "UNUSED_IMPORT", "RedundantVisibilityModifier")

package ai.snaga.uarp.api

import ai.snaga.uarp.Body
import ai.snaga.uarp.FilePart
import ai.snaga.uarp.Part
import ai.snaga.uarp.RequestOptions
import ai.snaga.uarp.RequestSpec
import ai.snaga.uarp.ServerEvent
import ai.snaga.uarp.UarpClient
import ai.snaga.uarp.autoPaginate
import ai.snaga.uarp.encodePathSegment
import ai.snaga.uarp.uarpJson
import ai.snaga.uarp.models.*
import kotlinx.serialization.encodeToString
import kotlinx.serialization.json.JsonElement
import kotlinx.serialization.json.JsonObject
import kotlinx.coroutines.flow.Flow

/**
 * Content reports — abuse/moderation reporting for authenticated users and anonymous
 * public-chat visitors, plus the operator inbox
 */
public class ReportsApi internal constructor(private val client: UarpClient) {
    /**
     * File a content report
     *
     * Report a message, session or agent. Available to any authenticated principal — deliberately
     * not role- or scope-gated, since reporting abuse must never be blocked by RBAC. Rate limited
     * to 10/minute per caller. `self_harm` is still accepted with 202 but raises the operator
     * notification to critical priority.
     *
     * `POST /api/v1/reports`
     */
    public suspend fun createContentReport(body: ContentReportInput, options: RequestOptions = RequestOptions()): ContentReportAccepted {
        return client.request<ContentReportAccepted>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/reports",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * List content reports platform-wide (super-admin)
     *
     * Every report across every tenant, newest first. A tenant whose agent is the problem must not
     * be the only party holding the evidence.
     *
     * `GET /api/v1/admin/reports`
     *
     * Required scopes: `admin`.
     */
    public suspend fun listAllContentReports(limit: Long? = null, cursor: String? = null, options: RequestOptions = RequestOptions()): ListAllContentReportsResponse {
        val query = buildList {
            if (limit != null) add("limit" to limit.toString())
            if (cursor != null) add("cursor" to cursor)
        }
        return client.request<ListAllContentReportsResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/admin/reports",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Stream every item returned by `listAllContentReports`, following the `cursor` cursor until
     * the server reports no further pages.
     */
    public fun listAllContentReportsAll(limit: Long? = null, cursor: String? = null, options: RequestOptions = RequestOptions()): Flow<ContentReport> = autoPaginate(
        fetch = { pageCursor -> listAllContentReports(limit = limit, cursor = pageCursor, options = options) },
        items = { it.items },
        cursor = { it.cursor },
        hasMore = { it.hasMore },
    )

    /**
     * List content reports for this tenant (admin+)
     *
     * Moderation queue for the tenant that owns the reported content, newest first. Requires the
     * admin role — reports carry reporter free text and there is no moderation duty below admin.
     *
     * `GET /api/v1/reports`
     */
    public suspend fun listContentReports(limit: Long? = null, cursor: String? = null, options: RequestOptions = RequestOptions()): ListContentReportsResponse {
        val query = buildList {
            if (limit != null) add("limit" to limit.toString())
            if (cursor != null) add("cursor" to cursor)
        }
        return client.request<ListContentReportsResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/reports",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Stream every item returned by `listContentReports`, following the `cursor` cursor until the
     * server reports no further pages.
     */
    public fun listContentReportsAll(limit: Long? = null, cursor: String? = null, options: RequestOptions = RequestOptions()): Flow<ContentReport> = autoPaginate(
        fetch = { pageCursor -> listContentReports(limit = limit, cursor = pageCursor, options = options) },
        items = { it.items },
        cursor = { it.cursor },
        hasMore = { it.hasMore },
    )
}

/**
 * Content reports — abuse/moderation reporting for authenticated users and anonymous
 * public-chat visitors, plus the operator inbox
 */
public val UarpClient.reports: ReportsApi get() = ReportsApi(this)
