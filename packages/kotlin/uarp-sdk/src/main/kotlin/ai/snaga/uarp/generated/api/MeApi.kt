// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

@file:Suppress("unused", "UNUSED_IMPORT", "RedundantVisibilityModifier")

package ai.snaga.uarp.api

import ai.snaga.uarp.Body
import ai.snaga.uarp.FilePart
import ai.snaga.uarp.Part
import ai.snaga.uarp.RequestOptions
import ai.snaga.uarp.RequestSpec
import ai.snaga.uarp.ServerEvent
import ai.snaga.uarp.UarpClient
import ai.snaga.uarp.autoPaginate
import ai.snaga.uarp.encodePathSegment
import ai.snaga.uarp.uarpJson
import ai.snaga.uarp.models.*
import kotlinx.serialization.encodeToString
import kotlinx.serialization.json.JsonElement
import kotlinx.serialization.json.JsonObject

/**
 * The calling identity: profile, tenants, head agent
 */
public class MeApi internal constructor(private val client: UarpClient) {
    /**
     * Delete your own account
     *
     * Self-serve account deletion — the App Store 5.1.1(v) requirement, and the only path to it in
     * the mobile clients. Any authenticated member deletes their OWN account: the user record and
     * the email-membership index in every tenant the address belongs to, their api keys (the
     * per-tenant row AND the global hash, so live sessions die at once), and data tagged with them
     * as data subject, erased through the same sweep as `/data-subject/erasure`.
     *
     * Every refusal is checked across ALL memberships BEFORE anything is deleted, so a half-delete
     * cannot happen.
     *
     * WRITE SEMANTICS: replaces nothing — it removes. A second call answers 404: after a
     * successful delete there is no record to delete, which is the honest answer rather than a
     * silent success.
     *
     * `DELETE /api/v1/me`
     */
    public suspend fun delete(options: RequestOptions = RequestOptions()): DeleteMeResponse {
        return client.request<DeleteMeResponse>(
            RequestSpec(
                method = "DELETE",
                path = "/api/v1/me",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Export everything in this account
     *
     * Chats, projects, memories and the account record. `format=zip` (the default) answers a zip
     * attachment; `format=json` answers the same bundle as one document, which is easier to pipe
     * and which a browser will show rather than download.
     *
     * Check `complete`: when a size ceiling is hit the export still succeeds and `omitted` names
     * what was left out. A truncated export that does not say so is the failure this field exists
     * to prevent.
     *
     * `GET /api/v1/me/export`
     */
    public suspend fun exportMyAccount(format: ExportMyAccountFormat? = null, options: RequestOptions = RequestOptions()): AccountExport {
        val query = buildList {
            if (format != null) add("format" to format.value)
        }
        return client.request<AccountExport>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/me/export",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Read personal instructions
     *
     * Requires a credential bound to a person: an API key that carries no user identity is refused
     * with 403, because personal instructions belong to the account holder rather than the key.
     *
     * `GET /api/v1/me/preferences`
     */
    public suspend fun getMyPreferences(options: RequestOptions = RequestOptions()): UserPreferences {
        return client.request<UserPreferences>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/me/preferences",
                options = options,
            )
        )
    }

    /**
     * Set or clear your avatar
     *
     * The one field a person may change about themselves here: `avatar_url`. It must name a file
     * this tenant owns — `/api/v1/files/<file_id>/content`, absolute or relative — whose
     * `mime_type` is `image&#47;*`; `null` clears it. The change is written to the caller's user
     * row in every tenant they belong to (`updated_rows` says how many). Anything else in the body
     * is ignored; a body without `avatar_url` is 400. WRITE SEMANTICS: replaces — `avatar_url` is
     * the whole writable surface and must be present; there is nothing to merge.
     *
     * `PATCH /api/v1/me`
     */
    public suspend fun patch(body: PatchMeRequest, options: RequestOptions = RequestOptions()): PatchMeResponse {
        return client.request<PatchMeResponse>(
            RequestSpec(
                method = "PATCH",
                path = "/api/v1/me",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Switch the active workspace
     *
     * Requires a **user session (JWT)**. An API key is refused with 403, because a key is bound to
     * the tenant it was created in — create a key inside the target tenant instead. This endpoint
     * used to answer `{"switched": true}` for api-key callers and change nothing, which is why the
     * refusal is now explicit and documented.
     *
     * The answer carries the target context; the client re-issues a key through the normal
     * post-login flow.
     *
     * `POST /api/v1/me/tenants/switch`
     */
    public suspend fun switchTenant(body: SwitchTenantRequest, options: RequestOptions = RequestOptions()): SwitchTenantResponse {
        return client.request<SwitchTenantResponse>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/me/tenants/switch",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Write personal instructions
     *
     * Both fields are optional; what is sent is merged onto the stored record. Same person-bound
     * credential rule as the read.
     *
     * `PUT /api/v1/me/preferences`
     */
    public suspend fun updateMyPreferences(body: UpdateMyPreferencesRequest, options: RequestOptions = RequestOptions()): UserPreferences {
        return client.request<UserPreferences>(
            RequestSpec(
                method = "PUT",
                path = "/api/v1/me/preferences",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }
}

/**
 * The calling identity: profile, tenants, head agent
 */
public val UarpClient.me: MeApi get() = MeApi(this)
