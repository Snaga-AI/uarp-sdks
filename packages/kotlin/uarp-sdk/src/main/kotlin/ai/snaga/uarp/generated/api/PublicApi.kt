// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

@file:Suppress("unused", "UNUSED_IMPORT", "RedundantVisibilityModifier")

package ai.snaga.uarp.api

import ai.snaga.uarp.Body
import ai.snaga.uarp.FilePart
import ai.snaga.uarp.Part
import ai.snaga.uarp.RequestOptions
import ai.snaga.uarp.RequestSpec
import ai.snaga.uarp.ServerEvent
import ai.snaga.uarp.UarpClient
import ai.snaga.uarp.autoPaginate
import ai.snaga.uarp.encodePathSegment
import ai.snaga.uarp.uarpJson
import ai.snaga.uarp.models.*
import kotlinx.serialization.encodeToString
import kotlinx.serialization.json.JsonElement
import kotlinx.serialization.json.JsonObject
import kotlinx.coroutines.flow.Flow

/**
 * Unauthenticated public-facing endpoints
 */
public class PublicApi internal constructor(private val client: UarpClient) {
    /**
     * Cancel a run of this chat
     *
     * Cancels a run that belongs to this public session. No body. A run id from outside the
     * session is 404; a run of this session that was not started publicly is 409. Forms measured
     * through the router with a seeded session (public-served-forms_test.ts, 2026-09-10).
     *
     * `POST /api/v1/public/sessions/{sessionId}/runs/{runId}/cancel`
     */
    public suspend fun cancelPublicSessionRun(sessionId: String, runId: String, options: RequestOptions = RequestOptions()): CancelPublicSessionRunResponse {
        return client.request<CancelPublicSessionRunResponse>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/public/sessions/${encodePathSegment(sessionId)}/runs/${encodePathSegment(runId)}/cancel",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Create public session
     *
     * `POST /api/v1/public/sessions`
     */
    public suspend fun createPublicSession(body: CreatePublicSessionRequest, options: RequestOptions = RequestOptions()): CreatePublicSessionResponse {
        return client.request<CreatePublicSessionResponse>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/public/sessions",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Report abusive content from a public chat session
     *
     * Anonymous abuse report filed by a public-chat visitor. Requires no authentication of any
     * kind — the session id in the path is the scope, and it must resolve in the public-session
     * index. Rate limited per session (5/hour) and per IP (10/hour). The report lands in the
     * moderation queue of the tenant that owns the agent and raises an in-product notification
     * there.
     *
     * `POST /api/v1/public/sessions/{sessionId}/reports`
     */
    public suspend fun createPublicSessionReport(sessionId: String, body: ContentReportInput, options: RequestOptions = RequestOptions()): ContentReportAccepted {
        return client.request<ContentReportAccepted>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/public/sessions/${encodePathSegment(sessionId)}/reports",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Has THIS browser already signed up?
     *
     * Deliberately not an address oracle. The answer is `registered: true` only when the caller
     * carries the sign-up cookie this browser was given AND it matches the address asked about;
     * any other address, or the same address from a browser that did not sign up, answers
     * `registered: false`. So the route cannot be used to test whether an address is on the
     * roster.
     *
     * `GET /api/v1/public/testing/android/status`
     */
    public suspend fun getAndroidTestingStatus(email: String, options: RequestOptions = RequestOptions()): GetAndroidTestingStatusResponse {
        val query = buildList {
            add("email" to email)
        }
        return client.request<GetAndroidTestingStatusResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/testing/android/status",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Public landing overrides
     *
     * No authentication. Text overrides and partner logos for the landing page.
     *
     * `GET /api/v1/public/landing/overrides`
     */
    public suspend fun getLandingOverrides(options: RequestOptions = RequestOptions()): LandingOverrides {
        return client.request<LandingOverrides>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/landing/overrides",
                options = options,
            )
        )
    }

    /**
     * Unfurl a cited link into card metadata
     *
     * Open Graph metadata for a URL an agent cited, so the client renders a card instead of a bare
     * link. SSRF-guarded: a private or loopback address is refused with 403 rather than fetched.
     * Every member of `preview` except `url` and `site` may be null.
     *
     * `GET /api/v1/public/link-preview`
     */
    public suspend fun getLinkPreview(url: String, options: RequestOptions = RequestOptions()): GetLinkPreviewResponse {
        val query = buildList {
            add("url" to url)
        }
        return client.request<GetLinkPreviewResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/link-preview",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Proxy the og:image
     *
     * Serves the preview image through this origin so the visitor never connects to the
     * third-party host. Answers the image bytes with the upstream content type, `Cache-Control:
     * public, max-age=86400, immutable`, `nosniff` and a `default-src 'none'` CSP. A target that
     * is not an image, or that the fetch could not complete, is 404 rather than a broken picture;
     * one over the size cap is 413, including when the responder simply had more to send — half an
     * image renders as our bug rather than their oversized file.
     *
     * `GET /api/v1/public/link-preview/image`
     */
    public suspend fun getLinkPreviewImage(url: String, options: RequestOptions = RequestOptions()): ByteArray {
        val query = buildList {
            add("url" to url)
        }
        return client.requestBytes(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/link-preview/image",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Maintenance state
     *
     * No authentication, by design: during maintenance the authenticated surface is exactly what a
     * client cannot reach, so asking “is it me or is it you” must not itself require a session.
     *
     * A read failure answers `enabled: false` — the platform is assumed open unless it is known to
     * be closed.
     *
     * `GET /api/v1/maintenance/status`
     */
    public suspend fun getMaintenanceStatus(options: RequestOptions = RequestOptions()): MaintenanceStatus {
        return client.request<MaintenanceStatus>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/maintenance/status",
                options = options,
            )
        )
    }

    /**
     * Public deployment info
     *
     * No authentication. Contact addresses and the public base URL as the operator configured
     * them, plus enough setup state to render a “being set up” banner.
     *
     * `GET /api/v1/public/platform-info`
     */
    public suspend fun getPlatformInfo(options: RequestOptions = RequestOptions()): PlatformInfo {
        return client.request<PlatformInfo>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/platform-info",
                options = options,
            )
        )
    }

    /**
     * Get public agent card
     *
     * `GET /api/v1/public/agents/{agentId}`
     */
    public suspend fun getPublicAgentCard(agentId: String, options: RequestOptions = RequestOptions()): PublicAgentCard {
        return client.request<PublicAgentCard>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/agents/${encodePathSegment(agentId)}",
                options = options,
            )
        )
    }

    /**
     * One published post, with its body
     *
     * A post that exists but is not published is 404, the same as one that does not exist — a
     * draft must not be discoverable by its status.
     *
     * `GET /api/v1/public/blog/posts/{slug}`
     */
    public suspend fun getPublicBlogPost(slug: String, options: RequestOptions = RequestOptions()): GetPublicBlogPostResponse {
        return client.request<GetPublicBlogPostResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/blog/posts/${encodePathSegment(slug)}",
                options = options,
            )
        )
    }

    /**
     * RSS 2.0 feed of published posts
     *
     * `/api/v1/public/blog/rss.xml` is the same feed under the extension readers expect; both
     * paths answer identically.
     *
     * `GET /api/v1/public/blog/rss`
     */
    public suspend fun getPublicBlogRss(options: RequestOptions = RequestOptions()): String {
        return client.requestText(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/blog/rss",
                options = options,
            )
        )
    }

    /**
     * Get the public featured agent for the landing-page hero
     *
     * `GET /api/v1/public/landing/featured-agent`
     */
    public suspend fun getPublicFeaturedAgent(options: RequestOptions = RequestOptions()): GetPublicFeaturedAgentResponse {
        return client.request<GetPublicFeaturedAgentResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/landing/featured-agent",
                options = options,
            )
        )
    }

    /**
     * Get public file content
     *
     * `GET /api/v1/public/files/{fileId}/content`
     */
    public suspend fun getPublicFileContent(fileId: String, options: RequestOptions = RequestOptions()): ByteArray {
        return client.requestBytes(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/files/${encodePathSegment(fileId)}/content",
                options = options,
            )
        )
    }

    /**
     * Get public session
     *
     * `GET /api/v1/public/sessions/{sessionId}`
     */
    public suspend fun getPublicSession(sessionId: String, options: RequestOptions = RequestOptions()): PublicSessionView {
        return client.request<PublicSessionView>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/sessions/${encodePathSegment(sessionId)}",
                options = options,
            )
        )
    }

    /**
     * Get public state detail
     *
     * `GET /api/v1/public/states/{stateId}`
     */
    public suspend fun getPublicState(stateId: String, options: RequestOptions = RequestOptions()): GetPublicStateResponse {
        return client.request<GetPublicStateResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/states/${encodePathSegment(stateId)}",
                options = options,
            )
        )
    }

    /**
     * Get public tenant profile
     *
     * `GET /api/v1/public/tenants/{slug}`
     */
    public suspend fun getPublicTenantProfile(slug: String, options: RequestOptions = RequestOptions()): PublicTenant {
        return client.request<PublicTenant>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/tenants/${encodePathSegment(slug)}",
                options = options,
            )
        )
    }

    /**
     * Tenant custom CSS stylesheet
     *
     * Serves tenant-configured branding CSS as `text/css`. Cached with ETag; safe for `<link
     * rel=stylesheet>`.
     *
     * `GET /api/v1/public/tenants/{slug}/style.css`
     */
    public suspend fun getPublicTenantStylesheet(slug: String, options: RequestOptions = RequestOptions()): String {
        return client.requestText(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/tenants/${encodePathSegment(slug)}/style.css",
                options = options,
            )
        )
    }

    /**
     * Is sign-up open
     *
     * No authentication; cached for 30 seconds.
     *
     * **Fails open.** If the setting cannot be read the answer is `registration_open: true`,
     * because the worst case of guessing open is a missing notice, while guessing closed would
     * turn a storage blip into a closed front door. A client cannot distinguish the two — this
     * endpoint is the state, not a health check.
     *
     * `GET /api/v1/public/registration-status`
     */
    public suspend fun getRegistrationStatus(options: RequestOptions = RequestOptions()): GetRegistrationStatusResponse {
        return client.request<GetRegistrationStatusResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/registration-status",
                options = options,
            )
        )
    }

    /**
     * List published blog posts
     *
     * Published posts only, newest first, paged. `all_tags` is the distinct tag set across every
     * published post — the whole set, not just this page — so a filter UI can be built from one
     * call. `excerpt` is the body with its leading heading and markdown punctuation stripped, cut
     * to 240 characters. `total` and `total_pages` count posts AFTER `tag` and `q` are applied.
     *
     * `GET /api/v1/public/blog`
     */
    public suspend fun listPublicBlogPosts(tag: String? = null, q: String? = null, page: Long? = null, limit: Long? = null, options: RequestOptions = RequestOptions()): ListPublicBlogPostsResponse {
        val query = buildList {
            if (tag != null) add("tag" to tag)
            if (q != null) add("q" to q)
            if (page != null) add("page" to page.toString())
            if (limit != null) add("limit" to limit.toString())
        }
        return client.request<ListPublicBlogPostsResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/blog",
                query = query,
                options = options,
            )
        )
    }

    /**
     * List the integrations a visitor could connect
     *
     * The connectors the platform offers today: the registry MINUS anything an admin has switched
     * off, which is the same answer `GET /integrations/catalog` gives a signed-in tenant — both
     * call one function, so a page rendered from this cannot advertise what the product refuses.
     *
     * It exists because a hand-kept list drifted: a marketing page counted the connector registry
     * and said twenty, naming three integrations that are not in the catalogue at all, while a
     * tenant was served seven. A number a page keeps by hand is a number that can be wrong; this
     * one cannot.
     *
     * Deliberately thinner than the tenant catalogue — no `config_schema`, because a visitor
     * deciding whether to sign up does not need to know which credential fields a connector wants.
     * Anonymous, and it says nothing about any tenant.
     *
     * `GET /api/v1/public/integrations`
     */
    public suspend fun listPublicIntegrations(options: RequestOptions = RequestOptions()): ListPublicIntegrationsResponse {
        return client.request<ListPublicIntegrationsResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/integrations",
                options = options,
            )
        )
    }

    /**
     * List public plans
     *
     * `GET /api/v1/public/plans`
     */
    public suspend fun listPublicPlans(options: RequestOptions = RequestOptions()): ListPublicPlansResponse {
        return client.request<ListPublicPlansResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/plans",
                options = options,
            )
        )
    }

    /**
     * List public states
     *
     * `GET /api/v1/public/states`
     */
    public suspend fun listPublicStates(options: RequestOptions = RequestOptions()): ListPublicStatesResponse {
        return client.request<ListPublicStatesResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/states",
                options = options,
            )
        )
    }

    /**
     * List public tenants
     *
     * `GET /api/v1/public/tenants`
     */
    public suspend fun listPublicTenants(category: String? = null, sort: String? = null, search: String? = null, limit: Long? = null, cursor: String? = null, options: RequestOptions = RequestOptions()): ListPublicTenantsResponse {
        val query = buildList {
            if (category != null) add("category" to category)
            if (sort != null) add("sort" to sort)
            if (search != null) add("search" to search)
            if (limit != null) add("limit" to limit.toString())
            if (cursor != null) add("cursor" to cursor)
        }
        return client.request<ListPublicTenantsResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/tenants",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Stream every item returned by `listPublicTenants`, following the `cursor` cursor until the
     * server reports no further pages.
     */
    public fun listPublicTenantsAll(category: String? = null, sort: String? = null, search: String? = null, limit: Long? = null, cursor: String? = null, options: RequestOptions = RequestOptions()): Flow<PublicTenant> = autoPaginate(
        fetch = { pageCursor -> listPublicTenants(category = category, sort = sort, search = search, limit = limit, cursor = pageCursor, options = options) },
        items = { it.items ?: emptyList() },
        cursor = { it.cursor },
        hasMore = { it.hasMore },
    )

    /**
     * Domain lookup
     *
     * `GET /api/v1/public/domain-lookup`
     */
    public suspend fun publicDomainLookup(domain: String, options: RequestOptions = RequestOptions()): PublicDomainLookupResponse {
        val query = buildList {
            add("domain" to domain)
        }
        return client.request<PublicDomainLookupResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/domain-lookup",
                query = query,
                options = options,
            )
        )
    }

    /**
     * Anonymous analytics beacon
     *
     * Fire-and-forget event tracker for landing-page funnels. No auth, no PII required;
     * rate-limited by IP.
     *
     * `POST /api/v1/public/track`
     */
    public suspend fun publicTrackEvent(body: PublicTrackEventRequest, options: RequestOptions = RequestOptions()) {
        client.requestUnit(
            RequestSpec(
                method = "POST",
                path = "/api/v1/public/track",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Respond to public HITL
     *
     * `POST /api/v1/public/sessions/{sessionId}/respond`
     */
    public suspend fun respondToPublicHitl(sessionId: String, body: RespondToPublicHitlRequest, options: RequestOptions = RequestOptions()): RespondToPublicHitlResponse {
        return client.request<RespondToPublicHitlResponse>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/public/sessions/${encodePathSegment(sessionId)}/respond",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Send public message
     *
     * `POST /api/v1/public/sessions/{sessionId}/messages`
     */
    public suspend fun sendPublicMessage(sessionId: String, body: SendPublicMessageRequest, options: RequestOptions = RequestOptions()): SendPublicMessageResponse {
        return client.request<SendPublicMessageResponse>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/public/sessions/${encodePathSegment(sessionId)}/messages",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Publish a read-only copy of this chat
     *
     * Snapshots the last 60 user/assistant turns of the session into a share record that
     * `getPublicSharedChat` serves for a limited time, and returns its token. No body. A session
     * with no turns yet is 400 `Nothing to share yet`. Forms measured through the router with a
     * seeded session (public-served-forms_test.ts, 2026-09-10).
     *
     * `POST /api/v1/public/sessions/{sessionId}/share`
     */
    public suspend fun sharePublicSession(sessionId: String, options: RequestOptions = RequestOptions()): SharePublicSessionResponse {
        return client.request<SharePublicSessionResponse>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/public/sessions/${encodePathSegment(sessionId)}/share",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Sign up for Android closed testing
     *
     * Records the address and, when a testing URL is configured, mails the join link. A repeat
     * submit is NOT an error: the same address answers 200 with `already_registered: true` instead
     * of 201, and no second letter goes out.
     *
     * `POST /api/v1/public/testing/android`
     */
    public suspend fun signUpForAndroidTesting(body: SignUpForAndroidTestingRequest, options: RequestOptions = RequestOptions()): AndroidTesterSignupResult {
        return client.request<AndroidTesterSignupResult>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/public/testing/android",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * SSE stream for public session
     *
     * `GET /api/v1/public/sessions/{sessionId}/events`
     *
     * Returns a cold flow of server-sent events.
     */
    public fun streamPublicSessionEvents(sessionId: String, options: RequestOptions = RequestOptions()): Flow<ServerEvent> {
        return client.stream(
            RequestSpec(
                method = "GET",
                path = "/api/v1/public/sessions/${encodePathSegment(sessionId)}/events",
                options = options,
            )
        )
    }

    /**
     * Attach an image to this chat
     *
     * The body is the raw image bytes — not multipart, not JSON — with its media type in
     * `Content-Type` (`image&#47;*` only; anything else is 415). Empty is 400; over 8 MB is 413;
     * more uploads than the session allows is 429; a tenant whose storage quota is full gets 403.
     * The image is stored as one of the agent tenant's files and the returned `file_id` is what
     * `sendPublicMessage` attaches. Forms measured through the router with a seeded session
     * (public-served-forms_test.ts, 2026-09-10).
     *
     * `POST /api/v1/public/sessions/{sessionId}/upload`
     */
    public suspend fun uploadPublicSessionImage(sessionId: String, body: FilePart, options: RequestOptions = RequestOptions()): UploadPublicSessionImageResponse {
        return client.request<UploadPublicSessionImageResponse>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/public/sessions/${encodePathSegment(sessionId)}/upload",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }
}

/**
 * Unauthenticated public-facing endpoints
 */
public val UarpClient.public: PublicApi get() = PublicApi(this)
