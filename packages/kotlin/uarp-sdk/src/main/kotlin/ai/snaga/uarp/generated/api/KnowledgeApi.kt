// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

@file:Suppress("unused", "UNUSED_IMPORT", "RedundantVisibilityModifier")

package ai.snaga.uarp.api

import ai.snaga.uarp.Body
import ai.snaga.uarp.FilePart
import ai.snaga.uarp.Part
import ai.snaga.uarp.RequestOptions
import ai.snaga.uarp.RequestSpec
import ai.snaga.uarp.ServerEvent
import ai.snaga.uarp.UarpClient
import ai.snaga.uarp.autoPaginate
import ai.snaga.uarp.encodePathSegment
import ai.snaga.uarp.uarpJson
import ai.snaga.uarp.models.*
import kotlinx.serialization.encodeToString
import kotlinx.serialization.json.JsonElement
import kotlinx.serialization.json.JsonObject

/**
 * Knowledge base management
 */
public class KnowledgeApi internal constructor(private val client: UarpClient) {
    /**
     * Create a knowledge base
     *
     * `POST /api/v1/knowledge-bases`
     *
     * Required scopes: `memory:write`.
     */
    public suspend fun createKnowledgeBase(body: KnowledgeBaseCreate, options: RequestOptions = RequestOptions()): KnowledgeBase {
        return client.request<KnowledgeBase>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/knowledge-bases",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Delete document
     *
     * `DELETE /api/v1/knowledge-bases/{id}/documents/{docId}`
     *
     * Required scopes: `memory:write`.
     */
    public suspend fun deleteKbDocument(id: String, docId: String, options: RequestOptions = RequestOptions()): DeleteKbDocumentResponse {
        return client.request<DeleteKbDocumentResponse>(
            RequestSpec(
                method = "DELETE",
                path = "/api/v1/knowledge-bases/${encodePathSegment(id)}/documents/${encodePathSegment(docId)}",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Delete knowledge base
     *
     * `DELETE /api/v1/knowledge-bases/{id}`
     *
     * Required scopes: `memory:write`.
     */
    public suspend fun deleteKnowledgeBase(id: String, options: RequestOptions = RequestOptions()): DeleteKnowledgeBaseResponse {
        return client.request<DeleteKnowledgeBaseResponse>(
            RequestSpec(
                method = "DELETE",
                path = "/api/v1/knowledge-bases/${encodePathSegment(id)}",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Get knowledge base
     *
     * `GET /api/v1/knowledge-bases/{id}`
     *
     * Required scopes: `memory:read`.
     */
    public suspend fun getKnowledgeBase(id: String, options: RequestOptions = RequestOptions()): KnowledgeBase {
        return client.request<KnowledgeBase>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/knowledge-bases/${encodePathSegment(id)}",
                options = options,
            )
        )
    }

    /**
     * Ingest document into knowledge base
     *
     * `POST /api/v1/knowledge-bases/{kbId}/documents`
     *
     * Required scopes: `memory:write`.
     */
    public suspend fun ingestKbDocument(kbId: String, body: IngestKbDocumentRequest, options: RequestOptions = RequestOptions()): IngestKbDocumentResponse {
        return client.request<IngestKbDocumentResponse>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/knowledge-bases/${encodePathSegment(kbId)}/documents",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * List documents in knowledge base
     *
     * `GET /api/v1/knowledge-bases/{kbId}/documents`
     *
     * Required scopes: `memory:read`.
     */
    public suspend fun listKbDocuments(kbId: String, options: RequestOptions = RequestOptions()): ListKbDocumentsResponse {
        return client.request<ListKbDocumentsResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/knowledge-bases/${encodePathSegment(kbId)}/documents",
                options = options,
            )
        )
    }

    /**
     * List knowledge bases
     *
     * `GET /api/v1/knowledge-bases`
     *
     * Required scopes: `memory:read`.
     */
    public suspend fun listKnowledgeBases(options: RequestOptions = RequestOptions()): ListKnowledgeBasesResponse {
        return client.request<ListKnowledgeBasesResponse>(
            RequestSpec(
                method = "GET",
                path = "/api/v1/knowledge-bases",
                options = options,
            )
        )
    }

    /**
     * Re-embed every chunk with the current model
     *
     * Recovers a knowledge base that was indexed without embeddings (keyword-only) and clears
     * embedding drift after a model change. Requires an embeddings backend: without one the answer
     * is 503 and nothing is written.
     *
     * `POST /api/v1/knowledge-bases/{kbId}/reindex`
     *
     * Required scopes: `memory:write`.
     */
    public suspend fun reindexKnowledgeBase(kbId: String, options: RequestOptions = RequestOptions()): ReindexKnowledgeBaseResponse {
        return client.request<ReindexKnowledgeBaseResponse>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/knowledge-bases/${encodePathSegment(kbId)}/reindex",
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Retrieve chunks from one knowledge base
     *
     * Vector search when an embeddings backend is configured, keyword scoring when it is not —
     * `mode` says which ran, and a vector pass that matches nothing falls back to keyword rather
     * than answering empty. `status` distinguishes the three outcomes a caller must render
     * differently: `KB_EMPTY` (nothing indexed yet), `NO_MATCHES` (indexed, nothing matched) and
     * `RESULTS_FOUND`. All three are 200: an empty result is an answer here, not a failure.
     *
     * `POST /api/v1/knowledge-bases/{kbId}/search`
     *
     * Required scopes: `memory:write`.
     */
    public suspend fun searchKnowledgeBase(kbId: String, body: SearchKnowledgeBaseRequest, options: RequestOptions = RequestOptions()): KnowledgeBaseSearchResult {
        return client.request<KnowledgeBaseSearchResult>(
            RequestSpec(
                method = "POST",
                path = "/api/v1/knowledge-bases/${encodePathSegment(kbId)}/search",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }

    /**
     * Update knowledge base
     *
     * WRITE SEMANTICS: merges. Measured 2026-08-31 on the wire: `{name}` left `description` intact
     * and vice versa, and `chunk_size` and `embedding_model` survived both. Note this is the
     * OPPOSITE of `PUT /agents/{id}/schedule`, which replaces — the verb decides nothing here, see
     * docs/WRITE_SEMANTICS.md.
     *
     * `PUT /api/v1/knowledge-bases/{id}`
     *
     * Required scopes: `memory:write`.
     */
    public suspend fun updateKnowledgeBase(id: String, body: KnowledgeBaseUpdate, options: RequestOptions = RequestOptions()): KnowledgeBase {
        return client.request<KnowledgeBase>(
            RequestSpec(
                method = "PUT",
                path = "/api/v1/knowledge-bases/${encodePathSegment(id)}",
                body = Body.Json(uarpJson.encodeToString(body)),
                idempotent = true,
                options = options,
            )
        )
    }
}

/**
 * Knowledge base management
 */
public val UarpClient.knowledge: KnowledgeApi get() = KnowledgeApi(this)
