// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.
//!
//! Squads — multi-agent groups that run a shared brief

#![allow(unused_imports, clippy::too_many_arguments)]

use reqwest::Method;
use serde::{Deserialize, Serialize};

use crate::client::{Client, Request, NO_BODY, NO_QUERY};
use crate::error::Result;
use crate::generated::models;
use crate::multipart::{field_text, FilePart};
use crate::sse::EventStream;
use crate::util::encode_path;

/// Query and header parameters for `getSquadChatHistory`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct GetSquadChatHistoryParams {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub thread_id: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub include_internal: Option<bool>,
}

/// Query and header parameters for `streamSquadChatEvents`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct StreamSquadChatEventsParams {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub thread_id: Option<String>,
}

/// Squads — multi-agent groups that run a shared brief
#[derive(Debug, Clone)]
pub struct SquadsApi {
    pub(crate) client: Client,
}

impl Client {
    /// Squads — multi-agent groups that run a shared brief
    pub fn squads(&self) -> SquadsApi {
        SquadsApi { client: self.clone() }
    }
}

impl SquadsApi {
    /// Add graph edge
    ///
    /// `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
    /// older noun: the server rewrites the leading path segment before dispatch, so the two are one
    /// endpoint and cannot drift apart.
    ///
    /// `POST /api/v1/squads/{squadId}/graph/edges`
    pub async fn add_squad_graph_edge(&self, squad_id: &str, body: &models::AddSquadGraphEdgeRequest) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::POST,
                path: format!("/api/v1/squads/{}/graph/edges", encode_path(squad_id)),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Add graph node
    ///
    /// `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
    /// older noun: the server rewrites the leading path segment before dispatch, so the two are one
    /// endpoint and cannot drift apart.
    ///
    /// `POST /api/v1/squads/{squadId}/graph/nodes`
    pub async fn add_squad_graph_node(&self, squad_id: &str, body: &models::AddSquadGraphNodeRequest) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::POST,
                path: format!("/api/v1/squads/{}/graph/nodes", encode_path(squad_id)),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Cancel a squad run
    ///
    /// Stops the orchestration loop first, then every non-terminal child run, and releases the chat
    /// state so the canvas does not stay locked on a run that was just killed.
    ///
    /// Order matters and is not an implementation detail: killing children while the loop is still
    /// running makes it spawn more — two fresh child runs were measured within two minutes of a
    /// “successful” cancel.
    ///
    /// `cancelledCount` is camelCase on the wire, unlike every neighbouring field. That is what the
    /// server sends.
    ///
    /// `/api/v1/squads/*` is the canonical surface; `/api/v1/teams/*` is the same handler under the
    /// older noun.
    ///
    /// `POST /api/v1/squads/{squadId}/runs/{teamRunId}/cancel`
    pub async fn cancel_squad_run(&self, squad_id: &str, team_run_id: &str) -> Result<models::CancelSquadRunResponse> {
        self.client
            .request_json(Request {
                method: Method::POST,
                path: format!("/api/v1/squads/{}/runs/{}/cancel", encode_path(squad_id), encode_path(team_run_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Create a squad
    ///
    /// `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
    /// older noun: the server rewrites the leading path segment before dispatch, so the two are one
    /// endpoint and cannot drift apart.
    ///
    /// `POST /api/v1/squads`
    pub async fn create(&self, body: &models::TeamCreate) -> Result<models::Team> {
        self.client
            .request_json(Request {
                method: Method::POST,
                path: "/api/v1/squads".to_string(),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Delete squad
    ///
    /// `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
    /// older noun: the server rewrites the leading path segment before dispatch, so the two are one
    /// endpoint and cannot drift apart.
    ///
    /// `DELETE /api/v1/squads/{squadId}`
    pub async fn delete(&self, squad_id: &str) -> Result<models::DeleteSquadResponse> {
        self.client
            .request_json(Request {
                method: Method::DELETE,
                path: format!("/api/v1/squads/{}", encode_path(squad_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Remove edge
    ///
    /// `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
    /// older noun: the server rewrites the leading path segment before dispatch, so the two are one
    /// endpoint and cannot drift apart.
    ///
    /// `DELETE /api/v1/squads/{squadId}/graph/edges/{edgeId}`
    pub async fn delete_squad_graph_edge(&self, squad_id: &str, edge_id: &str) -> Result<models::DeleteSquadGraphEdgeResponse> {
        self.client
            .request_json(Request {
                method: Method::DELETE,
                path: format!("/api/v1/squads/{}/graph/edges/{}", encode_path(squad_id), encode_path(edge_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Remove node
    ///
    /// `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
    /// older noun: the server rewrites the leading path segment before dispatch, so the two are one
    /// endpoint and cannot drift apart.
    ///
    /// `DELETE /api/v1/squads/{squadId}/graph/nodes/{agentId}`
    pub async fn delete_squad_graph_node(&self, squad_id: &str, agent_id: &str) -> Result<models::DeleteSquadGraphNodeResponse> {
        self.client
            .request_json(Request {
                method: Method::DELETE,
                path: format!("/api/v1/squads/{}/graph/nodes/{}", encode_path(squad_id), encode_path(agent_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Get squad
    ///
    /// `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
    /// older noun: the server rewrites the leading path segment before dispatch, so the two are one
    /// endpoint and cannot drift apart.
    ///
    /// `GET /api/v1/squads/{squadId}`
    pub async fn get(&self, squad_id: &str) -> Result<models::Team> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: format!("/api/v1/squads/{}", encode_path(squad_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Get squad chat history
    ///
    /// `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
    /// older noun: the server rewrites the leading path segment before dispatch, so the two are one
    /// endpoint and cannot drift apart.
    ///
    /// `GET /api/v1/squads/{squadId}/chat`
    pub async fn get_squad_chat_history(&self, squad_id: &str, params: &GetSquadChatHistoryParams) -> Result<models::GetSquadChatHistoryResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: format!("/api/v1/squads/{}/chat", encode_path(squad_id)),
                query: Some(params),
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Get full squad graph
    ///
    /// `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
    /// older noun: the server rewrites the leading path segment before dispatch, so the two are one
    /// endpoint and cannot drift apart.
    ///
    /// `GET /api/v1/squads/{squadId}/graph`
    pub async fn get_squad_graph(&self, squad_id: &str) -> Result<models::GetSquadGraphResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: format!("/api/v1/squads/{}/graph", encode_path(squad_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Get graph node
    ///
    /// `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
    /// older noun: the server rewrites the leading path segment before dispatch, so the two are one
    /// endpoint and cannot drift apart.
    ///
    /// `GET /api/v1/squads/{squadId}/graph/nodes/{agentId}`
    pub async fn get_squad_graph_node(&self, squad_id: &str, agent_id: &str) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: format!("/api/v1/squads/{}/graph/nodes/{}", encode_path(squad_id), encode_path(agent_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Get specific squad run
    ///
    /// `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
    /// older noun: the server rewrites the leading path segment before dispatch, so the two are one
    /// endpoint and cannot drift apart.
    ///
    /// `GET /api/v1/squads/{squadId}/runs/{teamRunId}`
    pub async fn get_squad_run(&self, squad_id: &str, team_run_id: &str) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: format!("/api/v1/squads/{}/runs/{}", encode_path(squad_id), encode_path(team_run_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Get squad run messages
    ///
    /// `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
    /// older noun: the server rewrites the leading path segment before dispatch, so the two are one
    /// endpoint and cannot drift apart.
    ///
    /// `GET /api/v1/squads/{squadId}/runs/{teamRunId}/messages`
    pub async fn get_squad_run_messages(&self, squad_id: &str, team_run_id: &str) -> Result<models::GetSquadRunMessagesResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: format!("/api/v1/squads/{}/runs/{}/messages", encode_path(squad_id), encode_path(team_run_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// List squads
    ///
    /// `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
    /// older noun: the server rewrites the leading path segment before dispatch, so the two are one
    /// endpoint and cannot drift apart.
    ///
    /// `GET /api/v1/squads`
    pub async fn list(&self) -> Result<models::ListSquadsResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/squads".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// List graph edges
    ///
    /// `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
    /// older noun: the server rewrites the leading path segment before dispatch, so the two are one
    /// endpoint and cannot drift apart.
    ///
    /// `GET /api/v1/squads/{squadId}/graph/edges`
    pub async fn list_squad_graph_edges(&self, squad_id: &str) -> Result<models::ListSquadGraphEdgesResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: format!("/api/v1/squads/{}/graph/edges", encode_path(squad_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// List graph nodes
    ///
    /// `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
    /// older noun: the server rewrites the leading path segment before dispatch, so the two are one
    /// endpoint and cannot drift apart.
    ///
    /// `GET /api/v1/squads/{squadId}/graph/nodes`
    pub async fn list_squad_graph_nodes(&self, squad_id: &str) -> Result<models::ListSquadGraphNodesResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: format!("/api/v1/squads/{}/graph/nodes", encode_path(squad_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// List runs for a squad
    ///
    /// `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
    /// older noun: the server rewrites the leading path segment before dispatch, so the two are one
    /// endpoint and cannot drift apart.
    ///
    /// `GET /api/v1/squads/{squadId}/runs`
    pub async fn list_squad_runs(&self, squad_id: &str) -> Result<models::ListSquadRunsResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: format!("/api/v1/squads/{}/runs", encode_path(squad_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Start a squad run
    ///
    /// `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
    /// older noun: the server rewrites the leading path segment before dispatch, so the two are one
    /// endpoint and cannot drift apart.
    ///
    /// `POST /api/v1/squads/{squadId}/runs`
    pub async fn start_squad_run(&self, squad_id: &str, body: &models::StartSquadRunRequest) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::POST,
                path: format!("/api/v1/squads/{}/runs", encode_path(squad_id)),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// SSE stream for squad chat
    ///
    /// `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
    /// older noun: the server rewrites the leading path segment before dispatch, so the two are one
    /// endpoint and cannot drift apart.
    ///
    /// `GET /api/v1/squads/{squadId}/chat/events`
    ///
    /// Required scopes: `events:read`.
    ///
    /// Returns a server-sent event stream.
    pub fn stream_squad_chat_events(&self, squad_id: &str, params: &StreamSquadChatEventsParams) -> EventStream {
        self.client.request_stream(
            &format!("/api/v1/squads/{}/chat/events", encode_path(squad_id)),
            Some(params),
            Vec::new(),
        )
    }

    /// Stream squad run events (SSE)
    ///
    /// `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
    /// older noun: the server rewrites the leading path segment before dispatch, so the two are one
    /// endpoint and cannot drift apart.
    ///
    /// `GET /api/v1/squads/{squadId}/runs/{runId}/events`
    ///
    /// Required scopes: `events:read`.
    ///
    /// Returns a server-sent event stream.
    pub fn stream_squad_run_events(&self, squad_id: &str, run_id: &str) -> EventStream {
        self.client.request_stream(
            &format!("/api/v1/squads/{}/runs/{}/events", encode_path(squad_id), encode_path(run_id)),
            NO_QUERY,
            Vec::new(),
        )
    }

    /// Update squad
    ///
    /// `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
    /// older noun: the server rewrites the leading path segment before dispatch, so the two are one
    /// endpoint and cannot drift apart.
    ///
    /// `PUT /api/v1/squads/{squadId}`
    pub async fn update(&self, squad_id: &str, body: &models::TeamUpdate) -> Result<models::Team> {
        self.client
            .request_json(Request {
                method: Method::PUT,
                path: format!("/api/v1/squads/{}", encode_path(squad_id)),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Update graph node
    ///
    /// `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
    /// older noun: the server rewrites the leading path segment before dispatch, so the two are one
    /// endpoint and cannot drift apart.
    ///
    /// `PATCH /api/v1/squads/{squadId}/graph/nodes/{agentId}`
    pub async fn update_squad_graph_node(&self, squad_id: &str, agent_id: &str, body: &models::UpdateSquadGraphNodeRequest) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::PATCH,
                path: format!("/api/v1/squads/{}/graph/nodes/{}", encode_path(squad_id), encode_path(agent_id)),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }
}
