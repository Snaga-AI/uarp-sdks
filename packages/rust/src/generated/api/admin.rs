// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.
//!
//! Platform admin: tenants, audit, reconciliation, DLQ, analytics

#![allow(unused_imports, clippy::too_many_arguments)]

use reqwest::Method;
use serde::{Deserialize, Serialize};
use futures_core::Stream;

use crate::client::{Client, Request, NO_BODY, NO_QUERY};
use crate::error::Result;
use crate::generated::models;
use crate::multipart::{field_text, FilePart};
use crate::pagination::CursorGuard;
use crate::util::encode_path;

/// Query and header parameters for `adminAnalyticsAgents`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct AdminAnalyticsAgentsParams {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub days: Option<i64>,
}

/// Query and header parameters for `adminAnalyticsEvents`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct AdminAnalyticsEventsParams {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub days: Option<i64>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub r#type: Option<String>,
}

/// Query and header parameters for `adminAnalyticsOverview`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct AdminAnalyticsOverviewParams {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub days: Option<i64>,
}

/// Query and header parameters for `adminDataExplorerRawKeys`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct AdminDataExplorerRawKeysParams {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cursor: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
    /// Comma-separated KV key parts; restricts the scan range.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub prefix: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub search: Option<String>,
}

/// Query and header parameters for `getAuditForTarget`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct GetAuditForTargetParams {
    /// Target type (e.g., agent, run, tenant)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub r#type: Option<String>,
}

/// Query and header parameters for `getImmutableAudit`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct GetImmutableAuditParams {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub agent_id: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub event: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub from: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub to: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
}

/// Query and header parameters for `getPlatformEconomics`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct GetPlatformEconomicsParams {
    /// `1` bypasses the cache and recomputes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub refresh: Option<models::DeleteCustomPlanForce>,
}

/// Query and header parameters for `getTenantUsage`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct GetTenantUsageParams {
    /// ISO YYYY-MM period (defaults to current month)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub period: Option<String>,
}

/// Query and header parameters for `listAndroidTesters`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct ListAndroidTestersParams {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cursor: Option<String>,
}

/// Query and header parameters for `listFeedback`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct ListFeedbackParams {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<models::ErrorReportStatus>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
}

/// Query and header parameters for `queryAuditLog`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct QueryAuditLogParams {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub action: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub actor_id: Option<String>,
}

/// Platform admin: tenants, audit, reconciliation, DLQ, analytics
#[derive(Debug, Clone)]
pub struct AdminApi {
    pub(crate) client: Client,
}

impl Client {
    /// Platform admin: tenants, audit, reconciliation, DLQ, analytics
    pub fn admin(&self) -> AdminApi {
        AdminApi { client: self.clone() }
    }
}

impl AdminApi {
    /// Add addresses to the roster by hand
    ///
    /// **Super-admin only.** Addresses reach the owner from the Play console and from people who
    /// write directly, so the landing form is not the only door. Partial success is normal: each
    /// address lands in exactly one of the three lists and the call is still 200.
    ///
    /// `POST /api/v1/admin/testers/android`
    ///
    /// Required scopes: `admin`.
    pub async fn add_android_testers(&self, body: &models::AddAndroidTestersRequest) -> Result<models::AddAndroidTestersResponse> {
        self.client
            .request_json(Request {
                method: Method::POST,
                path: "/api/v1/admin/testers/android".to_string(),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Platform-wide agent analytics
    ///
    /// `GET /api/v1/admin/analytics/agents`
    ///
    /// Required scopes: `admin`.
    pub async fn admin_analytics_agents(&self, params: &AdminAnalyticsAgentsParams) -> Result<models::AgentAnalyticsSummary> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/analytics/agents".to_string(),
                query: Some(params),
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Platform-wide event analytics
    ///
    /// `GET /api/v1/admin/analytics/events`
    ///
    /// Required scopes: `admin`.
    pub async fn admin_analytics_events(&self, params: &AdminAnalyticsEventsParams) -> Result<models::AdminAnalyticsEventsResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/analytics/events".to_string(),
                query: Some(params),
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Platform-wide analytics overview
    ///
    /// `GET /api/v1/admin/analytics/overview`
    ///
    /// Required scopes: `admin`.
    pub async fn admin_analytics_overview(&self, params: &AdminAnalyticsOverviewParams) -> Result<models::AdminAnalyticsOverviewResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/analytics/overview".to_string(),
                query: Some(params),
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Full KV scan (admin diagnostic)
    ///
    /// Dangerous: full DB key scan. Admin-only. Use the namespace-scoped endpoints under
    /// `/admin/data-explorer/*` for normal browsing.
    ///
    /// `GET /api/v1/admin/data-explorer/raw-keys`
    ///
    /// Required scopes: `admin`.
    pub async fn admin_data_explorer_raw_keys(&self, params: &AdminDataExplorerRawKeysParams) -> Result<models::AdminDataExplorerRawKeysResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/data-explorer/raw-keys".to_string(),
                query: Some(params),
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Stream every item returned by `adminDataExplorerRawKeys`, following the `cursor` cursor
    /// until the server reports no further pages.
    pub fn admin_data_explorer_raw_keys_all<'a>(&'a self, params: &'a AdminDataExplorerRawKeysParams) -> impl Stream<Item = Result<models::AdminDataExplorerRawKeysResponseKey>> + 'a {
        async_stream::try_stream! {
            let mut guard = CursorGuard::new();
            let mut cursor = params.cursor.clone();
            loop {
                let mut page_params = params.clone();
                page_params.cursor = cursor.clone();
                let page = self.admin_data_explorer_raw_keys(&page_params).await?;
                let items = page.keys;
                let was_empty = items.is_empty();
                for item in items {
                    yield item;
                }
                match guard.advance(page.cursor, None, was_empty) {
                    Some(next) => cursor = Some(next),
                    None => break,
                }
            }
        }
    }

    /// Remove OAuth login provider (super-admin)
    ///
    /// `DELETE /api/v1/admin/oauth-login-providers/{provider}`
    ///
    /// Required scopes: `admin`.
    pub async fn admin_delete_o_auth_provider(&self, provider: &models::OAuthLoginProviderConfigStatusProvider) -> Result<models::OAuthLoginProviderConfigDeleted> {
        self.client
            .request_json(Request {
                method: Method::DELETE,
                path: format!("/api/v1/admin/oauth-login-providers/{}", encode_path(&provider.to_string())),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Admin: get landing-page featured-agent config
    ///
    /// `GET /api/v1/admin/config/landing`
    ///
    /// Required scopes: `admin`.
    pub async fn admin_get_landing_config(&self) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/config/landing".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Admin: get curated model catalog
    ///
    /// `GET /api/v1/admin/config/model-catalog`
    ///
    /// Required scopes: `admin`.
    pub async fn admin_get_model_catalog(&self) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/config/model-catalog".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Admin: model catalog default seed
    ///
    /// `GET /api/v1/admin/config/model-catalog/seed`
    ///
    /// Required scopes: `admin`.
    pub async fn admin_get_model_catalog_seed(&self) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/config/model-catalog/seed".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Admin: live per-model pricing (synced from the configured provider)
    ///
    /// `GET /api/v1/admin/config/model-pricing`
    ///
    /// Required scopes: `admin`.
    pub async fn admin_get_model_pricing(&self) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/config/model-pricing".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Get configured OAuth login provider (super-admin)
    ///
    /// Returns provider config with the client_secret masked (last 4 chars only). Returns
    /// `{configured: false}` for unconfigured providers.
    ///
    /// `GET /api/v1/admin/oauth-login-providers/{provider}`
    ///
    /// Required scopes: `admin`.
    pub async fn admin_get_o_auth_provider(&self, provider: &models::OAuthLoginProviderConfigStatusProvider) -> Result<models::OAuthLoginProviderConfigStatus> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: format!("/api/v1/admin/oauth-login-providers/{}", encode_path(&provider.to_string())),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Get last stored cost-reconciliation result for a tenant
    ///
    /// `GET /api/v1/admin/reconciliation/{tenantId}`
    ///
    /// Required scopes: `admin`.
    pub async fn admin_get_reconciliation(&self, tenant_id: &str) -> Result<models::AdminGetReconciliationResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: format!("/api/v1/admin/reconciliation/{}", encode_path(tenant_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Admin: get default voice (STT/TTS) config
    ///
    /// `GET /api/v1/admin/config/voice`
    ///
    /// Required scopes: `admin`.
    pub async fn admin_get_voice_config(&self) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/config/voice".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Admin: get per-model voice presets
    ///
    /// `GET /api/v1/admin/config/voice-presets`
    ///
    /// Required scopes: `admin`.
    pub async fn admin_get_voice_presets(&self) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/config/voice-presets".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Admin: enumerate available built-in tools
    ///
    /// `GET /api/v1/admin/tools`
    ///
    /// Required scopes: `admin`.
    pub async fn admin_list_tools(&self) -> Result<models::AdminListToolsResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/tools".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// List dead-lettered webhook deliveries
    ///
    /// `GET /api/v1/admin/webhooks/dlq`
    ///
    /// Required scopes: `admin`.
    pub async fn admin_list_webhook_dlq(&self) -> Result<models::AdminListWebhookDLQResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/webhooks/dlq".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Admin: set landing-page featured-agent config
    ///
    /// `PUT /api/v1/admin/config/landing`
    ///
    /// Required scopes: `admin`.
    pub async fn admin_put_landing_config(&self, body: &serde_json::Map<String, serde_json::Value>) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::PUT,
                path: "/api/v1/admin/config/landing".to_string(),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Admin: replace curated model catalog
    ///
    /// `PUT /api/v1/admin/config/model-catalog`
    ///
    /// Required scopes: `admin`.
    pub async fn admin_put_model_catalog(&self, body: &serde_json::Map<String, serde_json::Value>) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::PUT,
                path: "/api/v1/admin/config/model-catalog".to_string(),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Set/update OAuth login provider (super-admin)
    ///
    /// Merge semantics: omitting client_secret keeps the existing one (so toggling `enabled`
    /// doesn't require re-pasting the secret). First-time PUT requires both client_id and
    /// client_secret. Stored encrypted at rest.
    ///
    /// `PUT /api/v1/admin/oauth-login-providers/{provider}`
    ///
    /// Required scopes: `admin`.
    pub async fn admin_put_o_auth_provider(&self, provider: &models::OAuthLoginProviderConfigStatusProvider, body: &models::OAuthLoginProviderConfigUpdate) -> Result<models::OAuthLoginProviderConfigUpdateResponse> {
        self.client
            .request_json(Request {
                method: Method::PUT,
                path: format!("/api/v1/admin/oauth-login-providers/{}", encode_path(&provider.to_string())),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Admin: set default voice config
    ///
    /// `PUT /api/v1/admin/config/voice`
    ///
    /// Required scopes: `admin`.
    pub async fn admin_put_voice_config(&self, body: &serde_json::Map<String, serde_json::Value>) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::PUT,
                path: "/api/v1/admin/config/voice".to_string(),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Admin: set per-model voice presets
    ///
    /// `PUT /api/v1/admin/config/voice-presets`
    ///
    /// Required scopes: `admin`.
    pub async fn admin_put_voice_presets(&self, body: &serde_json::Map<String, serde_json::Value>) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::PUT,
                path: "/api/v1/admin/config/voice-presets".to_string(),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Replay a dead-lettered webhook event
    ///
    /// `POST /api/v1/admin/webhooks/dlq/{eventId}/replay`
    ///
    /// Required scopes: `admin`.
    pub async fn admin_replay_webhook_dlq(&self, event_id: &str) -> Result<models::AdminReplayWebhookDLQResponse> {
        self.client
            .request_json(Request {
                method: Method::POST,
                path: format!("/api/v1/admin/webhooks/dlq/{}/replay", encode_path(event_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Write a post by hand
    ///
    /// `source` is stamped `manual` and cannot be set by the caller. The slug is derived from the
    /// title and made unique; `status` defaults to `draft`.
    ///
    /// `POST /api/v1/admin/blog/posts`
    ///
    /// Required scopes: `admin`.
    pub async fn create_admin_blog_post(&self, body: &models::CreateAdminBlogPostRequest) -> Result<models::CreateAdminBlogPostResponse> {
        self.client
            .request_json(Request {
                method: Method::POST,
                path: "/api/v1/admin/blog/posts".to_string(),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Create custom provider
    ///
    /// `POST /api/v1/admin/providers`
    ///
    /// Required scopes: `admin`.
    pub async fn create_admin_provider(&self, body: &serde_json::Map<String, serde_json::Value>) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::POST,
                path: "/api/v1/admin/providers".to_string(),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Create new tenant
    ///
    /// `POST /api/v1/admin/tenants`
    ///
    /// Required scopes: `admin`.
    pub async fn create_tenant(&self, body: &serde_json::Map<String, serde_json::Value>) -> Result<models::Tenant> {
        self.client
            .request_json(Request {
                method: Method::POST,
                path: "/api/v1/admin/tenants".to_string(),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Delete a post
    ///
    /// `DELETE /api/v1/admin/blog/posts/{postId}`
    ///
    /// Required scopes: `admin`.
    pub async fn delete_admin_blog_post(&self, post_id: &str) -> Result<models::DeleteAdminBlogPostResponse> {
        self.client
            .request_json(Request {
                method: Method::DELETE,
                path: format!("/api/v1/admin/blog/posts/{}", encode_path(post_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Remove one provider's stored OAuth credentials
    ///
    /// Deletes the record outright — this is the only way to clear a stored credential, since the
    /// PUT treats a blank value as "keep". Idempotent: deleting a provider that has nothing stored
    /// is still 200.
    ///
    /// `DELETE /api/v1/admin/integration-oauth-providers/{provider}`
    ///
    /// Required scopes: `admin`.
    pub async fn delete_admin_integration_o_auth_provider(&self, provider: &models::GetAdminIntegrationOAuthProviderProvider) -> Result<models::DeleteAdminIntegrationOAuthProviderResponse> {
        self.client
            .request_json(Request {
                method: Method::DELETE,
                path: format!("/api/v1/admin/integration-oauth-providers/{}", encode_path(&provider.to_string())),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Remove the platform API key for a provider
    ///
    /// Drops the stored platform key. The PROVIDER survives — this is the key, not the definition;
    /// `DELETE /api/v1/admin/providers/{providerId}` is the other one, and it removes a custom
    /// provider outright.
    ///
    /// Answers `configured: false` for the provider, which is the same field `GET
    /// /api/v1/admin/llm-defaults` reports per provider, so the caller can apply the answer without
    /// a re-read.
    ///
    /// `DELETE /api/v1/admin/llm-defaults/{providerId}`
    ///
    /// Required scopes: `admin`.
    pub async fn delete_admin_llm_default(&self, provider_id: &str) -> Result<models::DeleteAdminLLMDefaultResponse> {
        self.client
            .request_json(Request {
                method: Method::DELETE,
                path: format!("/api/v1/admin/llm-defaults/{}", encode_path(provider_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Remove a custom provider (super-admin, fresh MFA)
    ///
    /// Removes a CUSTOM provider and its settings. A built-in provider is not deletable and answers
    /// 404 here — that 404 means "no such CUSTOM provider", not "no such provider", which is worth
    /// knowing before reading it as a routing mistake.
    ///
    /// Gated on a fresh MFA challenge because it is irreversible: the definition and its settings
    /// are deleted outright, not disabled. To stop using a provider without losing it, PATCH
    /// `enabled: false` instead. To drop the platform API KEY while keeping the provider, use
    /// `DELETE /api/v1/admin/llm-defaults/{providerId}` — a different route with a different
    /// subject.
    ///
    /// `DELETE /api/v1/admin/providers/{providerId}`
    ///
    /// Required scopes: `admin`.
    pub async fn delete_admin_provider(&self, provider_id: &str) -> Result<models::DeleteAdminProviderResponse> {
        self.client
            .request_json(Request {
                method: Method::DELETE,
                path: format!("/api/v1/admin/providers/{}", encode_path(provider_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Take one address off the roster
    ///
    /// **Super-admin only.** The address is a path segment, so it arrives percent-encoded.
    ///
    /// `DELETE /api/v1/admin/testers/android/{email}`
    ///
    /// Required scopes: `admin`.
    pub async fn delete_android_tester(&self, email: &str) -> Result<()> {
        self.client
            .request_empty(Request {
                method: Method::DELETE,
                path: format!("/api/v1/admin/testers/android/{}", encode_path(email)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Run the authoring agent now and create a post
    ///
    /// Generates immediately, ignoring `frequency` — this is the operator's manual trigger, not a
    /// schedule nudge. Whether the result lands published or as a draft follows the stored
    /// `auto_publish`.
    ///
    /// A generation that FAILS is **422**, not a 200 carrying an error field, so a client reads the
    /// status. Success is **201**.
    ///
    /// `POST /api/v1/admin/blog/generate`
    ///
    /// Required scopes: `admin`.
    pub async fn generate_admin_blog_post(&self) -> Result<models::GenerateAdminBlogPostResponse> {
        self.client
            .request_json(Request {
                method: Method::POST,
                path: "/api/v1/admin/blog/generate".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Get the blog's authoring configuration
    ///
    /// Super-admin only. Wrapped in `{config}` rather than returned bare — the whole blog surface
    /// uses envelopes.
    ///
    /// `GET /api/v1/admin/blog/config`
    ///
    /// Required scopes: `admin`.
    pub async fn get_admin_blog_config(&self) -> Result<models::GetAdminBlogConfigResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/blog/config".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Read one provider's stored OAuth credentials (secret masked)
    ///
    /// **The response has two shapes and a client must handle both.** With nothing stored it is the
    /// three-field form — `{provider, enabled: false, configured: false}` — and `client_id`,
    /// `client_secret_hint` and `scopes` are ABSENT, not null. With a record stored, all six are
    /// present.
    ///
    /// The secret is never echoed. `client_secret_hint` is the last four characters behind dots,
    /// enough for an operator to confirm which credential is stored without seeing it, and it is
    /// null when the stored secret is empty.
    ///
    /// `GET /api/v1/admin/integration-oauth-providers/{provider}`
    ///
    /// Required scopes: `admin`.
    pub async fn get_admin_integration_o_auth_provider(&self, provider: &models::GetAdminIntegrationOAuthProviderProvider) -> Result<models::GetAdminIntegrationOAuthProviderResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: format!("/api/v1/admin/integration-oauth-providers/{}", encode_path(&provider.to_string())),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// List platform API keys (masked)
    ///
    /// `GET /api/v1/admin/llm-defaults`
    ///
    /// Required scopes: `admin`.
    pub async fn get_admin_llm_defaults(&self) -> Result<models::GetAdminLLMDefaultsResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/llm-defaults".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Get pricing configuration
    ///
    /// `GET /api/v1/admin/config/pricing`
    ///
    /// Required scopes: `admin`.
    pub async fn get_admin_pricing(&self) -> Result<serde_json::Value> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/config/pricing".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Get single provider
    ///
    /// `GET /api/v1/admin/providers/{providerId}`
    ///
    /// Required scopes: `admin`.
    pub async fn get_admin_provider(&self, provider_id: &str) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: format!("/api/v1/admin/providers/{}", encode_path(provider_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Aggregate usage stats
    ///
    /// `GET /api/v1/admin/stats`
    ///
    /// Required scopes: `admin`.
    pub async fn get_admin_stats(&self) -> Result<models::GetAdminStatsResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/stats".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Trace delegation chain
    ///
    /// `GET /api/v1/admin/trace/{traceId}`
    ///
    /// Required scopes: `admin`.
    pub async fn get_admin_trace(&self, trace_id: &str) -> Result<models::GetAdminTraceResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: format!("/api/v1/admin/trace/{}", encode_path(trace_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Get audit entries for a specific target
    ///
    /// `GET /api/v1/admin/audit/{targetId}`
    ///
    /// Required scopes: `admin`.
    pub async fn get_audit_for_target(&self, target_id: &str, params: &GetAuditForTargetParams) -> Result<serde_json::Value> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: format!("/api/v1/admin/audit/{}", encode_path(target_id)),
                query: Some(params),
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// EU AI Act conformity report
    ///
    /// `GET /api/v1/admin/conformity-report`
    ///
    /// Required scopes: `admin`.
    pub async fn get_conformity_report(&self) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/conformity-report".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Immutable audit log
    ///
    /// `GET /api/v1/admin/immutable-audit`
    ///
    /// Required scopes: `admin`.
    pub async fn get_immutable_audit(&self, params: &GetImmutableAuditParams) -> Result<models::GetImmutableAuditResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/immutable-audit".to_string(),
                query: Some(params),
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Full maintenance record
    ///
    /// The whole record, including who turned it on and when — the audit trail the public status
    /// deliberately omits. **Super-admin only**, and a caller on the synthetic default tenant is
    /// 401 rather than 403.
    ///
    /// `GET /api/v1/admin/maintenance`
    ///
    /// Required scopes: `admin`.
    pub async fn get_maintenance_state(&self) -> Result<models::MaintenanceState> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/maintenance".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Platform revenue, host cost and margin
    ///
    /// Stripe subscriptions against real DigitalOcean spend, with the computed margin.
    /// **Super-admin only.**
    ///
    /// Served from a short-lived cache; `cache` says whether this response was a hit, a miss, or a
    /// forced recomputation. A Stripe or provider outage does not fail the call — the affected
    /// block carries `error` and the rest is still served, so a partial answer is never mistaken
    /// for zeros.
    ///
    /// `GET /api/v1/admin/economics`
    ///
    /// Required scopes: `admin`.
    pub async fn get_platform_economics(&self, params: &GetPlatformEconomicsParams) -> Result<models::PlatformEconomics> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/economics".to_string(),
                query: Some(params),
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Get tenant details
    ///
    /// `GET /api/v1/admin/tenants/{tenantId}`
    ///
    /// Required scopes: `admin`.
    pub async fn get_tenant(&self, tenant_id: &str) -> Result<models::Tenant> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: format!("/api/v1/admin/tenants/{}", encode_path(tenant_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// One tenant's mission-framework overrides, and what they resolve to
    ///
    /// Answers both layers at once: `mef_config` is what an operator stored for this tenant,
    /// `effective` is what the runtime will actually do. They differ whenever the platform is the
    /// deciding factor — with the mission service absent, every effective flag is false no matter
    /// what the tenant record says, so an operator reading only `mef_config` sees settings that do
    /// nothing.
    ///
    /// `mef_config` is `null` when nothing is overridden, not an empty object.
    ///
    /// `GET /api/v1/admin/tenants/{tenantId}/mef-config`
    ///
    /// Required scopes: `admin`.
    pub async fn get_tenant_mef_config(&self, tenant_id: &str) -> Result<models::TenantMefConfigResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: format!("/api/v1/admin/tenants/{}/mef-config", encode_path(tenant_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Get tenant usage metrics
    ///
    /// `GET /api/v1/admin/tenants/{tenantId}/usage`
    ///
    /// Required scopes: `admin`.
    pub async fn get_tenant_usage(&self, tenant_id: &str, params: &GetTenantUsageParams) -> Result<models::GetTenantUsageResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: format!("/api/v1/admin/tenants/{}/usage", encode_path(tenant_id)),
                query: Some(params),
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Internal domain verification
    ///
    /// `GET /api/v1/internal/verify-domain`
    pub async fn internal_verify_domain(&self) -> Result<models::InternalVerifyDomainResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/internal/verify-domain".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// List every post, drafts included
    ///
    /// The admin view: unlike the public blog read, drafts are included. Unpaged.
    ///
    /// `GET /api/v1/admin/blog/posts`
    ///
    /// Required scopes: `admin`.
    pub async fn list_admin_blog_posts(&self) -> Result<models::ListAdminBlogPostsResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/blog/posts".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Every tenant's custom domain, worst first
    ///
    /// **The order is the product.** Rows are sorted by problem severity — failed, then drift, then
    /// renewal_due, then pending, then healthy — so an operator's eye lands on what is broken. A
    /// client that re-sorts alphabetically throws that away and should sort back, or not sort at
    /// all.
    ///
    /// Tenants with no custom domain are omitted entirely, so `count` is the number of configured
    /// domains and not the number of tenants.
    ///
    /// Bounded: the tenant registry scan takes at most 5000 entries in one pass and is not paged,
    /// so on a platform past that size this list is silently partial.
    ///
    /// `GET /api/v1/admin/domains/health`
    ///
    /// Required scopes: `admin`.
    pub async fn list_admin_domain_health(&self) -> Result<models::ListAdminDomainHealthResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/domains/health".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Which integration providers an operator has configured
    ///
    /// Super-admin only, and never returns a secret — provider id, an enabled bit, and whether
    /// credentials exist at all. The dashboard uses it to decide which connector rows need a
    /// "Configure" call to action.
    ///
    /// Distinct from `/api/v1/integrations/catalog`, which is tenant-facing and lists every known
    /// connector regardless of OAuth-readiness.
    ///
    /// The list is the SUPPORTED set, not the stored set: every supported provider appears, with
    /// `configured: false` where nothing is stored. So an empty result means the supported set is
    /// empty, never that nothing is configured.
    ///
    /// `GET /api/v1/admin/integration-oauth-providers`
    ///
    /// Required scopes: `admin`.
    pub async fn list_admin_integration_o_auth_providers(&self) -> Result<models::ListAdminIntegrationOAuthProvidersResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/integration-oauth-providers".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// List providers with admin settings
    ///
    /// `GET /api/v1/admin/providers`
    ///
    /// Required scopes: `admin`.
    pub async fn list_admin_providers(&self) -> Result<models::ListAdminProvidersResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/providers".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// The Android closed-testing roster
    ///
    /// **Super-admin only.** The addresses the owner works from when adding people to the Play
    /// group. `count`, `not_yet_emailed` and `given_up` describe THIS PAGE, not the whole roster:
    /// all three are computed over the rows returned, so a roster longer than `limit` under-reports
    /// until every page is walked with `cursor`. `not_yet_emailed` counts rows no letter has gone
    /// to; `given_up` counts the ones the backfill has stopped retrying after repeated refusals,
    /// which need an eye rather than another pass.
    ///
    /// `GET /api/v1/admin/testers/android`
    ///
    /// Required scopes: `admin`.
    pub async fn list_android_testers(&self, params: &ListAndroidTestersParams) -> Result<models::ListAndroidTestersResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/testers/android".to_string(),
                query: Some(params),
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Stream every item returned by `listAndroidTesters`, following the `cursor` cursor until the
    /// server reports no further pages.
    pub fn list_android_testers_all<'a>(&'a self, params: &'a ListAndroidTestersParams) -> impl Stream<Item = Result<models::AndroidTester>> + 'a {
        async_stream::try_stream! {
            let mut guard = CursorGuard::new();
            let mut cursor = params.cursor.clone();
            loop {
                let mut page_params = params.clone();
                page_params.cursor = cursor.clone();
                let page = self.list_android_testers(&page_params).await?;
                let items = page.testers;
                let was_empty = items.is_empty();
                for item in items {
                    yield item;
                }
                match guard.advance(page.cursor, None, was_empty) {
                    Some(next) => cursor = Some(next),
                    None => break,
                }
            }
        }
    }

    /// The reports inbox
    ///
    /// Every report from every tenant, newest first. **Super-admin only.** `new_count` counts the
    /// unresolved reports in the returned set, so a filtered list does not silently under-report
    /// the backlog.
    ///
    /// `GET /api/v1/admin/feedback`
    ///
    /// Required scopes: `admin`.
    pub async fn list_feedback(&self, params: &ListFeedbackParams) -> Result<models::ListFeedbackResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/feedback".to_string(),
                query: Some(params),
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// List all tenants (super admin only)
    ///
    /// `GET /api/v1/admin/tenants`
    ///
    /// Required scopes: `admin`.
    pub async fn list_tenants(&self) -> Result<models::ListTenantsResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/tenants".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Purge tenant
    ///
    /// `DELETE /api/v1/admin/tenants/{tenantId}`
    ///
    /// Required scopes: `admin`.
    pub async fn purge_admin_tenant(&self, tenant_id: &str) -> Result<models::PurgeAdminTenantResponse> {
        self.client
            .request_json(Request {
                method: Method::DELETE,
                path: format!("/api/v1/admin/tenants/{}", encode_path(tenant_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Query admin audit log
    ///
    /// `GET /api/v1/admin/audit`
    ///
    /// Required scopes: `admin`.
    pub async fn query_audit_log(&self, params: &QueryAuditLogParams) -> Result<serde_json::Value> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/admin/audit".to_string(),
                query: Some(params),
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Reactivate a suspended tenant
    ///
    /// `PUT /api/v1/admin/tenants/{tenantId}/reactivate`
    ///
    /// Required scopes: `admin`.
    pub async fn reactivate_tenant(&self, tenant_id: &str) -> Result<serde_json::Value> {
        self.client
            .request_json(Request {
                method: Method::PUT,
                path: format!("/api/v1/admin/tenants/{}/reactivate", encode_path(tenant_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Store or update one provider's OAuth credentials
    ///
    /// WRITE SEMANTICS: merges, and the merge is what makes the route usable. An omitted or blank
    /// `client_id` or `client_secret` keeps the stored one, so an operator can flip `enabled` or
    /// rotate `scopes` WITHOUT re-pasting a secret they cannot read back. Blank counts as omitted
    /// here: a whitespace-only value does not clear anything.
    ///
    /// The consequence is that there is no way to clear a credential through this route — DELETE
    /// the provider instead.
    ///
    /// A FIRST write still needs both: with no stored record and either missing, the answer is 400.
    /// `enabled` defaults to true on a first write and otherwise keeps its stored value. A present
    /// `scopes` REPLACES the stored list.
    ///
    /// The response is the short form, not the record: `{provider, enabled, configured: true}`,
    /// with no echo of the credentials just written.
    ///
    /// `PUT /api/v1/admin/integration-oauth-providers/{provider}`
    ///
    /// Required scopes: `admin`.
    pub async fn set_admin_integration_o_auth_provider(&self, provider: &models::GetAdminIntegrationOAuthProviderProvider, body: &models::SetAdminIntegrationOAuthProviderRequest) -> Result<models::SetAdminIntegrationOAuthProviderResponse> {
        self.client
            .request_json(Request {
                method: Method::PUT,
                path: format!("/api/v1/admin/integration-oauth-providers/{}", encode_path(&provider.to_string())),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Set platform API key
    ///
    /// `PUT /api/v1/admin/llm-defaults/{providerId}`
    ///
    /// Required scopes: `admin`.
    pub async fn set_admin_llm_default(&self, provider_id: &str, body: &models::SetAdminLLMDefaultRequest) -> Result<models::SetAdminLLMDefaultResponse> {
        self.client
            .request_json(Request {
                method: Method::PUT,
                path: format!("/api/v1/admin/llm-defaults/{}", encode_path(provider_id)),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Set default model config
    ///
    /// `PUT /api/v1/admin/llm-defaults/model-config`
    ///
    /// Required scopes: `admin`.
    pub async fn set_admin_model_config(&self, body: &serde_json::Map<String, serde_json::Value>) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::PUT,
                path: "/api/v1/admin/llm-defaults/model-config".to_string(),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Turn maintenance mode on or off (super-admin)
    ///
    /// Undocumented until now, while the GET beside it was described in full — so a generated
    /// client could READ the maintenance state and had no way to change it. The web has been
    /// calling this all along (`lib/hooks/use-maintenance-mode.ts`).
    ///
    /// Answers the whole record back, the same shape the GET serves, so a client need not re-read
    /// to learn `enabled_at` and `enabled_by_email`.
    ///
    /// WRITE SEMANTICS: replaces. The record is rebuilt from this body and written whole; nothing
    /// is read first. Turning maintenance ON without a `message` DROPS the message a previous ON
    /// had set, and turning it OFF wipes the message unconditionally, keeping only the timestamp
    /// and the actor so the audit trail still shows who closed the window. `enabled_at` and
    /// `enabled_by_email` are stamped on EVERY call, including one that changes nothing.
    ///
    /// `PUT /api/v1/admin/maintenance`
    ///
    /// Required scopes: `admin`.
    pub async fn set_maintenance_state(&self, body: &models::SetMaintenanceStateRequest) -> Result<models::MaintenanceState> {
        self.client
            .request_json(Request {
                method: Method::PUT,
                path: "/api/v1/admin/maintenance".to_string(),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Suspend a tenant
    ///
    /// `PUT /api/v1/admin/tenants/{tenantId}/suspend`
    ///
    /// Required scopes: `admin`.
    pub async fn suspend_tenant(&self, tenant_id: &str, body: &models::SuspendTenantRequest) -> Result<serde_json::Value> {
        self.client
            .request_json(Request {
                method: Method::PUT,
                path: format!("/api/v1/admin/tenants/{}/suspend", encode_path(tenant_id)),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Pull one provider's model list into the catalogue
    ///
    /// Asks the provider what models it offers and MERGES the result into the platform catalogue.
    /// Additive only — nothing is removed, so a model the provider has withdrawn stays in the
    /// catalogue until it is deleted deliberately.
    ///
    /// CUSTOM providers only: the 404 means "no custom provider with that id", so a built-in
    /// provider id is also 404 here. `added` counts new entries, `scanned` is the provider's
    /// reported inventory, and `total` is the catalogue size after the merge — so `added` is zero
    /// on a run that changed nothing, which is the normal result of a second run.
    ///
    /// `POST /api/v1/admin/providers/{providerId}/sync-models`
    ///
    /// Required scopes: `admin`.
    pub async fn sync_provider_models(&self, provider_id: &str) -> Result<models::SyncProviderModelsResponse> {
        self.client
            .request_json(Request {
                method: Method::POST,
                path: format!("/api/v1/admin/providers/{}/sync-models", encode_path(provider_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Update the blog's authoring configuration
    ///
    /// WRITE SEMANTICS: merges. The body is spread over the stored record, so an omitted field
    /// keeps its value.
    ///
    /// One field is not a plain merge: setting `agent_id` also pins `agent_tenant_id` to the
    /// CALLING tenant, because the cron that auto-writes posts runs without a request context and
    /// would otherwise have no tenant to run the agent in. Clearing `agent_id` to `null` nulls
    /// both. `agent_tenant_id` is therefore never sent by a client and never has to be — it is
    /// derived.
    ///
    /// Answers the stored record, so a client sees what took effect.
    ///
    /// `PUT /api/v1/admin/blog/config`
    ///
    /// Required scopes: `admin`.
    pub async fn update_admin_blog_config(&self, body: &models::UpdateAdminBlogConfigRequest) -> Result<models::UpdateAdminBlogConfigResponse> {
        self.client
            .request_json(Request {
                method: Method::PUT,
                path: "/api/v1/admin/blog/config".to_string(),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Edit a post
    ///
    /// WRITE SEMANTICS: merges. An omitted field keeps its value; `tags` present REPLACES the list.
    ///
    /// Three things move on their own and a client should not try to send them. Editing the `title`
    /// or the `body` re-stamps `source` to `manual`, even on a post the agent wrote — the record
    /// then says who last shaped it rather than who started it. Changing the `title` mints a new
    /// unique `slug`, so a published post's URL changes under it. And `published_at` follows
    /// `status`: it is stamped on the first transition to `published` and set back to null on
    /// `draft`, so a republished post carries a NEW timestamp rather than its original one.
    ///
    /// `PATCH /api/v1/admin/blog/posts/{postId}`
    ///
    /// Required scopes: `admin`.
    pub async fn update_admin_blog_post(&self, post_id: &str, body: &models::UpdateAdminBlogPostRequest) -> Result<models::UpdateAdminBlogPostResponse> {
        self.client
            .request_json(Request {
                method: Method::PATCH,
                path: format!("/api/v1/admin/blog/posts/{}", encode_path(post_id)),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Update pricing configuration
    ///
    /// `PUT /api/v1/admin/config/pricing`
    ///
    /// Required scopes: `admin`.
    pub async fn update_admin_pricing(&self, body: &serde_json::Map<String, serde_json::Value>) -> Result<serde_json::Value> {
        self.client
            .request_json(Request {
                method: Method::PUT,
                path: "/api/v1/admin/config/pricing".to_string(),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Update provider
    ///
    /// `PATCH /api/v1/admin/providers/{providerId}`
    ///
    /// Required scopes: `admin`.
    pub async fn update_admin_provider(&self, provider_id: &str, body: &serde_json::Map<String, serde_json::Value>) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::PATCH,
                path: format!("/api/v1/admin/providers/{}", encode_path(provider_id)),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Update tenant settings
    ///
    /// `PATCH /api/v1/admin/tenants/{tenantId}/settings`
    ///
    /// Required scopes: `admin`.
    pub async fn update_admin_tenant_settings(&self, tenant_id: &str, body: &serde_json::Map<String, serde_json::Value>) -> Result<serde_json::Map<String, serde_json::Value>> {
        self.client
            .request_json(Request {
                method: Method::PATCH,
                path: format!("/api/v1/admin/tenants/{}/settings", encode_path(tenant_id)),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Mark one feedback report resolved, or reopen it
    ///
    /// **Only the exact string `resolved` resolves a report; every other value sets it to `new`.**
    /// There is no validation and no error path: `"Resolved"`, `"closed"`, a typo, a missing
    /// `status`, or a body that is not JSON at all are each accepted with 200 and REOPEN a resolved
    /// report. A client must send the literal value and must not rely on being told when it did
    /// not.
    ///
    /// WRITE SEMANTICS: replaces the status field only; nothing else on the report is touched.
    ///
    /// `PATCH /api/v1/admin/feedback/{reportId}`
    ///
    /// Required scopes: `admin`.
    pub async fn update_feedback_report_status(&self, report_id: &str, body: &models::UpdateFeedbackReportStatusRequest) -> Result<models::UpdateFeedbackReportStatusResponse> {
        self.client
            .request_json(Request {
                method: Method::PATCH,
                path: format!("/api/v1/admin/feedback/{}", encode_path(report_id)),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Set or clear one tenant's mission-framework overrides
    ///
    /// WRITE SEMANTICS: merges, per key. Only the four known keys are read; a key the body omits
    /// keeps its stored value, and a key sent as `null` CLEARS that override so the flag falls back
    /// to the platform default. Unknown keys are ignored silently rather than rejected.
    ///
    /// A non-boolean, non-null value for a known key is **422**. Note the title on that one is
    /// `ValidationError` without a space, unlike the `Validation Error` used elsewhere on this
    /// surface.
    ///
    /// When clearing the last override leaves nothing set, the whole `mef_config` is dropped rather
    /// than stored as `{}` — a later read answers `null`. And every accepted request writes: even a
    /// body that changes nothing stamps a new `updated_at` on the tenant record.
    ///
    /// Answers the same body as the GET, so a client sees both the stored overrides and what they
    /// now resolve to.
    ///
    /// `PATCH /api/v1/admin/tenants/{tenantId}/mef-config`
    ///
    /// Required scopes: `admin`.
    pub async fn update_tenant_mef_config(&self, tenant_id: &str, body: &models::UpdateTenantMefConfigRequest) -> Result<models::TenantMefConfigResponse> {
        self.client
            .request_json(Request {
                method: Method::PATCH,
                path: format!("/api/v1/admin/tenants/{}/mef-config", encode_path(tenant_id)),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Set a tenant's plan (super-admin)
    ///
    /// The operator's manual plan grant, undocumented while GET, PATCH and DELETE on this same path
    /// were described. It is not a general tenant update: `plan` is required and it is what the
    /// route is for.
    ///
    /// WRITE SEMANTICS: mixed. The write is a compare-and-set MERGE onto the current record, so
    /// `name`, `slug` and `quota_overrides` keep their stored values when omitted. `plan` and
    /// `quotas` do not: both are written on every call, and omitting `quotas` REPLACES the tenant
    /// quotas with the resolved plan defaults rather than leaving them alone. A caller raising one
    /// dimension must send `quota_overrides`, not `quotas`.
    ///
    /// `quotas` REPLACES the plan's quotas for this tenant. `quota_overrides` is the durable one —
    /// a partial grant that survives a later Stripe subscription change, where a plain `quotas`
    /// write does not. When a tenant's plan keeps reverting, the override is the field that makes
    /// it stick, and it must be set BEFORE the plan is put back, not after.
    ///
    /// Granting any paid plan clears a stale `billing_status` (a "cancelled" left over from an
    /// earlier Stripe cancellation becomes "active"); a downgrade to free leaves the status alone,
    /// since free is never gated.
    ///
    /// `PUT /api/v1/admin/tenants/{tenantId}`
    ///
    /// Required scopes: `admin`.
    pub async fn update_tenant_plan(&self, tenant_id: &str, body: &models::UpdateTenantPlanRequest) -> Result<models::UpdateTenantPlanResponse> {
        self.client
            .request_json(Request {
                method: Method::PUT,
                path: format!("/api/v1/admin/tenants/{}", encode_path(tenant_id)),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }
}
