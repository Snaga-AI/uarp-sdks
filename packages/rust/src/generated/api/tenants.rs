// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.
//!
//! Tenant management and API keys

#![allow(unused_imports, clippy::too_many_arguments)]

use reqwest::Method;
use serde::{Deserialize, Serialize};

use crate::client::{Client, Request, NO_BODY, NO_QUERY};
use crate::error::Result;
use crate::generated::models;
use crate::multipart::{field_text, FilePart};
use crate::util::encode_path;

/// Tenant management and API keys
#[derive(Debug, Clone)]
pub struct TenantsApi {
    pub(crate) client: Client,
}

impl Client {
    /// Tenant management and API keys
    pub fn tenants(&self) -> TenantsApi {
        TenantsApi { client: self.clone() }
    }
}

impl TenantsApi {
    /// Accept a pending invite from the tenant picker
    ///
    /// The tenant is in the PATH, and that is the whole reason this route exists beside `POST
    /// /api/v1/users/invites/{inviteId}/accept`. The older route resolves the invite against the
    /// caller's active tenant, which cannot work here: the caller is not a member of the inviting
    /// tenant yet — making them one is what the call is for. Without a tenant-in-path route the
    /// picker's Accept button can never succeed for a cross-tenant invite.
    ///
    /// Semantics are otherwise identical to the older route: the same atomic create-user + email
    /// index + membership index + `invite.status = accepted` write.
    ///
    /// Four refusals, and they are different questions: **403** the invite is addressed to another
    /// email, or the `token` does not match; **409** the invite is not `pending` (already accepted,
    /// revoked, declined) or the email already belongs to a member; **410** the invite has expired;
    /// **404** no such invite, or the caller has no user record.
    ///
    /// `POST /api/v1/me/invites/{tenantId}/{inviteId}/accept`
    pub async fn accept_invite_from_picker(&self, tenant_id: &str, invite_id: &str, body: &models::AcceptInviteFromPickerRequest) -> Result<models::AcceptInviteFromPickerResponse> {
        self.client
            .request_json(Request {
                method: Method::POST,
                path: format!("/api/v1/me/invites/{}/{}/accept", encode_path(tenant_id), encode_path(invite_id)),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Create a new API key
    ///
    /// `POST /api/v1/tenants/me/keys`
    ///
    /// Required scopes: `api_keys:write`.
    pub async fn create_api_key(&self, body: &models::CreateAPIKeyRequest) -> Result<models::APIKeyResponse> {
        self.client
            .request_json(Request {
                method: Method::POST,
                path: "/api/v1/tenants/me/keys".to_string(),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Create a new tenant with the caller as owner
    ///
    /// Provisions a brand-new tenant on the `free` plan (or `enterprise` for super-admin). The
    /// caller becomes the founding owner. Onboarding wizard auto-runs on /browser to provision the
    /// Head Agent.
    ///
    /// `POST /api/v1/me/tenants/create`
    pub async fn create_my_tenant(&self, body: &models::CreateMyTenantRequest) -> Result<models::CreateMyTenantResponse> {
        self.client
            .request_json(Request {
                method: Method::POST,
                path: "/api/v1/me/tenants/create".to_string(),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Decline a pending invite
    ///
    /// Deliberately distinct from an admin's revoke, so the audit trail and the Members page can
    /// tell "the invitee said no" from "an admin pulled it". The caller's email must match the
    /// invite's — without that check anyone with a current session could decline someone else's
    /// invites.
    ///
    /// No token is required here, unlike accept: declining grants nothing.
    ///
    /// `POST /api/v1/me/invites/{tenantId}/{inviteId}/decline`
    pub async fn decline_invite_from_picker(&self, tenant_id: &str, invite_id: &str) -> Result<models::DeclineInviteFromPickerResponse> {
        self.client
            .request_json(Request {
                method: Method::POST,
                path: format!("/api/v1/me/invites/{}/{}/decline", encode_path(tenant_id), encode_path(invite_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Get current tenant
    ///
    /// Returns the calling tenant's profile. Any authenticated key passes — no scope required.
    ///
    /// `GET /api/v1/tenants/me`
    ///
    /// Required scopes: `api_keys:read`.
    pub async fn get_current_tenant(&self) -> Result<models::Tenant> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/tenants/me".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Tiered Head Agent template for the onboarding wizard
    ///
    /// Returns Basic/Standard/Full tier metadata for the current tenant under the Variant-A
    /// SPEC-driven contract. Each tier names the `@platform/*` SPECs the wizard installs (via
    /// `install_specs`) plus an `auto_approve_tools` HITL list. Availability is a plan-rank check
    /// (`required_plan`); locked tiers ship empty `install_specs`.
    ///
    /// `GET /api/v1/me/head-agent-template`
    pub async fn get_my_head_agent_template(&self) -> Result<models::GetMyHeadAgentTemplateResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/me/head-agent-template".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// DNS and certificate state for this tenant's custom domain
    ///
    /// **Two different 404s, and a client should tell them apart:** no such tenant, and a tenant
    /// with no custom domain configured. The second is the ordinary state of most tenants and is
    /// not an error condition — a UI that renders both as a failure will report a fault to every
    /// customer who has not set up a vanity domain.
    ///
    /// Records written before the lifecycle schema are lifted on read, so `dns` and `cert` are
    /// present here even for a domain added under the old flat fields.
    ///
    /// `GET /api/v1/tenants/me/domain/health`
    ///
    /// Required scopes: `api_keys:read`.
    pub async fn get_tenant_domain_health(&self) -> Result<models::GetTenantDomainHealthResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/tenants/me/domain/health".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Leave a tenant
    ///
    /// Removes the caller's own membership. The user-record cascade matches an admin-driven
    /// removal.
    ///
    /// Two refusals, both **409**, and both name a specific thing to do first. The caller is the
    /// only ACTIVE owner: transfer ownership before leaving. Or the caller is the only veto-holding
    /// ambassador: rotate the founder ambassador via `/api/v1/governance/ambassadors` first. The
    /// second check is skipped entirely when governance is not enabled, so its absence is not a
    /// promise that no such constraint exists.
    ///
    /// `DELETE /api/v1/me/memberships/{tenantId}`
    pub async fn leave_tenant(&self, tenant_id: &str) -> Result<models::LeaveTenantResponse> {
        self.client
            .request_json(Request {
                method: Method::DELETE,
                path: format!("/api/v1/me/memberships/{}", encode_path(tenant_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// List API keys
    ///
    /// `GET /api/v1/tenants/me/keys`
    ///
    /// Required scopes: `api_keys:read`.
    pub async fn list_api_keys(&self) -> Result<models::ListAPIKeysResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/tenants/me/keys".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// List the caller's tenant memberships + pending invites
    ///
    /// Returns every tenant the calling user is a member of, plus any pending invites addressed to
    /// their email. Used by the Netflix-style picker (/browser/tenants).
    ///
    /// `GET /api/v1/me/tenants`
    pub async fn list_my_tenants(&self) -> Result<models::ListMyTenantsResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/api/v1/me/tenants".to_string(),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Find which tenant owns the given agent across caller's memberships
    ///
    /// Used by the chat layout fallback when the URL points at an agent under a different active
    /// tenant. Returns the owning tenant_id in one round-trip; the alternative was N client-side
    /// probes that surfaced expected 404s in the browser console.
    ///
    /// `GET /api/v1/me/agents/{agentId}/locate`
    pub async fn locate_my_agent(&self, agent_id: &str) -> Result<models::LocateMyAgentResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: format!("/api/v1/me/agents/{}/locate", encode_path(agent_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Partial update tenant
    ///
    /// Partial update of tenant settings. Requires the `tenants:write` scope and role `owner`.
    ///
    /// `PATCH /api/v1/tenants/me`
    ///
    /// Required scopes: `tenants:write`.
    pub async fn patch(&self, body: &serde_json::Map<String, serde_json::Value>) -> Result<models::Tenant> {
        self.client
            .request_json(Request {
                method: Method::PATCH,
                path: "/api/v1/tenants/me".to_string(),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Revoke an API key
    ///
    /// Changes the key's state to `revoked` and keeps the record for the listing and the audit
    /// trail (`api_key.revoked`); the key stops authenticating immediately. Only the key's owner
    /// can revoke it — another member's key answers 404. Not reversible: mint a new key instead.
    ///
    /// `DELETE /api/v1/tenants/me/keys/{keyId}`
    ///
    /// Required scopes: `api_keys:write`.
    pub async fn revoke_api_key(&self, key_id: &str) -> Result<models::RevokeAPIKeyResponse> {
        self.client
            .request_json(Request {
                method: Method::DELETE,
                path: format!("/api/v1/tenants/me/keys/{}", encode_path(key_id)),
                query: NO_QUERY,
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Update current tenant settings
    ///
    /// `PUT /api/v1/tenants/me`
    ///
    /// Required scopes: `tenants:write`.
    pub async fn update(&self, body: &models::UpdateTenantRequest) -> Result<models::Tenant> {
        self.client
            .request_json(Request {
                method: Method::PUT,
                path: "/api/v1/tenants/me".to_string(),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }

    /// Verify domain
    ///
    /// `POST /api/v1/tenants/me/domain/verify`
    ///
    /// Required scopes: `tenants:write`.
    pub async fn verify_tenant_domain(&self, body: &serde_json::Map<String, serde_json::Value>) -> Result<models::VerifyTenantDomainResponse> {
        self.client
            .request_json(Request {
                method: Method::POST,
                path: "/api/v1/tenants/me/domain/verify".to_string(),
                query: NO_QUERY,
                body: Some(body),
                headers: Vec::new(),
                idempotent: true,
            })
            .await
    }
}
