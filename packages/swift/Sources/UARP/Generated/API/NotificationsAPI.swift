// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

import Foundation

/// User notifications
public struct NotificationsAPI: Sendable {
    let client: UARPClient

    init(client: UARPClient) { self.client = client }

    /// Bulk-purge notifications
    ///
    /// Bulk delete. Optional `?scope=read` keeps unread items so the operator can't accidentally
    /// wipe pending approvals; default wipes everything.
    ///
    /// `DELETE /api/v1/notifications`
    ///
    /// Required scopes: `notifications:write`.
    public func bulkDeleteNotifications(scope: BulkDeleteNotificationsScope? = nil, options: RequestOptions = .init()) async throws -> BulkDeleteNotificationsResponse {
        var query: [URLQueryItem] = []
        if let scope {
            query.append(URLQueryItem(name: "scope", value: scope.rawValue))
        }
        return try await client.send(RequestSpec(
            method: "DELETE",
            path: "/api/v1/notifications",
            query: query,
            idempotent: true,
            options: options
        ))
    }

    /// Purge a single notification
    ///
    /// `DELETE /api/v1/notifications/{notifId}`
    ///
    /// Required scopes: `notifications:write`.
    public func delete(notifId: String, options: RequestOptions = .init()) async throws -> DeleteNotificationResponse {
        return try await client.send(RequestSpec(
            method: "DELETE",
            path: "/api/v1/notifications/\(encodePathSegment(notifId))",
            idempotent: true,
            options: options
        ))
    }

    /// Remove a target
    ///
    /// `DELETE /api/v1/notifications/targets/{targetId}`
    ///
    /// Required scopes: `notifications:write`.
    public func deleteNotificationTarget(targetId: String, options: RequestOptions = .init()) async throws -> DeleteNotificationTargetResponse {
        return try await client.send(RequestSpec(
            method: "DELETE",
            path: "/api/v1/notifications/targets/\(encodePathSegment(targetId))",
            idempotent: true,
            options: options
        ))
    }

    /// Get unread notification count
    ///
    /// `GET /api/v1/notifications/unread`
    ///
    /// Required scopes: `notifications:read`.
    public func getUnreadCount(options: RequestOptions = .init()) async throws -> GetUnreadCountResponse {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/notifications/unread",
            options: options
        ))
    }

    /// List notifications
    ///
    /// `GET /api/v1/notifications`
    ///
    /// Required scopes: `notifications:read`.
    public func list(limit: Int? = nil, unread: Bool? = nil, options: RequestOptions = .init()) async throws -> ListNotificationsResponse {
        var query: [URLQueryItem] = []
        if let limit {
            query.append(URLQueryItem(name: "limit", value: String(limit)))
        }
        if let unread {
            query.append(URLQueryItem(name: "unread", value: String(unread)))
        }
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/notifications",
            query: query,
            options: options
        ))
    }

    /// List notification targets
    ///
    /// Every configured destination, with secrets redacted — see `NotificationTarget`.
    ///
    /// `GET /api/v1/notifications/targets`
    ///
    /// Required scopes: `notifications:read`.
    public func listNotificationTargets(options: RequestOptions = .init()) async throws -> ListNotificationTargetsResponse {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/notifications/targets",
            options: options
        ))
    }

    /// Mark all notifications as read
    ///
    /// `PUT /api/v1/notifications/read-all`
    ///
    /// Required scopes: `notifications:write`.
    public func markAllNotificationsRead(options: RequestOptions = .init()) async throws -> MarkAllNotificationsReadResponse {
        return try await client.send(RequestSpec(
            method: "PUT",
            path: "/api/v1/notifications/read-all",
            idempotent: true,
            options: options
        ))
    }

    /// Mark notification as read
    ///
    /// `PUT /api/v1/notifications/{notifId}/read`
    ///
    /// Required scopes: `notifications:write`.
    public func markNotificationRead(notifId: String, options: RequestOptions = .init()) async throws -> JSONObject {
        return try await client.send(RequestSpec(
            method: "PUT",
            path: "/api/v1/notifications/\(encodePathSegment(notifId))/read",
            idempotent: true,
            options: options
        ))
    }

    /// Open SSE stream of notifications for the tenant
    ///
    /// Server-Sent Events push channel. Replaces the 15-second polling loop: every new notification
    /// created in the store for the calling tenant is fanned out to all open streams within
    /// milliseconds. The first event is `event: ready` so the client knows the stream is live;
    /// comment lines every 20 s keep proxies from killing idle connections.
    ///
    /// `GET /api/v1/notifications/stream`
    ///
    /// Required scopes: `notifications:read`.
    ///
    /// Returns a server-sent event stream; iterate it with `for try await`.
    public func stream(token: String? = nil, lastEventId: String? = nil, options: RequestOptions = .init()) -> EventStream {
        var query: [URLQueryItem] = []
        if let token {
            query.append(URLQueryItem(name: "token", value: token))
        }
        var headers: [String: String] = [:]
        if let lastEventId {
            headers["Last-Event-ID"] = lastEventId
        }
        return client.sendStream(RequestSpec(
            method: "GET",
            path: "/api/v1/notifications/stream",
            query: query,
            headers: headers,
            options: options
        ))
    }

    /// Send a test notification
    ///
    /// Queues one notification through the real fan-out, so it proves the whole path rather than
    /// the stored configuration. **200 means queued, not delivered** — read `last_delivered_at` and
    /// `last_error` on the target afterwards for the outcome.
    ///
    /// `POST /api/v1/notifications/targets/{targetId}/test`
    ///
    /// Required scopes: `notifications:write`.
    public func testNotificationTarget(targetId: String, options: RequestOptions = .init()) async throws -> TestNotificationTargetResponse {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/notifications/targets/\(encodePathSegment(targetId))/test",
            idempotent: true,
            options: options
        ))
    }

    /// Create or update a target
    ///
    /// Upsert, not insert: sending an `id` rewrites that target. A device target (push or web push)
    /// additionally reuses the id of an existing entry for the same device, so a client that
    /// re-registers on every launch does not accumulate duplicates.
    ///
    /// The answer is the REDACTED target — the signing secret or device token you just sent is not
    /// echoed back.
    ///
    /// `POST /api/v1/notifications/targets`
    ///
    /// Required scopes: `notifications:write`.
    public func upsertNotificationTarget(body: UpsertNotificationTargetRequest, options: RequestOptions = .init()) async throws -> NotificationTarget {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/notifications/targets",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }
}

extension UARPClient {
    /// User notifications
    public var notifications: NotificationsAPI { NotificationsAPI(client: self) }
}
