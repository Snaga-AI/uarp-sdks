// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

import Foundation

/// Operations tagged `Creativity`.
public struct CreativityAPI: Sendable {
    let client: UARPClient

    init(client: UARPClient) { self.client = client }

    /// Append one event to the session log
    ///
    /// `POST /api/v1/creativity/sessions/{sessionId}/events`
    public func appendCreativityEvent(sessionId: String, body: AppendCreativityEventRequest, options: RequestOptions = .init()) async throws -> AppendCreativityEventResponse {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/creativity/sessions/\(encodePathSegment(sessionId))/events",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Archive a creativity session
    ///
    /// `DELETE /api/v1/creativity/sessions/{sessionId}`
    public func archiveCreativitySession(sessionId: String, options: RequestOptions = .init()) async throws {
        try await client.sendVoid(RequestSpec(
            method: "DELETE",
            path: "/api/v1/creativity/sessions/\(encodePathSegment(sessionId))",
            idempotent: true,
            options: options
        ))
    }

    /// Open (or reuse) the caller's creativity session for a mode
    ///
    /// `POST /api/v1/creativity/sessions`
    public func createCreativitySession(body: CreateCreativitySessionRequest, options: RequestOptions = .init()) async throws -> CreateCreativitySessionResponse {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/creativity/sessions",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Remove one event from the log (the eraser)
    ///
    /// Splices the event out of the log and does NOT modify scene_state, so layer and snap
    /// configuration survive.
    ///
    /// `DELETE /api/v1/creativity/sessions/{sessionId}/events/{eventId}`
    public func eraseCreativityEvent(sessionId: String, eventId: String, options: RequestOptions = .init()) async throws {
        try await client.sendVoid(RequestSpec(
            method: "DELETE",
            path: "/api/v1/creativity/sessions/\(encodePathSegment(sessionId))/events/\(encodePathSegment(eventId))",
            idempotent: true,
            options: options
        ))
    }

    /// Fetch one creativity session
    ///
    /// `GET /api/v1/creativity/sessions/{sessionId}`
    public func getCreativitySession(sessionId: String, options: RequestOptions = .init()) async throws -> JSONObject {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/creativity/sessions/\(encodePathSegment(sessionId))",
            options: options
        ))
    }

    /// Events since a sequence number, for resume and catch-up
    ///
    /// `GET /api/v1/creativity/sessions/{sessionId}/events`
    ///
    /// Required scopes: `events:read`.
    public func listCreativityEvents(sessionId: String, sinceSeq: Int? = nil, options: RequestOptions = .init()) async throws -> ListCreativityEventsResponse {
        var query: [URLQueryItem] = []
        if let sinceSeq {
            query.append(URLQueryItem(name: "since_seq", value: String(sinceSeq)))
        }
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/creativity/sessions/\(encodePathSegment(sessionId))/events",
            query: query,
            options: options
        ))
    }

    /// List the caller's creativity sessions
    ///
    /// `GET /api/v1/creativity/sessions`
    public func listCreativitySessions(options: RequestOptions = .init()) async throws -> ListCreativitySessionsResponse {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/creativity/sessions",
            options: options
        ))
    }

    /// Replace one event's payload
    ///
    /// `PATCH /api/v1/creativity/sessions/{sessionId}/events/{eventId}`
    public func replaceCreativityEventPayload(sessionId: String, eventId: String, body: ReplaceCreativityEventPayloadRequest, options: RequestOptions = .init()) async throws {
        try await client.sendVoid(RequestSpec(
            method: "PATCH",
            path: "/api/v1/creativity/sessions/\(encodePathSegment(sessionId))/events/\(encodePathSegment(eventId))",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Update mutable scene state (width, height, snap grid)
    ///
    /// `PATCH /api/v1/creativity/sessions/{sessionId}`
    public func updateCreativityScene(sessionId: String, body: UpdateCreativitySceneRequest, options: RequestOptions = .init()) async throws -> UpdateCreativitySceneResponse {
        return try await client.send(RequestSpec(
            method: "PATCH",
            path: "/api/v1/creativity/sessions/\(encodePathSegment(sessionId))",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }
}

extension UARPClient {
    /// Operations tagged `Creativity`.
    public var creativity: CreativityAPI { CreativityAPI(client: self) }
}
