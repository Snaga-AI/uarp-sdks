// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

import Foundation

/// Agent memory management
public struct MemoryAPI: Sendable {
    let client: UARPClient

    init(client: UARPClient) { self.client = client }

    /// Delete memory entry
    ///
    /// `DELETE /api/v1/agents/{agentId}/memory/{entryId}`
    ///
    /// Required scopes: `memory:write`.
    public func deleteMemoryEntry(agentId: String, entryId: String, options: RequestOptions = .init()) async throws {
        try await client.sendVoid(RequestSpec(
            method: "DELETE",
            path: "/api/v1/agents/\(encodePathSegment(agentId))/memory/\(encodePathSegment(entryId))",
            idempotent: true,
            options: options
        ))
    }

    /// Get a core memory block
    ///
    /// `GET /api/v1/agents/{agentId}/memory/core/{label}`
    ///
    /// Required scopes: `memory:read`.
    public func getCoreMemoryBlock(agentId: String, label: String, options: RequestOptions = .init()) async throws -> CoreMemoryBlock {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/agents/\(encodePathSegment(agentId))/memory/core/\(encodePathSegment(label))",
            options: options
        ))
    }

    /// Get memory entries associated with an entity
    ///
    /// `GET /api/v1/agents/{agentId}/memory/entities/{entityId}`
    ///
    /// Required scopes: `memory:read`.
    public func getMemoriesByEntity(agentId: String, entityId: String, options: RequestOptions = .init()) async throws -> GetMemoriesByEntityResponse {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/agents/\(encodePathSegment(agentId))/memory/entities/\(encodePathSegment(entityId))",
            options: options
        ))
    }

    /// Get memory entry
    ///
    /// `GET /api/v1/agents/{agentId}/memory/{entryId}`
    ///
    /// Required scopes: `memory:read`.
    public func getMemoryEntry(agentId: String, entryId: String, options: RequestOptions = .init()) async throws -> MemoryEntry {
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/agents/\(encodePathSegment(agentId))/memory/\(encodePathSegment(entryId))",
            options: options
        ))
    }

    /// Put back what an export took out
    ///
    /// Accepts the `memory.json` an account export writes: a flat `entries` array, or
    /// `agents[].entries` (every agent's entries are imported into THIS agent). Each entry is
    /// tagged `imported`. Dedup is the store's: re-importing the same file returns the existing
    /// entries as `duplicates` instead of doubling them. A file with no entries is 400.
    ///
    /// `POST /api/v1/agents/{agentId}/memory/import`
    ///
    /// Required scopes: `memory:write`.
    public func importAgentMemory(agentId: String, body: ImportAgentMemoryRequest, options: RequestOptions = .init()) async throws -> ImportAgentMemoryResponse {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/agents/\(encodePathSegment(agentId))/memory/import",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Ingest a file into agent memory
    ///
    /// Chunks the file content (default 600 chars), embeds each chunk, and stores embeddings as
    /// memory entries. Source: pre-uploaded `file_id` (preferred) or inline `content`. Max 2 MB of
    /// text per ingest call; rate-limited and quota-counted.
    ///
    /// `POST /api/v1/agents/{agentId}/memory/ingest`
    ///
    /// Required scopes: `memory:write`.
    public func ingestMemory(agentId: String, body: IngestMemoryRequest, options: RequestOptions = .init()) async throws -> IngestMemoryResponse {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/agents/\(encodePathSegment(agentId))/memory/ingest",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// List recent memories for an agent
    ///
    /// `GET /api/v1/agents/{agentId}/memory`
    ///
    /// Required scopes: `memory:read`.
    public func listMemories(agentId: String, limit: Int? = nil, options: RequestOptions = .init()) async throws -> ListMemoriesResponse {
        var query: [URLQueryItem] = []
        if let limit {
            query.append(URLQueryItem(name: "limit", value: String(limit)))
        }
        return try await client.send(RequestSpec(
            method: "GET",
            path: "/api/v1/agents/\(encodePathSegment(agentId))/memory",
            query: query,
            options: options
        ))
    }

    /// Search agent memories
    ///
    /// `POST /api/v1/agents/{agentId}/memory/search`
    ///
    /// Required scopes: `memory:write`.
    public func search(agentId: String, body: JSONValue, options: RequestOptions = .init()) async throws -> SearchMemoryResponse {
        return try await client.send(RequestSpec(
            method: "POST",
            path: "/api/v1/agents/\(encodePathSegment(agentId))/memory/search",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Update memory entry
    ///
    /// `PUT /api/v1/agents/{agentId}/memory/{entryId}`
    ///
    /// Required scopes: `memory:write`.
    public func updateAgentMemoryEntry(agentId: String, entryId: String, body: JSONObject, options: RequestOptions = .init()) async throws -> MemoryEntry {
        return try await client.send(RequestSpec(
            method: "PUT",
            path: "/api/v1/agents/\(encodePathSegment(agentId))/memory/\(encodePathSegment(entryId))",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }

    /// Update a core memory block
    ///
    /// `PUT /api/v1/agents/{agentId}/memory/core/{label}`
    ///
    /// Required scopes: `memory:write`.
    public func updateCoreMemoryBlock(agentId: String, label: String, body: UpdateCoreMemoryBlockRequest, options: RequestOptions = .init()) async throws -> CoreMemoryBlock {
        return try await client.send(RequestSpec(
            method: "PUT",
            path: "/api/v1/agents/\(encodePathSegment(agentId))/memory/core/\(encodePathSegment(label))",
            body: try client.encode(body),
            idempotent: true,
            options: options
        ))
    }
}

extension UARPClient {
    /// Agent memory management
    public var memory: MemoryAPI { MemoryAPI(client: self) }
}
