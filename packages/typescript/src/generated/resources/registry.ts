// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

import { APIResource } from '../../core/resource.js';
import type { RequestOptions } from '../../core/transport.js';
import { pick } from '../../core/util.js';
import type {
  RegistryAdminListSpecsResponse,
  RegistryGetFileResponse,
  RegistryGetReadmeResponse,
  RegistryGetShareResponse,
  RegistryGetSparseIndexResponse,
  RegistryGetSpecMetadataResponse,
  RegistryGetSpecVersionResponse,
  RegistryListFilesResponse,
  RegistryPublishRequest,
  RegistryPublishResponse,
  RegistrySearchResponse,
  RegistrySetShareRequest,
  RegistrySetShareResponse,
  RegistrySpecFeatureState,
  RegistryYankVersionRequest,
  SeedStarterSpecsResponse,
  SetRegistrySpecVisibilityRequest,
  SetRegistrySpecVisibilityResponse,
} from '../models.js';

/**
 * Query and header parameters for `registryAdminListSpecs`.
 */
export interface RegistryAdminListSpecsParams {
  limit?: number;
  cursor?: string;
}

/**
 * Query and header parameters for `registryGetFile`.
 */
export interface RegistryGetFileParams {
  /**
   * Relative path within the artifact.
   */
  path: string;
}

/**
 * Query and header parameters for `registrySearch`.
 */
export interface RegistrySearchParams {
  q?: string;
  scope?: string;
  category?: string;
  keyword?: string;
  limit?: number;
  cursor?: string;
}

/**
 * Spec registry — publish, search, yank, share spec artifacts
 */
export class RegistryResource extends APIResource {
  /**
   * Add a SPEC to the featured set (super-admin)
   *
   * The SPEC's existence is confirmed BEFORE the featured set is touched, so a typo cannot leave
   * a dangling id in a shared list — that is what the 404 protects. The set itself is mutated
   * under a compare-and-set retry, because it is one shared record and a plain read-modify-write
   * was last-writer-wins.
   *
   * Idempotent: featuring an already-featured SPEC is 200, not a conflict.
   *
   * `POST /api/v1/registry/admin/specs/{scope}/{name}/feature`
   */
  featureRegistrySpec(scope: string, name: string, options?: RequestOptions): Promise<RegistrySpecFeatureState> {
    return this._client.request({
      method: 'POST',
      path: `/api/v1/registry/admin/specs/${encodeURIComponent(String(scope))}/${encodeURIComponent(String(name))}/feature`,
      idempotent: true,
      options,
    });
  }

  /**
   * Admin: list all specs (regardless of visibility)
   *
   * `GET /api/v1/registry/admin/specs`
   */
  registryAdminListSpecs(params?: RegistryAdminListSpecsParams, options?: RequestOptions): Promise<RegistryAdminListSpecsResponse> {
    return this._client.request({
      method: 'GET',
      path: '/api/v1/registry/admin/specs',
      query: pick(params, ['limit', 'cursor']),
      options,
    });
  }

  /**
   * Download tarball/artifact bundle for a spec version
   *
   * `GET /api/v1/registry/spec/{scope}/{name}/{version}/artifact`
   */
  registryGetArtifact(scope: string, name: string, version: string, options?: RequestOptions): Promise<Blob> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/registry/spec/${encodeURIComponent(String(scope))}/${encodeURIComponent(String(name))}/${encodeURIComponent(String(version))}/artifact`,
      responseType: 'binary',
      options,
    });
  }

  /**
   * Get a single file from inside a spec version artifact
   *
   * `GET /api/v1/registry/spec/{scope}/{name}/{version}/file`
   */
  registryGetFile(scope: string, name: string, version: string, params: RegistryGetFileParams, options?: RequestOptions): Promise<RegistryGetFileResponse> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/registry/spec/${encodeURIComponent(String(scope))}/${encodeURIComponent(String(name))}/${encodeURIComponent(String(version))}/file`,
      query: pick(params, ['path']),
      options,
    });
  }

  /**
   * Get README for a spec version
   *
   * `GET /api/v1/registry/spec/{scope}/{name}/{version}/readme`
   */
  registryGetReadme(scope: string, name: string, version: string, options?: RequestOptions): Promise<RegistryGetReadmeResponse> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/registry/spec/${encodeURIComponent(String(scope))}/${encodeURIComponent(String(name))}/${encodeURIComponent(String(version))}/readme`,
      options,
    });
  }

  /**
   * Get share/visibility settings for a spec
   *
   * `GET /api/v1/registry/spec/{scope}/{name}/share`
   */
  registryGetShare(scope: string, name: string, options?: RequestOptions): Promise<RegistryGetShareResponse> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/registry/spec/${encodeURIComponent(String(scope))}/${encodeURIComponent(String(name))}/share`,
      options,
    });
  }

  /**
   * Get sparse-index entry for a spec
   *
   * `GET /api/v1/registry/index/{shard}/{scope}/{name}`
   */
  registryGetSparseIndex(shard: string, scope: string, name: string, options?: RequestOptions): Promise<RegistryGetSparseIndexResponse> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/registry/index/${encodeURIComponent(String(shard))}/${encodeURIComponent(String(scope))}/${encodeURIComponent(String(name))}`,
      options,
    });
  }

  /**
   * Get spec metadata (latest version pointers, owners)
   *
   * `GET /api/v1/registry/spec/{scope}/{name}`
   */
  registryGetSpecMetadata(scope: string, name: string, options?: RequestOptions): Promise<RegistryGetSpecMetadataResponse> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/registry/spec/${encodeURIComponent(String(scope))}/${encodeURIComponent(String(name))}`,
      options,
    });
  }

  /**
   * Get a specific version of a spec
   *
   * Returns the full PublishedSpec record plus a `download_url` pointing at the artifact
   * endpoint.
   *
   * `GET /api/v1/registry/spec/{scope}/{name}/{version}`
   */
  registryGetSpecVersion(scope: string, name: string, version: string, options?: RequestOptions): Promise<RegistryGetSpecVersionResponse> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/registry/spec/${encodeURIComponent(String(scope))}/${encodeURIComponent(String(name))}/${encodeURIComponent(String(version))}`,
      options,
    });
  }

  /**
   * List files inside a spec version artifact
   *
   * `GET /api/v1/registry/spec/{scope}/{name}/{version}/files`
   */
  registryListFiles(scope: string, name: string, version: string, options?: RequestOptions): Promise<RegistryListFilesResponse> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/registry/spec/${encodeURIComponent(String(scope))}/${encodeURIComponent(String(name))}/${encodeURIComponent(String(version))}/files`,
      options,
    });
  }

  /**
   * Publish a new spec version to the registry
   *
   * Multipart upload of a tarball + manifest. Server validates manifest, verifies optional
   * sha256, and persists the artifact. Returns the full PublishedSpec record. `Location` header
   * points at the version's metadata endpoint.
   *
   * `POST /api/v1/registry/publish`
   */
  registryPublish(body: RegistryPublishRequest, options?: RequestOptions): Promise<RegistryPublishResponse> {
    return this._client.request({
      method: 'POST',
      path: '/api/v1/registry/publish',
      multipart: body,
      idempotent: true,
      options,
    });
  }

  /**
   * Search the registry for specs
   *
   * `GET /api/v1/registry/search`
   */
  registrySearch(params?: RegistrySearchParams, options?: RequestOptions): Promise<RegistrySearchResponse> {
    return this._client.request({
      method: 'GET',
      path: '/api/v1/registry/search',
      query: pick(params, ['q', 'scope', 'category', 'keyword', 'limit', 'cursor']),
      options,
    });
  }

  /**
   * Set share/visibility settings for a spec
   *
   * `POST /api/v1/registry/spec/{scope}/{name}/share`
   */
  registrySetShare(scope: string, name: string, body: RegistrySetShareRequest, options?: RequestOptions): Promise<RegistrySetShareResponse> {
    return this._client.request({
      method: 'POST',
      path: `/api/v1/registry/spec/${encodeURIComponent(String(scope))}/${encodeURIComponent(String(name))}/share`,
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * Reverse a yank on a published spec version
   *
   * `POST /api/v1/registry/spec/{scope}/{name}/{version}/unyank`
   */
  registryUnyankVersion(scope: string, name: string, version: string, options?: RequestOptions): Promise<void> {
    return this._client.request({
      method: 'POST',
      path: `/api/v1/registry/spec/${encodeURIComponent(String(scope))}/${encodeURIComponent(String(name))}/${encodeURIComponent(String(version))}/unyank`,
      idempotent: true,
      responseType: 'void',
      options,
    });
  }

  /**
   * Yank (mark unsafe) a published spec version
   *
   * `POST /api/v1/registry/spec/{scope}/{name}/{version}/yank`
   */
  registryYankVersion(scope: string, name: string, version: string, body?: RegistryYankVersionRequest, options?: RequestOptions): Promise<void> {
    return this._client.request({
      method: 'POST',
      path: `/api/v1/registry/spec/${encodeURIComponent(String(scope))}/${encodeURIComponent(String(name))}/${encodeURIComponent(String(version))}/yank`,
      body,
      idempotent: true,
      responseType: 'void',
      options,
    });
  }

  /**
   * Bulk-publish the bundled starter SPECs (super-admin)
   *
   * Idempotent: a starter SPEC already present is counted in `skipped`, not republished, so
   * re-running is safe and is how a partially-failed seed is completed.
   *
   * `errors` is present ONLY when at least one SPEC failed. Its absence means no failures — not
   * that the field is unavailable — so a client should treat missing as empty rather than
   * unknown. A run can be partially successful: `added` and `errors` are both meaningful in the
   * same response, and the status is still 200.
   *
   * `POST /api/v1/registry/admin/specs/seed`
   */
  seedStarterSpecs(options?: RequestOptions): Promise<SeedStarterSpecsResponse> {
    return this._client.request({
      method: 'POST',
      path: '/api/v1/registry/admin/specs/seed',
      idempotent: true,
      options,
    });
  }

  /**
   * Make a SPEC public or private (super-admin)
   *
   * PATCH and only PATCH — a PUT is 405 with the allowed methods named.
   *
   * WRITE SEMANTICS: replaces the visibility, which is the only field this route touches.
   * `visibility` is required and must be exactly `public` or `private`; anything else, including
   * a missing body or a near-miss like `unlisted`, is 400 rather than being coerced or ignored.
   *
   * `PATCH /api/v1/registry/admin/specs/{scope}/{name}/visibility`
   */
  setRegistrySpecVisibility(scope: string, name: string, body: SetRegistrySpecVisibilityRequest, options?: RequestOptions): Promise<SetRegistrySpecVisibilityResponse> {
    return this._client.request({
      method: 'PATCH',
      path: `/api/v1/registry/admin/specs/${encodeURIComponent(String(scope))}/${encodeURIComponent(String(name))}/visibility`,
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * Remove a SPEC from the featured set (super-admin)
   *
   * Same shape and same guards as the POST; the response's `featured` is simply `false`.
   * Idempotent — un-featuring something that was not featured is 200.
   *
   * `DELETE /api/v1/registry/admin/specs/{scope}/{name}/feature`
   */
  unfeatureRegistrySpec(scope: string, name: string, options?: RequestOptions): Promise<RegistrySpecFeatureState> {
    return this._client.request({
      method: 'DELETE',
      path: `/api/v1/registry/admin/specs/${encodeURIComponent(String(scope))}/${encodeURIComponent(String(name))}/feature`,
      idempotent: true,
      options,
    });
  }
}
