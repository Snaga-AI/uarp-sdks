// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

import { APIResource } from '../../core/resource.js';
import type { RequestOptions } from '../../core/transport.js';
import { pick } from '../../core/util.js';
import type {
  CreateProjectRequest,
  DeleteProjectResponse,
  GetRunChangedFiles,
  ListProjectsResponse,
  Project,
  ProjectDetail,
  UpdateProjectRequest,
} from '../models.js';

/**
 * Query and header parameters for `listProjects`.
 */
export interface ListProjectsParams {
  /**
   * `true` returns the archived projects instead of the live ones.
   */
  archived?: GetRunChangedFiles;
}

/**
 * Operations tagged `Projects`.
 */
export class ProjectsResource extends APIResource {
  /**
   * Create a project
   *
   * File ids that do not resolve in this tenant are dropped rather than rejected, so the stored
   * project never claims a file the agent cannot read.
   *
   * `POST /api/v1/projects`
   */
  create(body: CreateProjectRequest, options?: RequestOptions): Promise<Project> {
    return this._client.request({
      method: 'POST',
      path: '/api/v1/projects',
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * Delete a project
   *
   * Unlinks rather than cascades: every chat filed under the project survives and is simply no
   * longer in it. `chats_kept` is how many were unfiled. Deleting a folder that takes the work
   * with it is how people lose things they cannot get back.
   *
   * `DELETE /api/v1/projects/{projectId}`
   */
  delete(projectId: string, options?: RequestOptions): Promise<DeleteProjectResponse> {
    return this._client.request({
      method: 'DELETE',
      path: `/api/v1/projects/${encodeURIComponent(String(projectId))}`,
      idempotent: true,
      options,
    });
  }

  /**
   * Read a project with its chats
   *
   * Includes the chats filed under it and the caller's own access level. A project the caller
   * may not open answers **404, not 403** — the existence of a private project is itself
   * private.
   *
   * `GET /api/v1/projects/{projectId}`
   */
  get(projectId: string, options?: RequestOptions): Promise<ProjectDetail> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/projects/${encodeURIComponent(String(projectId))}`,
      options,
    });
  }

  /**
   * List projects
   *
   * Live projects, most recently updated first. Archived ones leave this list — ask for them
   * with `archived=true`. `archived_count` is always the number of archived projects the caller
   * can see, whichever list was requested, so a client can badge the Archived section without a
   * second call.
   *
   * `GET /api/v1/projects`
   */
  list(params?: ListProjectsParams, options?: RequestOptions): Promise<ListProjectsResponse> {
    return this._client.request({
      method: 'GET',
      path: '/api/v1/projects',
      query: pick(params, ['archived']),
      options,
    });
  }

  /**
   * Edit a project
   *
   * Only fields present in the body are changed. `archived_at` is the archive switch: an ISO
   * timestamp archives, and **null restores** — the field must be sent explicitly, because an
   * omitted key means “leave as is”.
   *
   * View access is 403; no access is a project the caller may not open answers **404, not 403**
   * — the existence of a private project is itself private.
   *
   * Turning on `private` for a project with no recorded owner and no signed-in caller is refused
   * with 400: it would leave nobody able to open it. Signing in stamps the caller as owner at
   * that moment.
   *
   * `PATCH /api/v1/projects/{projectId}`
   */
  update(projectId: string, body: UpdateProjectRequest, options?: RequestOptions): Promise<Project> {
    return this._client.request({
      method: 'PATCH',
      path: `/api/v1/projects/${encodeURIComponent(String(projectId))}`,
      body,
      idempotent: true,
      options,
    });
  }
}
