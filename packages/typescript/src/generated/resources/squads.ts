// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

import { APIResource } from '../../core/resource.js';
import type { RequestOptions } from '../../core/transport.js';
import { pick } from '../../core/util.js';
import type { EventStream } from '../../core/sse.js';
import type {
  AddSquadGraphEdgeRequest,
  AddSquadGraphNodeRequest,
  CancelSquadRunResponse,
  DeleteSquadGraphEdgeResponse,
  DeleteSquadGraphNodeResponse,
  DeleteSquadResponse,
  GetSquadChatHistoryResponse,
  GetSquadGraphResponse,
  GetSquadRunMessagesResponse,
  JsonObject,
  ListSquadGraphEdgesResponse,
  ListSquadGraphNodesResponse,
  ListSquadRunsResponse,
  ListSquadsResponse,
  StartSquadRunRequest,
  StartSquadRunResponse,
  Team,
  TeamCreate,
  TeamGraphNode,
  TeamRunDetail,
  TeamUpdate,
  UpdateSquadGraphNodeRequest,
} from '../models.js';

/**
 * Query and header parameters for `getSquadChatHistory`.
 */
export interface GetSquadChatHistoryParams {
  thread_id?: string;
  include_internal?: boolean;
}

/**
 * Query and header parameters for `streamSquadChatEvents`.
 */
export interface StreamSquadChatEventsParams {
  thread_id?: string;
}

/**
 * Squads — multi-agent groups that run a shared brief
 */
export class SquadsResource extends APIResource {
  /**
   * Add graph edge
   *
   * `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
   * older noun: the server rewrites the leading path segment before dispatch, so the two are one
   * endpoint and cannot drift apart.
   *
   * `POST /api/v1/squads/{squadId}/graph/edges`
   */
  addSquadGraphEdge(squadId: string, body: AddSquadGraphEdgeRequest, options?: RequestOptions): Promise<JsonObject> {
    return this._client.request({
      method: 'POST',
      path: `/api/v1/squads/${encodeURIComponent(String(squadId))}/graph/edges`,
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * Add graph node
   *
   * `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
   * older noun: the server rewrites the leading path segment before dispatch, so the two are one
   * endpoint and cannot drift apart.
   *
   * `POST /api/v1/squads/{squadId}/graph/nodes`
   */
  addSquadGraphNode(squadId: string, body: AddSquadGraphNodeRequest, options?: RequestOptions): Promise<JsonObject> {
    return this._client.request({
      method: 'POST',
      path: `/api/v1/squads/${encodeURIComponent(String(squadId))}/graph/nodes`,
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * Cancel a squad run
   *
   * Stops the orchestration loop first, then every non-terminal child run, and releases the chat
   * state so the canvas does not stay locked on a run that was just killed.
   *
   * Order matters and is not an implementation detail: killing children while the loop is still
   * running makes it spawn more — two fresh child runs were measured within two minutes of a
   * “successful” cancel.
   *
   * `cancelledCount` is camelCase on the wire, unlike every neighbouring field. That is what the
   * server sends.
   *
   * `/api/v1/squads/*` is the canonical surface; `/api/v1/teams/*` is the same handler under the
   * older noun.
   *
   * `POST /api/v1/squads/{squadId}/runs/{teamRunId}/cancel`
   */
  cancelSquadRun(squadId: string, teamRunId: string, options?: RequestOptions): Promise<CancelSquadRunResponse> {
    return this._client.request({
      method: 'POST',
      path: `/api/v1/squads/${encodeURIComponent(String(squadId))}/runs/${encodeURIComponent(String(teamRunId))}/cancel`,
      idempotent: true,
      options,
    });
  }

  /**
   * Create a squad
   *
   * `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
   * older noun: the server rewrites the leading path segment before dispatch, so the two are one
   * endpoint and cannot drift apart.
   *
   * `POST /api/v1/squads`
   */
  create(body: TeamCreate, options?: RequestOptions): Promise<Team> {
    return this._client.request({
      method: 'POST',
      path: '/api/v1/squads',
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * Delete squad
   *
   * `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
   * older noun: the server rewrites the leading path segment before dispatch, so the two are one
   * endpoint and cannot drift apart.
   *
   * `DELETE /api/v1/squads/{squadId}`
   */
  delete(squadId: string, options?: RequestOptions): Promise<DeleteSquadResponse> {
    return this._client.request({
      method: 'DELETE',
      path: `/api/v1/squads/${encodeURIComponent(String(squadId))}`,
      idempotent: true,
      options,
    });
  }

  /**
   * Remove edge
   *
   * `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
   * older noun: the server rewrites the leading path segment before dispatch, so the two are one
   * endpoint and cannot drift apart.
   *
   * `DELETE /api/v1/squads/{squadId}/graph/edges/{edgeId}`
   */
  deleteSquadGraphEdge(squadId: string, edgeId: string, options?: RequestOptions): Promise<DeleteSquadGraphEdgeResponse> {
    return this._client.request({
      method: 'DELETE',
      path: `/api/v1/squads/${encodeURIComponent(String(squadId))}/graph/edges/${encodeURIComponent(String(edgeId))}`,
      idempotent: true,
      options,
    });
  }

  /**
   * Remove node
   *
   * `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
   * older noun: the server rewrites the leading path segment before dispatch, so the two are one
   * endpoint and cannot drift apart.
   *
   * `DELETE /api/v1/squads/{squadId}/graph/nodes/{agentId}`
   */
  deleteSquadGraphNode(squadId: string, agentId: string, options?: RequestOptions): Promise<DeleteSquadGraphNodeResponse> {
    return this._client.request({
      method: 'DELETE',
      path: `/api/v1/squads/${encodeURIComponent(String(squadId))}/graph/nodes/${encodeURIComponent(String(agentId))}`,
      idempotent: true,
      options,
    });
  }

  /**
   * Get squad
   *
   * `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
   * older noun: the server rewrites the leading path segment before dispatch, so the two are one
   * endpoint and cannot drift apart.
   *
   * `GET /api/v1/squads/{squadId}`
   */
  get(squadId: string, options?: RequestOptions): Promise<Team> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/squads/${encodeURIComponent(String(squadId))}`,
      options,
    });
  }

  /**
   * Get squad chat history
   *
   * `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
   * older noun: the server rewrites the leading path segment before dispatch, so the two are one
   * endpoint and cannot drift apart.
   *
   * `GET /api/v1/squads/{squadId}/chat`
   */
  getSquadChatHistory(squadId: string, params?: GetSquadChatHistoryParams, options?: RequestOptions): Promise<GetSquadChatHistoryResponse> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/squads/${encodeURIComponent(String(squadId))}/chat`,
      query: pick(params, ['thread_id', 'include_internal']),
      options,
    });
  }

  /**
   * Get full squad graph
   *
   * `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
   * older noun: the server rewrites the leading path segment before dispatch, so the two are one
   * endpoint and cannot drift apart.
   *
   * `GET /api/v1/squads/{squadId}/graph`
   */
  getSquadGraph(squadId: string, options?: RequestOptions): Promise<GetSquadGraphResponse> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/squads/${encodeURIComponent(String(squadId))}/graph`,
      options,
    });
  }

  /**
   * Get graph node
   *
   * `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
   * older noun: the server rewrites the leading path segment before dispatch, so the two are one
   * endpoint and cannot drift apart.
   *
   * `GET /api/v1/squads/{squadId}/graph/nodes/{agentId}`
   */
  getSquadGraphNode(squadId: string, agentId: string, options?: RequestOptions): Promise<TeamGraphNode> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/squads/${encodeURIComponent(String(squadId))}/graph/nodes/${encodeURIComponent(String(agentId))}`,
      options,
    });
  }

  /**
   * Get specific squad run
   *
   * `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
   * older noun: the server rewrites the leading path segment before dispatch, so the two are one
   * endpoint and cannot drift apart.
   *
   * `GET /api/v1/squads/{squadId}/runs/{teamRunId}`
   */
  getSquadRun(squadId: string, teamRunId: string, options?: RequestOptions): Promise<TeamRunDetail> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/squads/${encodeURIComponent(String(squadId))}/runs/${encodeURIComponent(String(teamRunId))}`,
      options,
    });
  }

  /**
   * Get squad run messages
   *
   * `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
   * older noun: the server rewrites the leading path segment before dispatch, so the two are one
   * endpoint and cannot drift apart.
   *
   * `GET /api/v1/squads/{squadId}/runs/{teamRunId}/messages`
   */
  getSquadRunMessages(squadId: string, teamRunId: string, options?: RequestOptions): Promise<GetSquadRunMessagesResponse> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/squads/${encodeURIComponent(String(squadId))}/runs/${encodeURIComponent(String(teamRunId))}/messages`,
      options,
    });
  }

  /**
   * List squads
   *
   * `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
   * older noun: the server rewrites the leading path segment before dispatch, so the two are one
   * endpoint and cannot drift apart.
   *
   * `GET /api/v1/squads`
   */
  list(options?: RequestOptions): Promise<ListSquadsResponse> {
    return this._client.request({
      method: 'GET',
      path: '/api/v1/squads',
      options,
    });
  }

  /**
   * List graph edges
   *
   * `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
   * older noun: the server rewrites the leading path segment before dispatch, so the two are one
   * endpoint and cannot drift apart.
   *
   * `GET /api/v1/squads/{squadId}/graph/edges`
   */
  listSquadGraphEdges(squadId: string, options?: RequestOptions): Promise<ListSquadGraphEdgesResponse> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/squads/${encodeURIComponent(String(squadId))}/graph/edges`,
      options,
    });
  }

  /**
   * List graph nodes
   *
   * `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
   * older noun: the server rewrites the leading path segment before dispatch, so the two are one
   * endpoint and cannot drift apart.
   *
   * `GET /api/v1/squads/{squadId}/graph/nodes`
   */
  listSquadGraphNodes(squadId: string, options?: RequestOptions): Promise<ListSquadGraphNodesResponse> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/squads/${encodeURIComponent(String(squadId))}/graph/nodes`,
      options,
    });
  }

  /**
   * List runs for a squad
   *
   * `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
   * older noun: the server rewrites the leading path segment before dispatch, so the two are one
   * endpoint and cannot drift apart.
   *
   * `GET /api/v1/squads/{squadId}/runs`
   */
  listSquadRuns(squadId: string, options?: RequestOptions): Promise<ListSquadRunsResponse> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/squads/${encodeURIComponent(String(squadId))}/runs`,
      options,
    });
  }

  /**
   * Start a squad run
   *
   * `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
   * older noun: the server rewrites the leading path segment before dispatch, so the two are one
   * endpoint and cannot drift apart.
   *
   * `POST /api/v1/squads/{squadId}/runs`
   */
  startSquadRun(squadId: string, body: StartSquadRunRequest, options?: RequestOptions): Promise<StartSquadRunResponse> {
    return this._client.request({
      method: 'POST',
      path: `/api/v1/squads/${encodeURIComponent(String(squadId))}/runs`,
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * SSE stream for squad chat
   *
   * `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
   * older noun: the server rewrites the leading path segment before dispatch, so the two are one
   * endpoint and cannot drift apart.
   *
   * `GET /api/v1/squads/{squadId}/chat/events`
   *
   * Required scopes: `events:read`.
   *
   * Returns a server-sent event stream; iterate it with `for await`.
   */
  streamSquadChatEvents(squadId: string, params?: StreamSquadChatEventsParams, options?: RequestOptions): EventStream {
    return this._client.stream({
      method: 'GET',
      path: `/api/v1/squads/${encodeURIComponent(String(squadId))}/chat/events`,
      query: pick(params, ['thread_id']),
      options,
    });
  }

  /**
   * Stream squad run events (SSE)
   *
   * `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
   * older noun: the server rewrites the leading path segment before dispatch, so the two are one
   * endpoint and cannot drift apart.
   *
   * `GET /api/v1/squads/{squadId}/runs/{runId}/events`
   *
   * Required scopes: `events:read`.
   *
   * Returns a server-sent event stream; iterate it with `for await`.
   */
  streamSquadRunEvents(squadId: string, runId: string, options?: RequestOptions): EventStream {
    return this._client.stream({
      method: 'GET',
      path: `/api/v1/squads/${encodeURIComponent(String(squadId))}/runs/${encodeURIComponent(String(runId))}/events`,
      options,
    });
  }

  /**
   * Update squad
   *
   * `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
   * older noun: the server rewrites the leading path segment before dispatch, so the two are one
   * endpoint and cannot drift apart.
   *
   * `PUT /api/v1/squads/{squadId}`
   */
  update(squadId: string, body: TeamUpdate, options?: RequestOptions): Promise<Team> {
    return this._client.request({
      method: 'PUT',
      path: `/api/v1/squads/${encodeURIComponent(String(squadId))}`,
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * Update graph node
   *
   * `/api/v1/squads/*` is the canonical surface. `/api/v1/teams/*` is the same handler under the
   * older noun: the server rewrites the leading path segment before dispatch, so the two are one
   * endpoint and cannot drift apart.
   *
   * `PATCH /api/v1/squads/{squadId}/graph/nodes/{agentId}`
   */
  updateSquadGraphNode(squadId: string, agentId: string, body: UpdateSquadGraphNodeRequest, options?: RequestOptions): Promise<JsonObject> {
    return this._client.request({
      method: 'PATCH',
      path: `/api/v1/squads/${encodeURIComponent(String(squadId))}/graph/nodes/${encodeURIComponent(String(agentId))}`,
      body,
      idempotent: true,
      options,
    });
  }
}
