// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

import { APIResource } from '../../core/resource.js';
import type { RequestOptions } from '../../core/transport.js';
import { pick } from '../../core/util.js';
import { autoPaginate } from '../../core/pagination.js';
import type {
  Agent,
  AgentUpdate,
  AiSystemCard,
  CreateAgentFriaRequest,
  CreateAgentRequest,
  CreateAgentVersionRequest,
  FriaReport,
  GetAgentActivityStatsResponse,
  GetAgentIdentityResponse,
  GetAgentSystemCardFormat,
  GetAgentTrafficResponse,
  GetAgentVersionDiffResponse,
  JsonObject,
  JsonValue,
  ListAgentMailResponse,
  ListAgentVersionsResponse,
  ListAgentsResponse,
  ResetAgentResponse,
  RiskClassification,
  RiskClassificationUpdate,
  RollbackAgentRequest,
  RotateAgentIdentityResponse,
  SetAgentCapabilitiesResponse,
  SetAgentTrafficRequest,
  SpecToolCatalog,
  SuspendAgentRequest,
  TerminateAgentResponse,
} from '../models.js';

/**
 * Query and header parameters for `getAgentActivityStats`.
 */
export interface GetAgentActivityStatsParams {
  days?: number;
}

/**
 * Query and header parameters for `getAgentSystemCard`.
 */
export interface GetAgentSystemCardParams {
  /**
   * Output format (default: json)
   */
  format?: GetAgentSystemCardFormat;
}

/**
 * Query and header parameters for `getAgentVersionDiff`.
 */
export interface GetAgentVersionDiffParams {
  /**
   * Version number to compare against (defaults to versionNum-1)
   */
  compare_to?: number;
}

/**
 * Query and header parameters for `listAgents`.
 */
export interface ListAgentsParams {
  /**
   * Filter by workspace
   */
  workspace_id?: string;
  limit?: number;
  /**
   * Opaque pagination cursor returned by previous page response. Use `has_more` to detect if
   * more pages exist.
   */
  cursor?: string;
  /**
   * Include bridge agents whose Snaga CLI is not currently `serve`-ing. By default offline
   * ghosts are hidden from main pickers.
   */
  include_offline?: boolean;
}

/**
 * Query and header parameters for `listAgentMail`.
 */
export interface ListAgentMailParams {
  /**
   * Restrict to one thread — a cheaper scan than filtering after the fact.
   */
  thread_id?: string;
  /**
   * Messages to OR from this agent.
   */
  agent_id?: string;
  limit?: number;
}

/**
 * Agent CRUD and versioning
 */
export class AgentsResource extends APIResource {
  /**
   * Activate agent
   *
   * `POST /api/v1/agents/{agentId}/activate`
   *
   * Required scopes: `agents:write`.
   */
  activateAgent(agentId: string, options?: RequestOptions): Promise<JsonObject> {
    return this._client.request({
      method: 'POST',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/activate`,
      idempotent: true,
      options,
    });
  }

  /**
   * Create an agent
   *
   * `POST /api/v1/agents`
   *
   * Required scopes: `agents:write`.
   */
  create(body: CreateAgentRequest, options?: RequestOptions): Promise<Agent> {
    return this._client.request({
      method: 'POST',
      path: '/api/v1/agents',
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * Create or update FRIA report
   *
   * `POST /api/v1/agents/{agentId}/fria`
   *
   * Required scopes: `agents:write`.
   */
  createAgentFria(agentId: string, body: CreateAgentFriaRequest, options?: RequestOptions): Promise<FriaReport> {
    return this._client.request({
      method: 'POST',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/fria`,
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * Create a version snapshot of an agent
   *
   * `POST /api/v1/agents/{agentId}/versions`
   *
   * Required scopes: `agents:write`.
   */
  createAgentVersion(agentId: string, body?: CreateAgentVersionRequest, options?: RequestOptions): Promise<JsonValue> {
    return this._client.request({
      method: 'POST',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/versions`,
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * Delete an agent
   *
   * `DELETE /api/v1/agents/{agentId}`
   *
   * Required scopes: `agents:write`.
   */
  delete(agentId: string, options?: RequestOptions): Promise<JsonValue> {
    return this._client.request({
      method: 'DELETE',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}`,
      idempotent: true,
      options,
    });
  }

  /**
   * Delete agent identity
   *
   * `DELETE /api/v1/agents/{agentId}/identity`
   *
   * Required scopes: `agents:write`.
   */
  deleteAgentIdentity(agentId: string, options?: RequestOptions): Promise<void> {
    return this._client.request({
      method: 'DELETE',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/identity`,
      idempotent: true,
      responseType: 'void',
      options,
    });
  }

  /**
   * Get an agent
   *
   * `GET /api/v1/agents/{agentId}`
   *
   * Required scopes: `agents:read`.
   */
  get(agentId: string, options?: RequestOptions): Promise<Agent> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}`,
      options,
    });
  }

  /**
   * Get agent activity stats
   *
   * `GET /api/v1/agents/{agentId}/activity-stats`
   *
   * Required scopes: `agents:read`.
   */
  getAgentActivityStats(agentId: string, params?: GetAgentActivityStatsParams, options?: RequestOptions): Promise<GetAgentActivityStatsResponse> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/activity-stats`,
      query: pick(params, ['days']),
      options,
    });
  }

  /**
   * Get agent capability manifest
   *
   * `GET /api/v1/agents/{agentId}/capabilities`
   *
   * Required scopes: `agents:read`.
   */
  getAgentCapabilities(agentId: string, options?: RequestOptions): Promise<JsonObject> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/capabilities`,
      options,
    });
  }

  /**
   * Get Fundamental Rights Impact Assessment
   *
   * `GET /api/v1/agents/{agentId}/fria`
   *
   * Required scopes: `agents:read`.
   */
  getAgentFria(agentId: string, options?: RequestOptions): Promise<FriaReport> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/fria`,
      options,
    });
  }

  /**
   * Get agent identity (public key)
   *
   * `GET /api/v1/agents/{agentId}/identity`
   *
   * Required scopes: `agents:read`.
   */
  getAgentIdentity(agentId: string, options?: RequestOptions): Promise<GetAgentIdentityResponse> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/identity`,
      options,
    });
  }

  /**
   * Read the EU AI Act risk classification
   *
   * `GET /api/v1/agents/{agentId}/risk-classification`
   *
   * Required scopes: `agents:read`.
   */
  getAgentRiskClassification(agentId: string, options?: RequestOptions): Promise<RiskClassification> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/risk-classification`,
      options,
    });
  }

  /**
   * Tool → output-view catalog
   *
   * What the builder needs to render a tool result: for each tool the agent can actually call,
   * the SPEC that owns it and the view to render its output with.
   *
   * Only tools the agent is entitled to are listed — the same filter the runtime applies — so
   * the UI cannot offer a view for a tool that will never fire. A stored view whose JSON will
   * not parse is skipped, not fatal: one broken view must not take the catalog down.
   *
   * `GET /api/v1/agents/{agentId}/spec-catalog`
   *
   * Required scopes: `agents:read`.
   */
  getAgentSpecCatalog(agentId: string, options?: RequestOptions): Promise<SpecToolCatalog> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/spec-catalog`,
      options,
    });
  }

  /**
   * Get EU AI Act Annex IV system card
   *
   * `GET /api/v1/agents/{agentId}/system-card`
   *
   * Required scopes: `agents:read`.
   */
  getAgentSystemCard(agentId: string, params?: GetAgentSystemCardParams, options?: RequestOptions): Promise<AiSystemCard> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/system-card`,
      query: pick(params, ['format']),
      options,
    });
  }

  /**
   * Get traffic split configuration
   *
   * `GET /api/v1/agents/{agentId}/traffic`
   *
   * Required scopes: `agents:read`.
   */
  getAgentTraffic(agentId: string, options?: RequestOptions): Promise<GetAgentTrafficResponse> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/traffic`,
      options,
    });
  }

  /**
   * Diff between two agent versions
   *
   * `GET /api/v1/agents/{agentId}/versions/{versionNum}/diff`
   *
   * Required scopes: `agents:read`.
   */
  getAgentVersionDiff(agentId: string, versionNum: number, params?: GetAgentVersionDiffParams, options?: RequestOptions): Promise<GetAgentVersionDiffResponse> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/versions/${encodeURIComponent(String(versionNum))}/diff`,
      query: pick(params, ['compare_to']),
      options,
    });
  }

  /**
   * List all agents
   *
   * `GET /api/v1/agents`
   *
   * Required scopes: `agents:read`.
   */
  list(params?: ListAgentsParams, options?: RequestOptions): Promise<ListAgentsResponse> {
    return this._client.request({
      method: 'GET',
      path: '/api/v1/agents',
      query: pick(params, ['workspace_id', 'limit', 'cursor', 'include_offline']),
      options,
    });
  }

  /**
   * Iterate every item returned by `listAgents`, following the `cursor` cursor until the server
   * reports no further pages.
   */
  listAll(params?: ListAgentsParams, options?: RequestOptions): AsyncIterableIterator<Agent> {
    return autoPaginate<Agent>(
      (cursor) => this.list({ ...params, cursor }, options),
      'items',
      'cursor',
      'has_more',
    );
  }

  /**
   * Messages between agents
   *
   * Newest first. `agent_id` matches a message in EITHER direction — sent or received — which is
   * what an inbox view of one agent means.
   *
   * `total_scanned` is how many messages the scan looked at before `limit` was applied, so a
   * client can tell a short page from an exhausted one.
   *
   * `GET /api/v1/agent-mail`
   *
   * Required scopes: `agents:read`.
   */
  listAgentMail(params?: ListAgentMailParams, options?: RequestOptions): Promise<ListAgentMailResponse> {
    return this._client.request({
      method: 'GET',
      path: '/api/v1/agent-mail',
      query: pick(params, ['thread_id', 'agent_id', 'limit']),
      options,
    });
  }

  /**
   * List version snapshots for an agent
   *
   * Returns the ordered version history for an agent. Lazily creates v1 from the current config
   * if no versions exist yet.
   *
   * `GET /api/v1/agents/{agentId}/versions`
   *
   * Required scopes: `agents:read`.
   */
  listAgentVersions(agentId: string, options?: RequestOptions): Promise<ListAgentVersionsResponse> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/versions`,
      options,
    });
  }

  /**
   * Partial update agent
   *
   * `PATCH /api/v1/agents/{agentId}`
   *
   * Required scopes: `agents:write`.
   */
  patch(agentId: string, body: JsonObject, options?: RequestOptions): Promise<Agent> {
    return this._client.request({
      method: 'PATCH',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}`,
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * Clear an agent's memory and conversation history
   *
   * Purges the agent's memory store and every session it owns — with their runs, events and
   * todos — plus any session-less runs. The agent RECORD is untouched: prompts, model, tools,
   * specs, workspaces and marketplace listings all survive. The same purge the super-agent's
   * cross-tenant `manageAgent("reset")` hook calls, deliberately shared rather than copied.
   *
   * Undeclared until 2026-08-31 while the console shipped a button for it — a route can be
   * missing from the contract and still be in people's hands. The two symptoms differ by client:
   * a hand-written client BUILDS a workaround (the web calls it with a direct POST, past the
   * SDK), while a generated one simply has no method and nobody notices. That makes the SDK side
   * the quieter of the two — there is not even a crutch to show someone came looking.
   *
   * `POST /api/v1/agents/{agentId}/reset`
   *
   * Required scopes: `agents:write`.
   */
  resetAgent(agentId: string, options?: RequestOptions): Promise<ResetAgentResponse> {
    return this._client.request({
      method: 'POST',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/reset`,
      idempotent: true,
      options,
    });
  }

  /**
   * Rollback an agent to a previous version
   *
   * `POST /api/v1/agents/{agentId}/rollback`
   *
   * Required scopes: `agents:write`.
   */
  rollbackAgent(agentId: string, body: RollbackAgentRequest, options?: RequestOptions): Promise<JsonValue> {
    return this._client.request({
      method: 'POST',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/rollback`,
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * Rotate agent identity
   *
   * `POST /api/v1/agents/{agentId}/identity/rotate`
   *
   * Required scopes: `agents:write`.
   */
  rotateAgentIdentity(agentId: string, options?: RequestOptions): Promise<RotateAgentIdentityResponse> {
    return this._client.request({
      method: 'POST',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/identity/rotate`,
      idempotent: true,
      options,
    });
  }

  /**
   * Set agent capabilities
   *
   * `PUT /api/v1/agents/{agentId}/capabilities`
   *
   * Required scopes: `agents:write`.
   */
  setAgentCapabilities(agentId: string, body: JsonObject, options?: RequestOptions): Promise<SetAgentCapabilitiesResponse> {
    return this._client.request({
      method: 'PUT',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/capabilities`,
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * Set traffic split configuration
   *
   * `PUT /api/v1/agents/{agentId}/traffic`
   *
   * Required scopes: `agents:write`.
   */
  setAgentTraffic(agentId: string, body: SetAgentTrafficRequest, options?: RequestOptions): Promise<JsonObject> {
    return this._client.request({
      method: 'PUT',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/traffic`,
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * Suspend agent
   *
   * `POST /api/v1/agents/{agentId}/suspend`
   *
   * Required scopes: `agents:write`.
   */
  suspendAgent(agentId: string, body?: SuspendAgentRequest, options?: RequestOptions): Promise<JsonObject> {
    return this._client.request({
      method: 'POST',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/suspend`,
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * Terminate/delete agent
   *
   * `POST /api/v1/agents/{agentId}/terminate`
   *
   * Required scopes: `agents:write`.
   */
  terminateAgent(agentId: string, options?: RequestOptions): Promise<TerminateAgentResponse> {
    return this._client.request({
      method: 'POST',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/terminate`,
      idempotent: true,
      options,
    });
  }

  /**
   * Update an agent
   *
   * `PUT /api/v1/agents/{agentId}`
   *
   * Required scopes: `agents:write`.
   */
  update(agentId: string, body?: AgentUpdate, options?: RequestOptions): Promise<Agent> {
    return this._client.request({
      method: 'PUT',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}`,
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * Update EU AI Act risk classification (admin only)
   *
   * `PATCH /api/v1/agents/{agentId}/risk-classification`
   *
   * Required scopes: `agents:write`.
   */
  updateAgentRiskClassification(agentId: string, body: RiskClassificationUpdate, options?: RequestOptions): Promise<Agent> {
    return this._client.request({
      method: 'PATCH',
      path: `/api/v1/agents/${encodeURIComponent(String(agentId))}/risk-classification`,
      body,
      idempotent: true,
      options,
    });
  }
}
