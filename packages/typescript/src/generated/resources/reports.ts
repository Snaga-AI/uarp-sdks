// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

import { APIResource } from '../../core/resource.js';
import type { RequestOptions } from '../../core/transport.js';
import { pick } from '../../core/util.js';
import { autoPaginate } from '../../core/pagination.js';
import type {
  ContentReport,
  ContentReportAccepted,
  ContentReportInput,
  ListAllContentReportsResponse,
  ListContentReportsResponse,
} from '../models.js';

/**
 * Query and header parameters for `listAllContentReports`.
 */
export interface ListAllContentReportsParams {
  limit?: number;
  cursor?: string;
}

/**
 * Query and header parameters for `listContentReports`.
 */
export interface ListContentReportsParams {
  limit?: number;
  cursor?: string;
}

/**
 * Content reports — abuse/moderation reporting for authenticated users and anonymous
 * public-chat visitors, plus the operator inbox
 */
export class ReportsResource extends APIResource {
  /**
   * File a content report
   *
   * Report a message, session or agent. Available to any authenticated principal — deliberately
   * not role- or scope-gated, since reporting abuse must never be blocked by RBAC. Rate limited
   * to 10/minute per caller. `self_harm` is still accepted with 202 but raises the operator
   * notification to critical priority.
   *
   * `POST /api/v1/reports`
   */
  createContentReport(body: ContentReportInput, options?: RequestOptions): Promise<ContentReportAccepted> {
    return this._client.request({
      method: 'POST',
      path: '/api/v1/reports',
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * List content reports platform-wide (super-admin)
   *
   * Every report across every tenant, newest first. A tenant whose agent is the problem must not
   * be the only party holding the evidence.
   *
   * `GET /api/v1/admin/reports`
   *
   * Required scopes: `admin`.
   */
  listAllContentReports(params?: ListAllContentReportsParams, options?: RequestOptions): Promise<ListAllContentReportsResponse> {
    return this._client.request({
      method: 'GET',
      path: '/api/v1/admin/reports',
      query: pick(params, ['limit', 'cursor']),
      options,
    });
  }

  /**
   * Iterate every item returned by `listAllContentReports`, following the `cursor` cursor until
   * the server reports no further pages.
   */
  listAllContentReportsAll(params?: ListAllContentReportsParams, options?: RequestOptions): AsyncIterableIterator<ContentReport> {
    return autoPaginate<ContentReport>(
      (cursor) => this.listAllContentReports({ ...params, cursor }, options),
      'items',
      'cursor',
      'has_more',
    );
  }

  /**
   * List content reports for this tenant (admin+)
   *
   * Moderation queue for the tenant that owns the reported content, newest first. Requires the
   * admin role — reports carry reporter free text and there is no moderation duty below admin.
   *
   * `GET /api/v1/reports`
   */
  listContentReports(params?: ListContentReportsParams, options?: RequestOptions): Promise<ListContentReportsResponse> {
    return this._client.request({
      method: 'GET',
      path: '/api/v1/reports',
      query: pick(params, ['limit', 'cursor']),
      options,
    });
  }

  /**
   * Iterate every item returned by `listContentReports`, following the `cursor` cursor until the
   * server reports no further pages.
   */
  listContentReportsAll(params?: ListContentReportsParams, options?: RequestOptions): AsyncIterableIterator<ContentReport> {
    return autoPaginate<ContentReport>(
      (cursor) => this.listContentReports({ ...params, cursor }, options),
      'items',
      'cursor',
      'has_more',
    );
  }
}
