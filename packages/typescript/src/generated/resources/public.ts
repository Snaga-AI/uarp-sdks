// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

import { APIResource } from '../../core/resource.js';
import type { RequestOptions } from '../../core/transport.js';
import { pick } from '../../core/util.js';
import type { EventStream } from '../../core/sse.js';
import { autoPaginate } from '../../core/pagination.js';
import type {
  ContentReportAccepted,
  ContentReportInput,
  CreatePublicSessionRequest,
  CreatePublicSessionResponse,
  GetPublicFeaturedAgentResponse,
  GetRegistrationStatusResponse,
  JsonObject,
  LandingOverrides,
  ListPublicIntegrationsResponse,
  ListPublicPlansResponse,
  ListPublicStatesResponse,
  ListPublicTenantsResponse,
  MaintenanceStatus,
  PlatformInfo,
  PublicDomainLookupResponse,
  PublicTenant,
  PublicTrackEventRequest,
  RespondToPublicHitlRequest,
  SendPublicMessageRequest,
  SendPublicMessageResponse,
} from '../models.js';

/**
 * Query and header parameters for `listPublicTenants`.
 */
export interface ListPublicTenantsParams {
  category?: string;
  sort?: string;
  search?: string;
  limit?: number;
  cursor?: string;
}

/**
 * Query and header parameters for `publicDomainLookup`.
 */
export interface PublicDomainLookupParams {
  domain: string;
}

/**
 * Unauthenticated public-facing endpoints
 */
export class PublicResource extends APIResource {
  /**
   * Create public session
   *
   * `POST /api/v1/public/sessions`
   */
  createPublicSession(body: CreatePublicSessionRequest, options?: RequestOptions): Promise<CreatePublicSessionResponse> {
    return this._client.request({
      method: 'POST',
      path: '/api/v1/public/sessions',
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * Report abusive content from a public chat session
   *
   * Anonymous abuse report filed by a public-chat visitor. Requires no authentication of any
   * kind — the session id in the path is the scope, and it must resolve in the public-session
   * index. Rate limited per session (5/hour) and per IP (10/hour). The report lands in the
   * moderation queue of the tenant that owns the agent and raises an in-product notification
   * there.
   *
   * `POST /api/v1/public/sessions/{sessionId}/reports`
   */
  createPublicSessionReport(sessionId: string, body: ContentReportInput, options?: RequestOptions): Promise<ContentReportAccepted> {
    return this._client.request({
      method: 'POST',
      path: `/api/v1/public/sessions/${encodeURIComponent(String(sessionId))}/reports`,
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * Public landing overrides
   *
   * No authentication. Text overrides and partner logos for the landing page.
   *
   * `GET /api/v1/public/landing/overrides`
   */
  getLandingOverrides(options?: RequestOptions): Promise<LandingOverrides> {
    return this._client.request({
      method: 'GET',
      path: '/api/v1/public/landing/overrides',
      options,
    });
  }

  /**
   * Maintenance state
   *
   * No authentication, by design: during maintenance the authenticated surface is exactly what a
   * client cannot reach, so asking “is it me or is it you” must not itself require a session.
   *
   * A read failure answers `enabled: false` — the platform is assumed open unless it is known to
   * be closed.
   *
   * `GET /api/v1/maintenance/status`
   */
  getMaintenanceStatus(options?: RequestOptions): Promise<MaintenanceStatus> {
    return this._client.request({
      method: 'GET',
      path: '/api/v1/maintenance/status',
      options,
    });
  }

  /**
   * Public deployment info
   *
   * No authentication. Contact addresses and the public base URL as the operator configured
   * them, plus enough setup state to render a “being set up” banner.
   *
   * `GET /api/v1/public/platform-info`
   */
  getPlatformInfo(options?: RequestOptions): Promise<PlatformInfo> {
    return this._client.request({
      method: 'GET',
      path: '/api/v1/public/platform-info',
      options,
    });
  }

  /**
   * Get public agent card
   *
   * `GET /api/v1/public/agents/{agentId}`
   */
  getPublicAgentCard(agentId: string, options?: RequestOptions): Promise<JsonObject> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/public/agents/${encodeURIComponent(String(agentId))}`,
      options,
    });
  }

  /**
   * Get the public featured agent for the landing-page hero
   *
   * `GET /api/v1/public/landing/featured-agent`
   */
  getPublicFeaturedAgent(options?: RequestOptions): Promise<GetPublicFeaturedAgentResponse> {
    return this._client.request({
      method: 'GET',
      path: '/api/v1/public/landing/featured-agent',
      options,
    });
  }

  /**
   * Get public file content
   *
   * `GET /api/v1/public/files/{fileId}/content`
   */
  getPublicFileContent(fileId: string, options?: RequestOptions): Promise<Blob> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/public/files/${encodeURIComponent(String(fileId))}/content`,
      responseType: 'binary',
      options,
    });
  }

  /**
   * Get public session
   *
   * `GET /api/v1/public/sessions/{sessionId}`
   */
  getPublicSession(sessionId: string, options?: RequestOptions): Promise<JsonObject> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/public/sessions/${encodeURIComponent(String(sessionId))}`,
      options,
    });
  }

  /**
   * Get public state detail
   *
   * `GET /api/v1/public/states/{stateId}`
   */
  getPublicState(stateId: string, options?: RequestOptions): Promise<JsonObject> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/public/states/${encodeURIComponent(String(stateId))}`,
      options,
    });
  }

  /**
   * Get public tenant profile
   *
   * `GET /api/v1/public/tenants/{slug}`
   */
  getPublicTenantProfile(slug: string, options?: RequestOptions): Promise<JsonObject> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/public/tenants/${encodeURIComponent(String(slug))}`,
      options,
    });
  }

  /**
   * Tenant custom CSS stylesheet
   *
   * Serves tenant-configured branding CSS as `text/css`. Cached with ETag; safe for `<link
   * rel=stylesheet>`.
   *
   * `GET /api/v1/public/tenants/{slug}/style.css`
   */
  getPublicTenantStylesheet(slug: string, options?: RequestOptions): Promise<string> {
    return this._client.request({
      method: 'GET',
      path: `/api/v1/public/tenants/${encodeURIComponent(String(slug))}/style.css`,
      responseType: 'text',
      options,
    });
  }

  /**
   * Is sign-up open
   *
   * No authentication; cached for 30 seconds.
   *
   * **Fails open.** If the setting cannot be read the answer is `registration_open: true`,
   * because the worst case of guessing open is a missing notice, while guessing closed would
   * turn a storage blip into a closed front door. A client cannot distinguish the two — this
   * endpoint is the state, not a health check.
   *
   * `GET /api/v1/public/registration-status`
   */
  getRegistrationStatus(options?: RequestOptions): Promise<GetRegistrationStatusResponse> {
    return this._client.request({
      method: 'GET',
      path: '/api/v1/public/registration-status',
      options,
    });
  }

  /**
   * List the integrations a visitor could connect
   *
   * The connectors the platform offers today: the registry MINUS anything an admin has switched
   * off, which is the same answer `GET /integrations/catalog` gives a signed-in tenant — both
   * call one function, so a page rendered from this cannot advertise what the product refuses.
   *
   * It exists because a hand-kept list drifted: a marketing page counted the connector registry
   * and said twenty, naming three integrations that are not in the catalogue at all, while a
   * tenant was served seven. A number a page keeps by hand is a number that can be wrong; this
   * one cannot.
   *
   * Deliberately thinner than the tenant catalogue — no `config_schema`, because a visitor
   * deciding whether to sign up does not need to know which credential fields a connector wants.
   * Anonymous, and it says nothing about any tenant.
   *
   * `GET /api/v1/public/integrations`
   */
  listPublicIntegrations(options?: RequestOptions): Promise<ListPublicIntegrationsResponse> {
    return this._client.request({
      method: 'GET',
      path: '/api/v1/public/integrations',
      options,
    });
  }

  /**
   * List public plans
   *
   * `GET /api/v1/public/plans`
   */
  listPublicPlans(options?: RequestOptions): Promise<ListPublicPlansResponse> {
    return this._client.request({
      method: 'GET',
      path: '/api/v1/public/plans',
      options,
    });
  }

  /**
   * List public states
   *
   * `GET /api/v1/public/states`
   */
  listPublicStates(options?: RequestOptions): Promise<ListPublicStatesResponse> {
    return this._client.request({
      method: 'GET',
      path: '/api/v1/public/states',
      options,
    });
  }

  /**
   * List public tenants
   *
   * `GET /api/v1/public/tenants`
   */
  listPublicTenants(params?: ListPublicTenantsParams, options?: RequestOptions): Promise<ListPublicTenantsResponse> {
    return this._client.request({
      method: 'GET',
      path: '/api/v1/public/tenants',
      query: pick(params, ['category', 'sort', 'search', 'limit', 'cursor']),
      options,
    });
  }

  /**
   * Iterate every item returned by `listPublicTenants`, following the `cursor` cursor until the
   * server reports no further pages.
   */
  listPublicTenantsAll(params?: ListPublicTenantsParams, options?: RequestOptions): AsyncIterableIterator<PublicTenant> {
    return autoPaginate<PublicTenant>(
      (cursor) => this.listPublicTenants({ ...params, cursor }, options),
      'items',
      'cursor',
      'has_more',
    );
  }

  /**
   * Domain lookup
   *
   * `GET /api/v1/public/domain-lookup`
   */
  publicDomainLookup(params: PublicDomainLookupParams, options?: RequestOptions): Promise<PublicDomainLookupResponse> {
    return this._client.request({
      method: 'GET',
      path: '/api/v1/public/domain-lookup',
      query: pick(params, ['domain']),
      options,
    });
  }

  /**
   * Anonymous analytics beacon
   *
   * Fire-and-forget event tracker for landing-page funnels. No auth, no PII required;
   * rate-limited by IP.
   *
   * `POST /api/v1/public/track`
   */
  publicTrackEvent(body: PublicTrackEventRequest, options?: RequestOptions): Promise<void> {
    return this._client.request({
      method: 'POST',
      path: '/api/v1/public/track',
      body,
      idempotent: true,
      responseType: 'void',
      options,
    });
  }

  /**
   * Respond to public HITL
   *
   * `POST /api/v1/public/sessions/{sessionId}/respond`
   */
  respondToPublicHitl(sessionId: string, body: RespondToPublicHitlRequest, options?: RequestOptions): Promise<JsonObject> {
    return this._client.request({
      method: 'POST',
      path: `/api/v1/public/sessions/${encodeURIComponent(String(sessionId))}/respond`,
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * Send public message
   *
   * `POST /api/v1/public/sessions/{sessionId}/messages`
   */
  sendPublicMessage(sessionId: string, body: SendPublicMessageRequest, options?: RequestOptions): Promise<SendPublicMessageResponse> {
    return this._client.request({
      method: 'POST',
      path: `/api/v1/public/sessions/${encodeURIComponent(String(sessionId))}/messages`,
      body,
      idempotent: true,
      options,
    });
  }

  /**
   * SSE stream for public session
   *
   * `GET /api/v1/public/sessions/{sessionId}/events`
   *
   * Returns a server-sent event stream; iterate it with `for await`.
   */
  streamPublicSessionEvents(sessionId: string, options?: RequestOptions): EventStream {
    return this._client.stream({
      method: 'GET',
      path: `/api/v1/public/sessions/${encodeURIComponent(String(sessionId))}/events`,
      options,
    });
  }
}
