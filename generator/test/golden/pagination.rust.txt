==> src/generated/mod.rs <==
// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

pub mod api;
pub mod meta;
pub mod models;

==> src/generated/meta.rs <==
// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.

/// Version of the OpenAPI document this SDK was generated from.
pub const SPEC_VERSION: &str = "1.0.0";

/// Production base URL.
pub const DEFAULT_BASE_URL: &str = "https://api.test";

/// Every scope named by the UARP security scheme.
pub const SCOPES: [&str; 0] = [
];

==> src/generated/models.rs <==
// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.
//!
//! Data models for Pagination (spec version 1.0.0).

#![allow(unused_imports, clippy::large_enum_variant)]

use std::collections::HashMap;

use serde::{Deserialize, Serialize};

use crate::multipart::FilePart;

/// `Item` model.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct Item {
    pub id: String,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub label: Option<String>,
}

/// `ListBoxesResponse` model.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct ListBoxesResponse {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub boxes: Option<Vec<Item>>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cursor: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub total: Option<i64>,
}

/// `ListItemsResponse` model.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct ListItemsResponse {
    pub items: Vec<Item>,
    #[serde(default)]
    pub cursor: Option<String>,
    pub has_more: bool,
}

==> src/generated/api/mod.rs <==
// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.
//!
//! One module per OpenAPI tag.

pub mod items;

pub use items::*;

==> src/generated/api/items.rs <==
// Code generated by @uarp/codegen from spec/openapi.json. DO NOT EDIT.
//!
//! Operations tagged `Items`.

#![allow(unused_imports, clippy::too_many_arguments)]

use reqwest::Method;
use serde::{Deserialize, Serialize};
use futures_core::Stream;

use crate::client::{Client, Request, NO_BODY, NO_QUERY};
use crate::error::Result;
use crate::generated::models;
use crate::multipart::{field_text, FilePart};
use crate::pagination::CursorGuard;
use crate::util::encode_path;

/// Query and header parameters for `listItems`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct ListItemsParams {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cursor: Option<String>,
}

/// Query and header parameters for `listBoxes`.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct ListBoxesParams {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cursor: Option<String>,
}

/// Operations tagged `Items`.
#[derive(Debug, Clone)]
pub struct ItemsApi {
    pub(crate) client: Client,
}

impl Client {
    /// Operations tagged `Items`.
    pub fn items(&self) -> ItemsApi {
        ItemsApi { client: self.clone() }
    }
}

impl ItemsApi {
    /// `GET /items`
    pub async fn list(&self, params: &ListItemsParams) -> Result<models::ListItemsResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/items".to_string(),
                query: Some(params),
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Stream every item returned by `listItems`, following the `cursor` cursor until the server
    /// reports no further pages.
    pub fn list_all<'a>(&'a self, params: &'a ListItemsParams) -> impl Stream<Item = Result<models::Item>> + 'a {
        async_stream::try_stream! {
            let mut guard = CursorGuard::new();
            let mut cursor = params.cursor.clone();
            loop {
                let mut page_params = params.clone();
                page_params.cursor = cursor.clone();
                let page = self.list(&page_params).await?;
                let items = page.items;
                let was_empty = items.is_empty();
                for item in items {
                    yield item;
                }
                match guard.advance(page.cursor, Some(page.has_more), was_empty) {
                    Some(next) => cursor = Some(next),
                    None => break,
                }
            }
        }
    }

    /// `GET /boxes`
    pub async fn list_boxes(&self, params: &ListBoxesParams) -> Result<models::ListBoxesResponse> {
        self.client
            .request_json(Request {
                method: Method::GET,
                path: "/boxes".to_string(),
                query: Some(params),
                body: NO_BODY,
                headers: Vec::new(),
                idempotent: false,
            })
            .await
    }

    /// Stream every item returned by `listBoxes`, following the `cursor` cursor until the server
    /// reports no further pages.
    pub fn list_boxes_all<'a>(&'a self, params: &'a ListBoxesParams) -> impl Stream<Item = Result<models::Item>> + 'a {
        async_stream::try_stream! {
            let mut guard = CursorGuard::new();
            let mut cursor = params.cursor.clone();
            loop {
                let mut page_params = params.clone();
                page_params.cursor = cursor.clone();
                let page = self.list_boxes(&page_params).await?;
                let items = page.boxes.unwrap_or_default();
                let was_empty = items.is_empty();
                for item in items {
                    yield item;
                }
                match guard.advance(page.cursor, None, was_empty) {
                    Some(next) => cursor = Some(next),
                    None => break,
                }
            }
        }
    }
}
